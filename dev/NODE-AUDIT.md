# NODE-AUDIT.md — 「有效节点」的原则在真材料上对过账没有

> 用途：原则写在 `references/material-to-nodes.md`，识别与清洗的判据写在 `PIPELINE-SPEC` §1.6。
> **写下来不等于被跑过**——这份文档只回答两件事：
> ① **逐条原则，在真材料上是有实例、有反例，还是空白**（§一—§四）；
> ② **换一个没看过答案的执行者、照着规范读材料，能不能读出同一套节点**（§六—§七，盲读复现）。
> 空白的那几条就是下一次开工的活；反例的那几条**不许修文档去迁就产物**（产物错就是产物错）。
> 记号：**成立** = 真材料上有实例 · **反例** = 原则成立而产物没做到 · **空白** = 从没行使过。

## 一、对账用的四份真跑

| 记号 | 材料 / 产物 | 规模（现取，不手抄） | 用它对什么账 |
|---|---|---|---|
| 甲 | `output/珈伟怡云智EPC合作/` | 账本 402 条（7 份材料）· 流程表 **43 节点 / 50 边** | 「理解 + 节点原则」主账 |
| 乙 | `output/组件技术协议/` | 账本 140 条（3 份）· 计划 0 条流程 / 3 条排除 | 七类判据的**反向极值**：敢不敢报"这份材料里没有过程" |
| 丙 | `output/sop-怡云智-珈伟/` | 账本 1171 条（15 份，含 7 条视觉证据）· 5 份读不动 | 识别与清洗那一半（质量门 / 读不动 / 视觉通道） |
| 丁 | `output/self-boot/` | 12 阶段 + 7 张子表 | 形状与自洽那一半（H1—H9 能核的部分） |

## 二、节点原则：逐条对账

| 原则（出处） | 判定 | 证据 |
|---|---|---|
| **步骤与注解分得开**（§1） | **成立** | 参数与口径全都进了「节点描述」：`费用并入框架批次结算`（甲 `flowtable.md:33`）· `500 元/人/天`（:43）· `10 个工作日`（:71）——**一个都没变成节点**；反向记号是 21 处 `★`（"漏看会出错的注解"集中在描述列） |
| **同一步的异述要合，两处出处都写**（§1 / §6 第 2 条） | **成立 1 例** | `20 签订EPC总承包合同` 一行两处出处（框架协议 1.3 + 踏勘流程第 9 项，甲 `flowtable.md:55`）= 同一动作两份材料互相印证；43 行里**没有一对同义节点**（图上没有平行路） |
| **五条件 1 有动作 / 4 有位置**（§2） | **成立**（4 机器核 · 1 靠人看） | 43 行全过 H1—H9（= 第 4 条：50 条边、无死胡同、全连通）；4 条回边（`→回 10` :47 · `→回 28` :66 · `→回 26` :69 · `→回 01` :73） |
| **五条件 2 有主体**（§2） | **成立但弱**（→ 该标 `⚠`） | 执行主体 43/43 有值（珈伟 19 / 怡云智 15 / 双方 9）；但**执行者**列 **11 行不是单一角色**：9 行填"双方共责"（那是**主体**，不是角色）、2 行填两个角色（`技术/商务` :52 · `商务/财务` :69） |
| **五条件 3 有产出 / 粒度拆合**（§2 / §5） | **1 处反例，其余 42 行守得住** | `17 现场复勘 / EPC报价`（:52）：名称自带斜杠、产出两个（复勘结论 + 包干价）⇒ 按 §5「多一个产出就拆一个」该拆成两步。**只有 1 处**说明"同一产出合成一个"这条在真表上基本被守住了 |
| **五条件 5 有出处**（§2 / §6） | **成立** | 43 行「依据」全部非空，且都指到 `M##` + 条号/页（`M02#p019` · `M03#p028` · `M04#p002`…）——**零空行**（表头 H 系列只查"判断节点必填"，普通行也全填了） |
| **留痕 `⚠` / `⚠?`**（§2 / §7） | **反例（最贵的一条）** | 整张表 **`⚠` = 0、`⚠?` = 0**。而按上面两条与 §4，至少 **13 处**是推断（11 行执行者含糊 + 1 处该拆 + 1 处并行没落）。"有依据的推断不问用户"是对的，**但不标就等于没留痕**——`table_to_dsl --check` 的**软提示自己就写着**「请 AI 处理并打 `⚠` 留痕」（真实表上有 1 条：回路回到起点），而表里一条 `⚠` 都没有 |
| **判断 vs 并行**（§4） | **1 处"只活在散文里的关系"（三处同源）** | `22 项目备案与地方手续`（:57）描述写着"与采购并行"，而它的「下个节点」只有一条边（`→23`）。**把用户那张原始图读回来核过**（`xml_reader.py`）：`珈伟怡亚通EPC合作流程图.drawio` 也是 **43 节点 / 50 边**、`22 → 23 → 24` **串行**——**图与表逐字同构**，所以这不是"转换丢了"，而是**这条并行关系从头到尾只活在一句描述里**（正文材料 M04 第 11 项也没写"并行"）。原则的价值恰在这里：它点出一条**图与校验都看不见的关系**，逼人回答"到底并还是不并" |
| **子流程 `⊞`**（§4） | **空白（0 例）** | 43 节点 / `⊞` = 0：`踏勘设计`（02—11）与 `工程执行`（21—27）**各讲得出一张图**（材料里本来就有一份独立的踏勘施工流程），从没试过下钻 |
| **七类不该成节点**（§3） | **成立（含反向极值）** | 参数 / 计价口径 / 罚则 / 背景段全部没成节点；更强的一例在乙：3 份材料（140 条证据）**一条流程都没报**，理由是"`流程` / `验收` 两个词在账本里 0 命中"——**敢报 0 才是判据在起作用**（乙 `plan.md` 的排除清单） |
| **两遍自检**（§7） | **正向成立，反向无过程** | `09 现场踏勘·深度收资`（★补遗漏C，:43）与 `34a 服务台账确认`（★补遗漏B，:71）都是"回头数一遍"抓回来的节点；**但只留下结果、没留下"数过哪两遍"的过程记录** |
| **依据可裁决冲突**（§6 第 4 条） | **成立 1 例** | `13 报价评审通过？`（:47）的依据里写着"流程先后顺序以《踏勘施工流程》为准"——两套材料说法不一致，靠**一条写进表里的依据**裁掉，而不是在脑子里裁 |
| **视觉来源按推断档**（§6 第 3 条） | **空白（0 例）** | 丙有 7 条 `vlm` 证据（扫描件 4 页），**甲的 43 行一条都没引到视觉证据**（甲的表来自用户转述的 Markdown，不是扫描件）——这条原则**从没在真表上被行使过** |

## 三、识别与清洗那一半（`PIPELINE-SPEC` §1.6）

| 判据 | 判定 | 证据 |
|---|---|---|
| **尺子一：抽出来是碎片**（质量门） | **成立**（真数据） | 丙的 M15（资质 PDF）单字行 **62%**（225 行里 140 行是单字行）⇒ `noisy`：丢 10 条纯碎片、留 6 条**逐条挂 `degraded`**；同批干净材料 **0 误判**；样本 < 30 行不判 |
| **尺子二：抽出来太少**（文本层薄） | **已实现，且双向真样本都验到了**（原先是"空白"） | 真读数：**16 页 / 1563 字 ⇒ 98 字/页 ⇒ 判薄 ✓**（扫描册，正文在像素里）；**548 字/页（质保书）与 988 字/页（规格书）不判** —— 三者**每页元素数一样（1.0 条）**，可见"按元素数下结论"必错。**这条判据也纠正了我上一轮登记的错样本**：原先拿"11 页只抽出 11 条元素"当薄的证据，那是 §1.4 的设计（一页一个 element）。实现：`parse_pdf.thin_note` + `pdf_text_layer:` 阈值 + 夹具 **53**（两份 PDF 每页元素数相同、只差字数） |
| **读不动 / 缺转换器留痕** | **成立** | 丙 5 份 legacy 读不动，逐份 `status=unreadable` + 可执行原因（指名族与另存目标）；甲 / 乙 / 丙**没有一份静默丢弃** |
| **视觉通道（转图片）** | **成立 1 次** | 丙 M13 扫描件 4 页 → 7 条 `vlm` 证据（第 4 页空白，那条按纪律删掉）+ `certainty=inferred` 由账本机器拦；材料层 `unreadable → ok`，**覆盖被打印出来** |
| **清洗不越界（不改字 / 不合并 / 不猜）** | **成立但靠人** | 43 行「依据」的摘录与账本逐字对得上（靠 `query.py` 抽查）；**"既不可逆又不留痕"这条没有仪器**——H10.1 未实现，`依据` 里的 `id` 目前无人机器核 |
| **依据的可引用粒度**（`parse_text` 的"按行打块"，2026-09-18 blind 复现时量出） | **已修**（原为"有保留"） | 同一批 md 里两种命：散行条款（空行分隔）→ `M02` **34 条**元素、逐条可引；**一份 16 项的 markdown 表格**（连续行）→ 整篇只 **2 条**元素，第 2 条是 **2781 字的一块**，把 16 项连表格结构一起压成一个 `paragraph`，`依据` 写 `M04#p002` **指不到"第几项"**（盲读者被迫手写"（表内序号 1）"）。⇒ **已实现**：`parse_text` 认**表格行的边界**（只认行边界、不认列语义，`kind` 仍是 `paragraph`；分隔行不占元素），夹具 **52** 断言"第 3 项自己就是一条证据"（§1.6） |

## 四、结论：该补的五条 —— **三条已补（都是通用能力），两条仍靠人**

1. **legacy 的自包含降级读法**（**本轮最值钱的一条，已实现**）：真 OLE 里按 UTF-16LE 抽文本 run，
   过质量门才入账、`degraded` 留痕、转换器在场仍优先。**它已经藏住了一个真差异**——
   原件写"每 **5MW** 或每半年"，用户转述件写"每 **10MW**"，而答案键用的是转述件；
   **原件进不了账本，"原件 vs 转述件"就永远比不了**（D 系列判据全都没得比）。
   落地与实测：`parse_legacy.runs_of` + 三道过滤防假正文；**读不动 5 份 → 1 份**、**119 条 `py:oletext` 证据**、夹具 **51**（§1.6）。
2. **`⚠` 留痕归零**（**仍靠人，没有也不该有仪器**）——13 处推断、0 处留痕，而"不确定显示在用户唯一会看的图上"是这套 SKILL 的核心承诺。
   补法**不需要新仪器**：`05 逐行回看内容出处` 那一圈，凡是"执行者含糊 / 产出两个 / 并行还是串行没明说"的，一律标 `⚠`
   （`⚠` = 有依据的推断，不问用户；只有"推不出且不同选法后果不同"才升 `⚠?`）。
   ——**反向证据在盲读里**：甲臂 49 个节点标了 **40 处 `⚠` / 4 处 `⚠?`**，说明这不是"做不到"，是"没人要求自己做到"。
3. **并行与子流程两条从没被真正行使**（**仍无实例**）——`⊞` **0 例**；全表唯一一处"并行"**只写在描述里**（图与表都是串行，见 §二）。
   材料信号已经摆在甲的表里："与采购并行"（:57）、一份独立的踏勘施工流程（可作 `⊞` 子表）。
4. **尺子二（太薄）**（**已实现**）——判据写定在 §1.6（两个量一起看），落在 `parse_pdf.thin_note` + `dictionary.yaml` 的 `pdf_text_layer:`，
   夹具 **53**（同元素数、只差字数的两份 PDF）；真样本双向验过（98 字/页判薄 · 548 / 988 字/页不判）。
   **顺带纠正了当初登记的错样本**：按"元素数"看出来的"薄"是假象，"一页一个 element"本来就是设计。
5. **依据的粒度**（**已实现**）：打块时遇表格行就断块——它不动节点集合，只把"依据指得到第几项"从**人话**变回**可引用的单元**；夹具 **52**。
   同一条路上还补了**质量门读数"丢前 / 丢完"并排打**（同一份材料 62% vs 37%，不并排打会让人以为读数说谎），也在夹具 **52**。

**反过来，这份对账也说明原则本身不是空中楼阁**：节点原则 **13 条**里 **8 条成立**
（6 条干净 + "有主体但弱" + "两遍自检只跑了正向"）· **3 处不达标**（`⚠` 0 处 / 一处两产出没拆 / 一条并行关系只活在散文里）· **2 处从没行使**（`⊞` / 视觉来源），
清洗那一半的 **3 处**（尺子二 · 依据粒度 · "丢前 / 丢后"读数口径）**都已补上**——
而且**3 处不达标全部落在"机器核不了"的那半边**（留痕 · 粒度 · 并行），
与 `material-to-nodes.md` §8 的分工完全吻合：**H1—H9 管自洽（真表全绿），这份文档管合理（3 处不达标）**。
**答案键是"三处同构"的**：用户给的节点表（43 行）、用户给的图（43 节点 / 50 边）、我转成的契约表（43 / 50）**逐字同构**——所以"节点集合"这件事**有唯一答案**，盲读复现才有对照物。
**答案键是"三处同构"的**：用户给的节点表（43 行）、用户给的图（43 节点 / 50 边）、我转成的契约表（43 / 50）**逐字同构**——所以"节点集合"这件事**有唯一答案**，盲读复现才有对照物。

## 五、怎么复跑这份对账

```bash
# 形状与自洽那一半（11 道门，含夹具与自举）
python dev/tools/accept.py

# 循环那一半：漂移必须为 0，缺口每条都要有处置与理由
python scripts/drift.py check output/珈伟怡云智EPC合作/drift.md \
    --flowtable output/珈伟怡云智EPC合作/EPC合作流程/flowtable.md \
    --ledger output/珈伟怡云智EPC合作/evidence.json \
    --recon output/珈伟怡云智EPC合作/recon.md \
    --intake output/珈伟怡云智EPC合作/intake.md

# 抽查"清洗没改字"：拿账本里的 id 回查原文，逐字比
python scripts/query.py output/珈伟怡云智EPC合作/evidence.json --grep 结算 --batch 5

# 盲读复现（§六）：先把"允许的材料"拷进 .accept_tmp/blind/arm1/ 与 arm2/资料包/
#   —— 答案的三份派生件（.drawio / .html / 节点表 .md）一份都不拷（这一步是人工拷贝，本仓没有工具代劳）
# 再重建两臂账本（甲臂示例，乙臂换目录跑同样两条命令）
python scripts/probe.py .accept_tmp/blind/arm1 --json > .accept_tmp/blind/ledger-arm1/materials.json
python scripts/parse.py --materials .accept_tmp/blind/ledger-arm1/materials.json \
    -o .accept_tmp/blind/ledger-arm1/elements.json --notes .accept_tmp/blind/ledger-arm1/notes.json \
    --ledger .accept_tmp/blind/ledger-arm1/evidence.json --task blind-arm1
python scripts/table_to_dsl.py --check .accept_tmp/blind/arm1/flowtable.md
```

> **两臂的产物本身不进库**（`.accept_tmp/` 已 gitignore，且它们含用户业务内容——按 `coding-spec` G13 的纪律，
> 用户材料正文不许进仓库）。要复核 §七 的每个判定，按上面的沙箱重建，再让子代理照规范跑一遍即可。
>
> 计数（43 / 50 / `⚠` 0 / `⊞` 0 / `→回` 4 / 执行者 11）是 **2026-09-18 现取**的读数，
> 表一改就要重数——**别把这一页当事实源**，事实源永远是 `flowtable.md` 与账本本身。

## 六、盲读复现：换个人、只看材料、不看答案，能不能读出同一套节点

**问题**：上面 §二 的对账是**我**拿原则去量**我**做的表——自己量自己，量不出"规范换个人还灵不灵"。
所以做一次**盲读复现**：把答案（用户给的节点表与其渲染产物）从材料里拿掉，让**没看过答案的子代理**
照 `references/material-to-nodes.md` 自己读、自己落表，再与答案键对照。

**两臂（材料不同，其余完全相同）**：

| 臂 | 材料（沙箱内） | 账本（现场生成） | 想验什么 |
|---|---|---|---|
| **甲臂** | 4 份 **markdown 转述件**（合作框架协议 / 补充协议 / 工商业光伏踏勘施工流程 / SKILL 最佳设计指南**干扰项**） | `.accept_tmp/blind/ledger-arm1/evidence.json`（4 份材料 · 367 条证据） | **材料够不够**：主干材料齐了，节点集合能不能被独立复现 |
| **乙臂** | 15 份 **原件**（合同模板 / EMC 合同 / 转租协议 / 预收购框架 / 测算表 / 技术要求 / 踏勘报告 / 验收标准 / 供应商准入 / 短名单 / 资料清单 / 资质证照 / 两份 legacy 补充协议） | `.accept_tmp/blind/ledger-arm2/evidence.json`（15 份材料 · 1158 条证据 · **5 份读不动** · 1 份质量门 `noisy`） | **材料缺了会不会被编出来**：这套包里根本没有中段（设计 / 采购 / 施工 / 调试 / 并网），看它标 `⚠`／报缺，还是拿"看起来合理"的话术填满 |

**防泄漏（硬约束，写进子代理的提示词）**：只许读自己那一臂的沙箱 + 账本 + `references/` + `templates/` + `scripts/`；
**严禁读** `D:\工作空间\`（答案的转述件就在那儿）、`output/`（答案键与我的 43 节点表）、`dev/`（维护区）；
只许写一个文件（自己臂里的 `flowtable.md`）；结束时**自报读过的文件清单**作为审计凭据。
答案键里的三份派生件（`M05` 流程图 `.drawio`、`M06` `.html`、`M07` 节点表 `.md`）**一份都没进沙箱**。

**结果**：见下一节 §七。

## 七、盲读复现的结果

| 读数 | 答案键 | **甲臂**（3 份转述件 + 1 份干扰项） | **乙臂**（15 份原件） |
|---|---|---|---|
| 节点 / 边 | 43 / 50 | **49 / 58** | **73 / 83** |
| `table_to_dsl --check` | 退 0 | **退 0**（首跑即过） | **退 0**（首跑即过） |
| **召回**（对答案键 43 节点） | — | **严格 30 / 部分 7 / 缺失 6 ⇒ 折半 33.5（78%）** | **≈10 个重合点**（但它答的是另一条流程，见下） |
| 判断 / 结局 | 9 / 2 | 13 / 4 | 20 / 10 |
| 依据可指 | 43/43 行非空 | 34 个 element id，**逐个回核账本 0 虚构** | 137 个 element id，**逐个回核账本 0 虚构** |
| 留痕 | `⚠` **0** · `⚠?` **0** | `⚠` **40 处** · `⚠?` **4 处**（含 1 处**误标**，见下） | `⚠` **31 处** · `⚠?` **2 处** |
| `⊞` | 0 | 0 | 0 |
| 干扰项 | — | `M01`（技能方法论手册）**正确排除**：不取材、不进 `refs`、只在描述里点名 | — |

> **召回的两种算法差 1 个百分点，都以"部分 = 半个"收敛到 ≈78%**：我先算的是"命中 34（把部分当命中）+ 缺失 9 = 79%"，
> 独立复核员算的是"严格 30 + 部分 7 + 缺失 6 ⇒ 折半 33.5 = 78%"。两边的分歧只在 `19 / 27 / 33` 三个节点算"缺失"还是"部分"
> （**任务在了、判断没落**，与 `30 整改` 同型 ⇒ 按"部分"算更一致）；**结论不受影响**。

### 甲臂：严格命中 30 / 部分 7 / 缺失 6 —— 还原了主干，丢了"增值机制"

**部分 7 个**（覆盖一半）：`02 提供项目基础资料`（并入踏勘，容量 / 电价 / 消纳 / 备案条件没落）·
`12 商务评估与报价`（只落"报价"那一半，且时点后移）· `16a 终止`（**没有"费用留台账"、未中标也没汇进来**）·
`19 比选中标？`（判断并进任务、未中标无出口）· `30 整改`（只留回边、回边接施工）·
`33 触发结算条件？`（口径只在 `⚠?` 描述里）· `34a 服务台账确认`（10 日 / 异议 5 日对上了，**台账工作量的确认那一半没有**）。

**缺失 6 个**，按"材料到底支不支撑"分两类：

- **漏得对（材料不支撑）3 个**：`13 报价评审通过？`（答案键自己也是拿 M04 第 8 项"比选"顶上去的）·
  `32 电站移交`（三份材料全文没有"移交"这个动作）· `38 归档`（材料只有"一式二份"，**基本不支撑**）；
- **有支撑却漏 3 个**：`21 垫资启动`（M02 1.1 明写"乙方垫资建设"）·
  `25 / 25a 第三方成果委托`（M02 2.6 明写，且产出的是荷载报告 / 鉴定 / 加固方案 —— 甲臂把它读成**费用口径**，
  只在节点 27 的「输入」里引了 `M02#p020`）。

**它多出 13 个，分三类**（前两类是好消息，第三类不是）：
`拆分类 7`（催告 · 商务方案 / 客户确认 / EMC 编制 · 异议协商 · 续签……**按 §5 拆得对**）·
`规范驱动的结局 1`（评估未通过 → 终止，答案键缺这个失败结局）·
**(c) 类 5 个："材料明写、答案键整段没有"**：争议诉讼（M03 6.2）· 保密与品牌保护 / 违约判断 / 停止侵权赔偿（M03 3.0）·
不可抗力通知（M03 4.0）。
**⇒ 这把尺子必须双向看**：单向"产物覆盖答案多少"**对答案键有利**——答案键整段漏掉了框架协议 3.0 / 4.0 / 6.2
（那些条款按 §2 五条件也是"有动作有产出"的），甲臂把它们补上了。**"召回率"不是唯一的读数，两边的差集各是什么才是。**

**甲臂的一处真错**：有一条 `⚠` **自相矛盾**——描述写"材料未给起止时点"，同一行的「依据」却写着
"终止后 5 年内有效"（材料明写了 5 年）。**留痕用错了方向**：该标的是"我推断了时点"，它标成了"材料没说"。

**"台账"整张表出现 0 次**——这是甲臂最系统的一处丢失：答案键里"外勤费 / 初设费 / 第三方费用 → 计台账 →
跨项目逐笔登记 → 批次结算 → 服务台账确认"这条**增值机制链**（含两处 `★补遗漏`）整体消失。
材料侧只写了"结算范围包括 EPC 工程款、初步设计费、外勤费用、第三方费用"（M02 3.2），
**"要建台账"是从这条推出来的**（答案键推了，甲臂没推）——**它属于"可推但没推"，不是"材料没写"**。

**三处它比答案键更对**（本次实验最值钱的部分）：

1. **`17 现场复勘 / EPC报价` 被拆成两步**——正是 §二判为"该拆"的那一处。一个**没见过答案**的读者自己拆了：
   这条判据（§5"多一个产出就拆一个"）**不依赖答案也存在**；
2. **接入 / 备案的先后跟着材料走**：M04 的顺序是"第 10 项 接入批复 → 第 11 项 备案办理"，甲臂照材料排
   （接入在前）；而答案键排的是"`22` 备案 → `23` 接入"——**答案键与材料反了**。
   （答案键 = 用户给的节点表 = 用户给的图，三处同构，所以**这不是我转换时引入的**，是源头就这么排的。）
3. **`03 项目由谁负责开发` 的排斥 / 并行判定被写明了理由**：甲臂在描述里写"两条路后续动作不同…
   **是排斥而非并行**（§4）"——判据被**用出来**了，而不只是"结果碰巧对"。

**它多出 13 个**（拆分类 7 + 规范驱动的结局 1 + (c) 类 5，见上表），其中 **(c) 类那 5 个不是能力问题，是规范有一个洞**：
它们**"有动作、有产出"**（发函 / 赔偿 / 出证明都是产出），按 §3 当时的判据**条条合规**。
⇒ 已按这次实测把判据换成**"答不出位置的，一定是规则"**（见 `references/material-to-nodes.md` §3 末段）。

### 乙臂：**答了另一道题，而且答得诚实——但它的两条"缺料"自报是错的**

它读出来的是一条**完全不同的流程**：EMC 合同 + 预收购框架 + 验收标准 + 运维结算那一条
（新设项目公司 → 屋面踏勘 → 商务测算 → 供应商准入 → 短名单 → EMC 签约 → 转租 → 备案并网 →
完工验收 → 尽调 → 收购交割 → 运维结算 → 买断），与答案键的"怡云智 × 珈伟 EPC 合作链"
**只有约 10 个语义重合点**，而且重合的那些（踏勘 / 备案接入 / 施工 / 调试 / 并网验收 / 运维）**落在另一条链上**。

**这不是它错了，是它没被告知主体与目的**——而这暴露了**规范的第二处洞**：
`material-to-nodes.md` 通篇讲"哪句话值一个节点"，**没有一句话要求先定"这是谁的、给谁看的流程"**
（那条在 `PIPELINE-SPEC` §4.0，属维护区，出图时的 AI 读不到）。于是一个只拿到材料包的读者**只能挑材料里最强的那条**
（EMC / 收购 / 运维有合同模板与标准撑着、主流程完整且条条可引）。

**它的两条"缺料"自报经独立复核为不实**（**这是本轮最值钱的一次抓错**，两个人各自犯了同一种错）：

| 乙臂自报 | 实测 |
|---|---|
| "M11 / M14《补充协议》是 legacy `.doc`，**不可读** ⇒ 合作框架层整段缺" | **假**：两份都是真 OLE，但**按 UTF-16LE 直接解码就捞回成段正文**（196 / 218 段 4 字以上中文，1.1 垫资、外勤工单 5 日 / 催告 3 日、第三方、结算 10 / 5 / 30 日全在）。它把"**本工具链**读不动"当成了"**材料**读不动" |
| "M15 资质 PDF 单字行 62%，碎片化" | **夸大**：62% 是**丢弃纯碎片之前**的读数（225 行 / 140 单字行），丢完之后账本里是 **37%（134 行 / 49 单字行）**；**丢掉的 91 行全部是单字行 ⇒ 判据零误伤**（那是"仅用于信息核查"的竖排水印），资质名称本身是完整行 |

⇒ 两条都写进规范（见下表 3 / 4 行）。另外 `M01` EPC 合同模板同样可解码（5454 段中文），它的 `⚠?` 也站不住。

### 顺带抓到的两个硬事实（与"能不能提取节点"无关，但更值钱）

1. **原件与转述件有一个真差异**：两份 legacy 补充协议原件都写"每 **5MW** 或每半年（以先到者为准），
   按已完成并网部分对应的工程款向乙方支付"，而用户转述件 `M02#p022` 写"每 **10MW** 或每半年"。
   **答案键（含我的 43 节点表）用的是转述件的 10MW。** 这个差异之所以能藏住，正是因为原件被判了"读不动"——
   **`依据` 指着一份转述件，而原件从来没进过账本**。
2. **"读不动"是个工具链结论，不是材料性质**：三份 legacy（两份补充协议 + EPC 合同模板）都能用
   §1.6 新写的那条自包含降级路（UTF-16LE 文本 run）捞回正文。**"没有可靠读法"≠"不可抽"**。

### 复核（对抗性）：分数不是我一个人说了算

评分由**第三个子代理独立重算**，不给它我的数字，并明确要求"**默认立场是还原得没那么好**"。它给出的差异与结论：

| 分歧点 | 我的算法 | 复核员 | 采纳 |
|---|---|---|---|
| 召回口径 | 命中 34（部分算命中）/ 缺失 9 = 79% | **严格 30 / 部分 7 / 缺失 6 ⇒ 折半 78%** | **采纳复核员的**（`19 / 27 / 33` 与 `30 整改` 同型——"任务在、判断没落"——按"部分"算更一致） |
| 甲臂"多出" | 真多余 5 + 合理 2 | 真多余 1 + 拆分 7 + **(c) 类 5**（材料明写而答案键没有） | 采纳：**(c) 类单列**，因为它把"召回率"这个单向口径的偏向暴露了 |
| "台账"链 | 未单列 | **甲臂全表"台账" 0 次**（答案键那条增值机制整体消失） | 采纳（本节已写） |
| 乙臂两条"缺料"自报 | 未核 | **两条都不实**（`.doc` 可解码 · 62% 是丢前读数） | 采纳（已独立复现，见上） |
| 乙臂重合节点数 | 6~8 | ≈10（宽松 15） | 采纳 ≈10 |

**两条它自己也没说清的**（我复核后仍按我的判定）：`27 并网调试` 算"缺失"还是"部分"——材料通篇无"调试"二字，
但甲臂的 `23 并网接入` 与它同类，我按"缺失（漏得对）"计，不影响总数（已并入上面的折半口径）。

**它举的六条"覆盖得没那么好"的证据里，有五条成立**（30% 的答案节点没完整落地 · 台账链消失 · 失败分支只落一半 ·
`13 / 32 / 38` 只能来自未交给执行者的原表 ⇒ **盲读可复现的上限≈40/43，甲臂拿到上限的 75%** · 单向口径对答案键有利）。

### 这次盲读改掉了规范的哪五处（这才是"验证"的产出）

| 洞 | 谁撞上的 | 怎么补的 |
|---|---|---|
| **"有产出"判不出义务 / 罚则 / 保密 / 不可抗力**（条款条条合规，会长出假节点） | 甲臂多出 5 个节点 | `material-to-nodes.md` §3 末段：判据换成 **"答不出位置的，一定是规则"**（有没有前驱 / 后继） |
| **"并行"落地要先找到共同岔口**；找不到就串行 + `⚠`，不许造岔口 | 乙臂自报"**现实中运营结算与尽调 / 交割是并行的，本表表达不了**" | 同文件 §4：新增"并行要落地，得先找到那个岔口" |
| **依据的粒度**：一份 markdown 表格被压成一个 2781 字元素，`依据` 指不到"第几项" | 甲臂被迫写成 `M04#p002（表内序号 1）` | **已实现**：`parse_text` 表格行按行成块（夹具 **52**）——**两臂独立撞上同一件事**，说明它不是个例 |
| **legacy 的"读不动"是工具链结论**：真 OLE 里按 UTF-16LE 抽文本 run 就能捞回正文（并因此抓到"原件 5MW / 转述件 10MW"） | 乙臂把"读不动"当成了理由；复核员独立解码证伪 | **已实现**（`parse_legacy`：`runs_of` + 三道过滤 + `extractor=py:oletext`；真包实测**读不动 5 份 → 1 份**，夹具 **51**）；同节还补了"**质量门读数要写清丢之前还是丢之后**"（**已实现**，夹具 **52**） |
| **规范缺"先定主体与目的"这一环**（没这句，"该有哪些节点"就没有基准） | 乙臂：同一包材料里挑出**另一条也成立**的流程（73 节点、`--check` 全过） | **已补**：`material-to-nodes.md` §1 开头新增"**先定主体与目的，再切句**"（含这次的 73 节点实测）；`SKILL.md` 的"必须有**三**样"相应改写（原来只写"原始材料 + 起止范围"） |
