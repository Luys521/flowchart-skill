# coding-spec.md — 代码质量自检对照表

> 用途：回答"**这条行业原则在本仓库由什么拦**"。
> **本文不重复任何规则的内容**：每条规则的家在「本仓库的规则（唯一出处）」那一列指向的文件里，这里只做
> **索引 + 覆盖状态**（同一条纪律见 `SKILL.md`「一条规则只陈述一次」）。审代码时逐行问两件事：
> **规则还在不在？拦它的仪器还在不在？**
> 与 `dev/ARCHITECTURE.md`（模块怎么切）／`dev/REPO-MAP.md`（数据怎么流）分工：本文管"**写得对不对**"。
> 行业出处：PEP 20《Zen of Python》· Google Eng-Practices《代码评审标准》《看什么》·
> NASA/JPL《The Power of Ten》· MISRA C / AUTOSAR C++14 / SEI CERT C（机检规则族）· ISO/IEC 25010（可维护性）。

## 一、状态记号

| 记号 | 含义 |
|---|---|
| **机** | 有仪器拦：改了会红。右侧点名**仪器 + 段名**，可照名字去 `verify` 输出里找那一行 |
| **记** | 纸面有、仪器不拦，靠改的人记着。**这就是缺口**，全数列在第三节 |
| **不适用** | 面向 C／安全关键或特定范式的条目，在本工具链上没有对应物——写明为什么，不许拿"已遵守"糊过去 |

仪器名字的四种前缀：**面①～面④** = `dev/verify/run.py` 的四个面；**门①～门⑪** = `dev/tools/accept.py`
的十一道门；**仪** = `dev/tools/` 下单独跑的仪器；**手** = 人工/子代理通读。

## 二、对照表

### A 可读性与表达

| # | 行业原则（出处） | 本仓库的规则（唯一出处） | 拦它的仪器 | 状态 |
|---|---|---|---|---|
| 1 | 可读性是一等目标（PEP 20） | 文件头写"为什么这么切"+ 逐条 why-not 注释；**文档已有的理由不许抄进代码** | 面① `注释归属：文档已有的理由不许抄进代码` · 面① `Markdown 格式：代码块必须标语言` | 机（部分，见 G4） |
| 2 | 显式优于隐式；歧义不许猜（PEP 20） | 推断留痕两档 `⚠` / `⚠?`，且必须在图上可见（`references/flowtable-spec.md` §2） | 面② `AI 推断留痕：描述以 ⚠ 开头 → 图上醒目虚线标出` · `澄清阶段：⚠ 分两类、frontier 只问可问的` | 机 |
| 3 | 简单优于复杂；反过度设计（PEP 20 + Google） | 不设类型白名单：既定处理／并行方式／连接符／注解符／延时／里程碑各有既定落点（`references/flowtable-spec.md` §2） | 面② `节点类型：四种类型 ←→ 三个形状（形状单射，图里不写类型）` · 面① `参数表与 dictionary.yaml 逐值一致` | 机 |
| 4 | 扁平优于深嵌（PEP 20） | 只三层的分层与"模块间零横向依赖"（`dev/ARCHITECTURE.md` 第八节） | 门⑦ `dev/tools/layering.py` | 机 |
| 5 | 命名长到说清、短到好读（Google: Naming） | 术语统一、改名不许漂回旧词；表头字面 = 代码里的字段名（`scripts/semantics.py`） | 面① `术语一致性：改名后不许漂回旧词` · 面① `命令行参数与 argparse 一致` | 机 |
| 6 | 注释解释"为什么"（Google: Comments） | 每条 why-not 挂决策编号；决策编号有序、不跳号、不许指空 | 面① `决策日志：只记改过主意的事，且每条有编号` | 机（部分，见 G4） |
| 7 | 函数一屏看完（JPL 第 4 条） | 决策条目 ≤16 行；一个模块层模块对应一份产物 | 面① `决策日志：…每条决策 ≤16 行` · 门⑦ | 机（部分：函数长度本身无阈值，见 G4） |

### B 结构与依赖

| # | 行业原则（出处） | 本仓库的规则（唯一出处） | 拦它的仪器 | 状态 |
|---|---|---|---|---|
| 8 | 单一真值／不重复（DRY） | 命名只许走 `scripts/artifact.py`；数值只许写 `scripts/dictionary.yaml`；路径只许走 `dev/_paths.py`（`dev/REPO-MAP.md` §三） | 面① `参数表与 dictionary.yaml 逐值一致` · `文档引用的路径都存在` · 面③ `幂等：build 两次产物不变` | 机 |
| 9 | 只有一个显然的做法（PEP 20） | 渲染器注册表：换渲染器 = 改一行（`dev/ARCHITECTURE.md` 第九节 W5–W7） | 面② `渲染器注册表：换渲染器 = 改注册表一行…` · `注册表能装第三类产物…` · `降级：可选渲染器不在也照出 html…` | 机 |
| 10 | SOLID：职责与依赖边界 | 归层判据是"**谁依赖谁**"，不是名字像哪层（`dev/REPO-MAP.md` §四） | 门⑦ `dev/tools/layering.py` | 机 |
| 11 | YAGNI：只解现在确定要解的问题 | "产物出现**第二个消费者**时不许合并"；新增抽象前先问**现有结构**能不能承载（`dev/ARCHITECTURE.md` 第四节） | —（尚无仪器） | **记** → G5 |
| 12 | 一致性压倒个人口味（Google: Principles） | 把"口味"降级成**数值**，文档与字典逐值核对；**三份产物同一口径**（`references/visual-spec.md` §1：内容不因产物而异 · 圆角以 drawio 的 `arcSize` 为准，D-86 · 箭头以 `semantics.ARROW_LEN` 为准，D-89） | 面① `参数表与 dictionary.yaml 逐值一致` · `代码注释与文档串里的引用路径都存在` · 面② `svg 该节点虚线醒目描边（三份产物同口径）` · `html/svg 的圆角 = 短边 × arcSize%` · `html 与 svg 的箭头 marker 逐字节同款` · `三份产物同一枚箭头` | 机 |

### C 正确性与健壮性

| # | 行业原则（出处） | 本仓库的规则（唯一出处） | 拦它的仪器 | 状态 |
|---|---|---|---|---|
| 13 | 错误绝不静默（PEP 20） | 阻断必须点名节点/列/哪张祖先表；降级必须留痕并在产物审核里重申（`references/visual-spec.md` §4.1） | 面② `结构校验：坏输入必须被拦，且报错指向具体节点` · `产物审核不过：不落盘（还原上一版）` · 仪 `coverage.py` 图不完整即退 2（D-83） | 机 |
| 14 | 控制流简单、循环有界（JPL 第 1、2 条） | 内嵌视图有上限 `MAX_VIEWS` 且截断要显式报出（`scripts/render_html.py`，D-52） | 面② `单文件：子图内嵌为主 html 里的视图，下钻不跳页` | 机（部分：Python 无"禁递归/禁 goto"的对应物 → 该类条目不适用） |
| 15 | 断言密度（JPL 第 5 条） | 自检即断言：四面 + 十一门 + 产物侧九项判据 | 门①～门⑪ | 机 |
| 16 | 检查返回值与参数（JPL 第 7 条） | 畸形 DSL 统一兜成人话；回写前逐字节复核 | 面② `validate.py 的失败路径…` · `回写保真：语义列以流程表为准…` | 机 |
| 17 | 从第一天起零警告（JPL 第 10 条） | 未用 import／死函数归零；函数覆盖 100% | 门③ `coverage.py`（覆盖）· **门⑧** `dev/tools/hygiene.py`（未用 import／死函数，2026-09-17 接进验收，D-88） | 机 |
| 18 | 机检规则族（MISRA／CERT 的思想） | **只拦能机械判定的**：结构 H1–H10（H10 有账本才启用）、八项质量门禁、产物九项、合流只准落在端口上（`references/visual-spec.md` §5 第 5 项，D-89） | 门② `结构 H1–H8 + 表头 H9` · 门④ `validate.py` 逐张 · 面② `合流只准发生在端口上：异源并线 / 错峰交叉` | 机 |
| 19 | 可维护性是可度量的属性（ISO/IEC 25010） | 对外 API 增删要记账；分层边数与违规数要报数 | 门⑦ `dev/tools/layering.py` · 仪 `dev/tools/api_audit.py`（git 底本 = tag `api-base`；2026-09-17 补 git 后已能跑，但仍不在验收路径） | 记 → G8 |
| 20 | 事实与数据压倒意见（Google: Principles） | 判据落在**产物里的数字**上：从产物反查几何，不看自述 | 面② `产物几何自检：接歪的线必须从产物里被反查出来` | 机 |

### D 流程与评审

| # | 行业原则（出处） | 本仓库的规则（唯一出处） | 拦它的仪器 | 状态 |
|---|---|---|---|---|
| 21 | 代码健康持续变好，不求完美（Google: Standard） | 决策日志只记"改过主意的事"，历史文档保留旧词 | 面① `决策日志：只记改过主意的事…` | 机（部分：是否"值得记"靠判断 → 记） |
| 22 | 小步、单一目的（Google: Style） | 一条决策 ≤16 行；决策编号有序 | 面① `决策日志…` | 机 |
| 23 | 测试与代码同批（Google: Tests） | 改渲染器必须同时对齐基线与断言——基线是**字节不变基准** | 面③ `重建产物与基线逐字节相同`（workflow）· 面③ `自举树重建产物与基线逐字节相同`（整棵自举树）· `examples/ 是只读的事实源` | 机 |
| 24 | 逐行审阅 + 看上下文（Google: Every Line / Context） | **子代理通读**（本表第三节就是它的作业清单） | 手 | **记** → G4 |
| 25 | 文档随代码一起改（Google: Documentation） | 文档引用的路径必须存在；模板示例逐字等于格式基准；**文档里的命令行、规模数字、H 编号也必须是真的** | 面① `文档引用的路径都存在` · `格式基准：模板示例必须逐字等于 workflow` · `命令行参数与 argparse 一致`（含**子命令**与**位置参数**）· `文档里的规模数字与现算一致` · `门禁编号与检查项数`（含**列举不许漏号**） | 机（三条新断言见 `AUDIT-FIX-PLAN` 批 0；**六条反向用例逐条验过会红**） |
| 26 | 冲突不许烂在那里（Google: Resolving Conflicts） | 决策日志的"曾这么定 / 为什么改"就是共识档案 | 面① `决策日志…` | 机（部分） |

## 三、缺口：靠人记的顺序性规则（本表最该被读的一节）

| # | 规则（唯一出处） | 为什么机器现在拦不住 | 建议的机器化 |
|---|---|---|---|
| G1 | 改过 `scripts/*.py` 之后必须重跑 `dev/tools/fn_graph.py`（`dev/REPO-MAP.md` §六 注意条），再重跑 `dev/tools/selfboot_gen.py` 让自举表跟上 | **前半段已经有门**：门⑦ 的 `graph_is_stale` 比 `scripts/*.py` ↔ 图的指纹。**后半段仍靠人记**：图新了不等于表新。不过面③ 现在每次都会"按图重造一遍自举树并与基线比"——**表没跟上就会红**（代价是面③ 多跑一次生成器 + 一次 build） | 已在"没跟上会红"这一档收口；把重跑塞进 `accept.py` 只是省一次人工，不做 |
| G2 | 基线重钉**顺序**：先 workflow（含删表）再镜像 self-boot，反了会删掉整棵自举树的表（`examples/README.md`「刷新基线」） | 顺序错的那一刻仍然没有断言——但**后果**已经有人守了：面③ 的"自举树基线张数现取（见 G7） + 重建逐字节相同"（2026-09-17 补）会在下一次跑测试时当场红，不必等到有人去用那棵树 | 已在"表少了会红"这一档收口；再进一步只能把重钉本身做成一个脚本，不划算 |
| G3 | 布局规则变了要**先删子表 `*-flow.yaml` 再 build**（D-77 副作用） | 自举树那一半**已收口**（2026-09-17）：面③ 会重跑生成器 + build 再与基线整树逐字节比，复用旧 yaml 出来的产物对不上就当场红。**用户工程那一半仍然靠人记**——`build._prepare_views` 见 yaml 即跳过 | 自举树已有人守；用户工程可在 `validate` 加"yaml 的布局版本戳 ≠ 当前字典 → 提示重建"，成本不低，暂不做；**2026-09-17 实测踩到**：D-92 的「臂」上线后按老路径重钉（复用旧 yaml），基线的偏心仍是 0.43——布局规则一改就必须走「删 yaml 再重排」这条路 |
| G4 | 注释是否冗余（面① `注释归属` 段明说"**体积不是冗余**"，判据："删掉它，下一个 AI 会不会做出错误的改进？"） | 判据不可机械判定 | 无——保留人工/子代理通读；本表即它的作业清单 |
| G5 | 新增抽象前的"第二个消费者"判据（`dev/ARCHITECTURE.md` 第四节） | 是设计判断，不是可判定的形态 | 可半机械化：在 `hygiene.py` 里报"只被一个模块引用的公共层函数"清单供人裁 |
| G6 | "未用 import／死函数归零"（第 17 行） | **已收口**（2026-09-17，D-88）：原先 `hygiene.py` 既不在四面也不在七门，实测它退 1（6 处未用 import + 1 个死函数）而七门全绿 | 已接进 `accept.py` 当**门⑧**（后又加门⑨ 审美，D-92 ⇒ 现为十一道门）；射程仍只有 `scripts/`（扩到 `dev/` 见 N31） |
| G7 | 文档里的**绝对规模数字**（函数个数、表张数、依赖边条数、节点/边数） | 数字随代码增长而文档手写；2026-09-17 实测漂了五处（502 / 498 / 25 / 26 / 71 条边）；**2026-09-18 又漂一次**：加 `drift` / `query` 两个模块，`38→39→40 张表`、`37→39 个模块` 连带改 12 处文档 | **自举树张数这一类已彻底不写**：`invariants.py` 改成**按依赖图现取**（模块数 + 1），ARCHITECTURE / coding-spec / accept / selfboot_gen / tools README 里那 12 处数字全换成"整棵自举树 / 每模块一行 / 现跑现取"。（还剩函数个数、依赖边条数这类——根治仍要让 `accept.py` 输出一份可引用的数字块，文档只许引用它。） |
| G8 | 对外 API 增删要记账（第 19 行） | `api_audit.py` / `equiv.py` 要 git 底本；本仓 2026-09-17 补了 git（tag `api-base`）⇒ **已能跑**，且 `equiv` 已接进 `accept.py` 当**门⑩**（夹具 + 可观测行为逐字节） | **等价性侧已收口**：门⑩ 每轮验收都跑 `equiv suite`（22 秒：夹具有效性 + 两侧逐字节）；行为有意变更就重钉 `git tag -f api-base HEAD`。**API 面仍未进门**：`api_audit` 只在有 git 底本时手跑一次 |
| G9 | 压注释/重排文档串时的"行为面未被触碰"证明（`dev/verify/_docguard.py`） | 它**手动**用：先 `--snapshot` 再改再 `--check`；四面不跑它（跑不了——它比的是"改动前那一版"，不是当前代码的属性）。顺序颠倒或忘了存快照就等于没有证明（2026-09-17 已把"没有快照"从"印一行提示"改成**退 1**，但顺序仍靠人记） | 无——保留手动；把它写进 `dev/verify/README.md` 的"怎么用"一节即它的位置 |
| G10 | "回写自检不过 ⇒ 不许覆盖《流程表》"（D-84①：`write()` 的返回值 + `sync` 的退出码） | 夹具难造：要让 `writeback` 写出一份**必然过不了 H1–H8** 的回写结果，得先有一个自相矛盾的图（现有夹具都是好图）。e2e 只走通过那一支 | 想造就得手写一份"目标编号指向自身/缺结束"的 drawio 夹具；成本高于收益，暂记为缺口 |
| G11 | "**用例自带合成表，不借 `examples/`**"（`dev/verify/README.md`「怎么加一条用例」，D-42） | **已收口**（2026-09-17）：原先 6 处拿 `examples/workflow` 当夹具（回写保真 ×2 · `--apply` 覆盖门禁 · HTML 健壮性 · 新鲜度 · 复用几何提示，另加 `--diff` 与外部图读回两处），并把样例字面内容写进断言 | 已全部换成 `_wb_project` 自造夹具（表形写死在该函数里，按**节点编号**取列而不是按名字找行）；`_src()` 随之删除，`examples/` 现在只剩"`validate --artifact` 拿基线产物当现成产物复核"这一处**产物**用途 |
| G12 | **绕行**那一半的审美目标（`references/visual-spec.md` §0.1：绕行均值 <=35%）| **偏心与通道半径已达标并接进门⑨**（D-92：可配平表偏心 0.00、通道半径 <=0.17）。绕行还差一口气：自举那批单列函数流停在 43%——几十条长跳的竖直跨度互相重叠，谁也借不了谁的竖道，束宽是「条数 x 束距」的硬账；两次「把档分到两侧」的尝试都更差。门⑨ 先按**当前收口值**（<=45% / 最坏 <=60%）钉住不许变差 | 要降得让**跨度**变短：让生成器把长跳拆到两列（布局/生成的活）；做到 <=35% 之后把门⑨ 的阈值收到目标值 |
| G13 | **材料流水线的判据没有仪器** | **已收口**（2026-09-18）：`dev/tools/pipeline-fixtures/suite.py` 造一棵 **20 份材料的合成树**（真 docx/xlsx/pptx/PNG · 改名件 `.et` / `.png` · 半容器 · 部件是垃圾 · 流式无 `<dimension>` · GBK · 空文件 · **三族各一份**假 OLE（`.doc` / `.wps` / `.dps` / `.xls`）· 有/无 `/Font` 的 PDF）并把判据做成断言接进**门⑪**（每条正反各一例）。**它当场抓出三件真缺陷**：① 容器认得出、正文坏掉的材料"零证据却 `status=ok`"→ 整链退 1（适配器现在补注读不动）；② 超护栏的事实只打 stderr、**不进表**（行里留白 = 没交代）；③ **T3（视觉路）被算成"探测说谎"** → 那张 4×4 的 PNG 让整链退 1，而本链本来就不该认领 T3（它的 owner 是 `render_pages`） | 剩：**"超体积"只测了护栏分支**（用 `--dict` 把阈值调小），没有真造几百 MB 的材料；**真实 WPS 文件从没进过库**（`.wps` / `.dps` / `.xls` 现在都有合成样本，但那是"假 OLE 魔数 + 真流名"，不是 WPS 自己写出来的字节） |
| G14 | **循环的发动机进了验收路径**（`PIPELINE-SPEC` §5）：漂移判据 D1—D5、缺口清单、`check` 的账目对账、§5.3 的点名取子集、pptx 全链（认得出来 / 有摘要 / 撬得开 / 纯图片页记账 / 幻灯片外文字报出来）、legacy 按族提示、护栏按"要读的部件"算，**每轮验收都跑**（门⑪ `pipeline-fixtures/suite.py`；**路径条数不在这里手抄**——门⑪ 逐条打印 PASS/FAIL） | **已收口**（2026-09-18）：合成夹具抓到过四件真问题（`check` 只查一半、伴生表静默跳过坏行、`rows=` 差点筛成 0、`('docx','xlsx')` 重复清单让 pptx 摘要为空）。**用户给的三份真样本又抓到三件合成夹具没想到的**：① 真稿 19 张里 3 张纯图片被抽成 `text: ""` 的空元素；② 真 `.wps` 是 Word 97-2003 族而提示不指名族；③ **42 MB 的真稿被判"超护栏"⇒ 摘要直接没有**（媒体占了 42 MB，而摘要只读几百 KB 的 slides —— 护栏量错了对象）。**第三轮（"规范一致性"批，2026-09-18）再抓五件**：④ 夹具自己那份 `intake.md` 有 16 条错——**它正好把阻断"D4 语法互斥"藏住了**（两边都不合规 ⇒ 测不出判据是死的），现在夹具的卡片改由 `intake.py build` 出骨架 + 填 AI 四列；⑤ `recon check` 的**正例从没被断言**（`rc_ok` 只打印，于是"永远退 1"也全绿）；⑥ `query` 的 `pages=` / `sheet=` 只有反例，**把这两支删掉也全绿**（夹具元素里 `page` / `sheet` 全是 `None`）；⑦ `probe` 对判不出的材料只给**判据**不给**出路**（GBK 的 `.txt` 拿到"魔数不认识"无从下手，而 §1.3 要求 `unreadable` 的原因可执行）；⑧ `.wps` / `.dps` 在合成树里根本没有样本——而它们**只能靠流名认**（`OLE_EXT` 兜底表里没有这两个后缀），是最干净的一份"按内容判"样本。五件全有断言了（㉔b / ㊳ / ㊴ / ㊵ / ㊷ + ㉝ 正例） | 剩：**软档判据的纪律**仍靠人记（先给精度读数才准入 §5.2）；**pptx 页序**按文件名数字排（三份真稿恰好都一致，未造过乱序稿 ⇒ **仍未验**）；**幻灯片之外的文字**（图表 / SmartArt / 备注页）现在**报出来但不读**（三份真稿里都没有这类部件 ⇒ 读法**仍未验**）；**读者侧没有字节护栏**——`parse_ooxml` 仍把整份读进内存（那份 42 MB 的稿子解析时峰值 42 MB；侦查侧已改成按需解压，读者侧还是老实读整份）。要收口就得让 pptx 读者也走路径（`pptx_text.slides_in_file` 已经有了），但"峰值内存"这件事**没有仪器能断言**，所以先记账、不假装守住；**`query` 的边界仍未收**（`--batch 0` / `--chars 0` 静默取默认、多个 `rows=` 只用第一个、`sheet=` 首尾带空格的表名点不到、点不存在的 `--material` 静默 0 命中）；**`parse.py` 的"id 重复 / 补注冲突 / `status=ok` 却零证据 ⇒ 退 1"三条**（§1.4 明令）仍无人跑；"子代理摘要的论断能指回 element id"与 G4 同类，保留人工/子代理通读。**第四轮（真材料集，2026-09-18）又抓三件**：⑨ `.md` / `.csv` 材料的"结构缩样"把正文里的 `|`（markdown 表格）原样带回侦查表 ⇒ 那一行列数被撑破、`recon.py check` 报"仪器故障"，而真因在材料正文里（`recon.render` 现在转义；㉓ 也改成**要求产物能被自己的 `check` 读回来**）；⑩ 账本那行"降级 N 条"只数了**本次 quote 截断**，而文件里带 `degraded` 的元素有 22 条 ⇒ **读数说谎**（已拆成"带降级留痕 N 条 · 本次 quote 截断 M 条"）；⑪ `plan.py` 的表头正则要求 ` · ` 两侧都有空格，于是 `）· 目的` 这种写法被判成"没澄清"（已容忍空格）。**第五轮（真材料跑 §5 的下半段，2026-09-18）又抓到一件，并第一次把循环闭合**：⑫ **D1 把"浏览摘录截断"当成等级拔高**——`ledger.degrade` 给每条摘录超限的元素挂 `quote 截断到 N 字`，而 D1 不分类地一律判，于是**一份每个节点都规矩引条款的真表被报成 23 条漂移**（精度问题，本仓对这类判据的纪律是"先给精度读数、太差就砍"）；已按降级**来源**分类：只认"证据被削弱"的那几类（质量门 / 收窄 / 转换），`quote 截断` 不算（那是 §2.3 的浏览摘录上限）。改完在同一条真链上闭合：读数 **0 漂移 · 4 缺口** → 缺口处置经 `cells.py` 回写 → `drift.py check` **✓ 收敛**（0 条漂移全处置、4 条缺口全有结论、没有读数漏在账外） |
| G15 | **规范与仪器的一致性**（`PIPELINE-SPEC` §1.5 / §3 / §5.3 / §5.4 的列模板 ↔ `recon.CARD_COLUMNS` / `intake.COLUMNS` / `drift.GAP_COLUMNS` / `drift.DRIFT_COLUMNS`；§8 的"现状"列） | **列模板那一半已收口**（2026-09-18）：门⑪ 的 **㊶** 在 `PIPELINE-SPEC.md` 里**逐字**搜这四个列规范，找不到就红。本文实测漂过两处——§5.3 的缺口表只有 6 列（`GAP_COLUMNS` 是 7 列）、§1.5 的示例表只有 6 列（`CARD_COLUMNS` 是 12 列），**照规范写出来的产物会被 `check` 退 2**；同轮把 §8 的"现状"列逐行改成实况（原先 4 行是假的：侦查 / 探测记账 / 材料层记账都写着"未进验收路径"，而夹具早就在跑它们） | 剩：**§8 的"现状"列仍是人工维护**——㊶ 核得了列模板，核不了"这一行说它进验收路径了、是不是真进"。要机器化就得让每条路径**自报编号**、由门⑪ 收成一张机读清单再与本文件比（等于给夹具加输出格式），本轮不做，登记在此 |
| G16 | **L2 计划层**（`PIPELINE-SPEC` §4）：`plan.md` 的三张表（流程清单 / 澄清申请 / 排除清单）· 种子 / **强合并**（§4.2 步骤 2 = 清点卡片上的事实）/ `F##` 排序 / 排除清单 / 澄清种子（`plan.py build`）· §4.4 的**三条可机器核**（材料集都在清点里 · 流程数 = `<流程名>/` 目录数 · 排除清单双向一致 + `待澄清` 有账 + `共享(M##)` 真被另一条流程共享 + 同组内不许 `接力`） | **已收口**（2026-09-18）：门⑪ 的 ㊸（机器列）/ ㊹（正例）/ ㊺（**反例十一连**：造材料 · 不确定不标 · 排除漏项 · 共享空话 · 接力撞并行组 · 组名歪 · 接力悬空 · 挂在悬空 · 表头坏）/ ㊻（§4.4 ② 少目录要报、补齐要过）四条路径；§4.1 三张表的表头由 ㊶ 逐字核。顺带把 §5.4 收敛口径里的"**澄清申请**"从"无仪器"变成有仪器（**换了写法**：不是"申请表空"，而是"计划里 `待澄清` 的流程 = 0"，`plan.py check` 打印读数） | 剩：**弱合并（§4.2 步骤 3）与甲 / 乙 / 丙（§4.3）是 AI 的判断**——脚本只核它们的**形态**与**后果**（`共享` 得真共享、同组不许有依赖、`挂在` 的父流程要在本表里），核不了"判得对不对"；`挂在` 里的**节点编号**要落表后才存在 ⇒ 那一半归 H9 / H10；`plan.py` **刻意不读 `evidence.json`**（那会变成第三份真值），要核 element id 存不存在走 H10.1；**「为什么并 / 为什么拆」没有落点**——§4.1 的流程清单只有七列（没有依据或描述列），而 §4.2 步骤 3 要求弱合并标 `⚠`，真材料集上只能把理由写在表头的散文里（要么给流程清单加一列，要么承认它落在表头） |

| G17 | **AI 格只写 JSON，不自排版**（`PIPELINE-SPEC` §0）：`build --todo` 出待填清单 → `cells.py fill` 按列名回写；`plan.md` 另有 `--scope`（表头三格）与 `--split`（行集合） | **已收口**（2026-09-18）：公共层 `scripts/cells.py`（`todo_from_doc` / `fill`，两种写法：整格替换 / 带前缀）+ `recon` / `intake` / `plan` / `drift` 四个模块的 `--todo`；**门⑪ 里 recon / intake / drift 三处 AI 列的填充全部改走这条路**（键错 / 列名错 / 空值三类反例都要求**不写盘**）；`plan` 另有 ㊽：`--scope` + `--split`（AI 的拆解落成 JSON，`build` 当场核"每份「含流程 = 是」的材料都有下落"，漏一份退 2 不写盘）。起因是同一类错连着踩三次：`.md` 缩样里的 `|` 撑破列数（12→18）· 填格脚本按**列序号**硬编码 · 值里带竖线撕表——**根因都是让 AI 去当排版工** | 剩：**`--split` 的"为什么并 / 为什么拆"仍没落点**（三段 JSON 只装结论，不装理由；§4.2 步骤 3 要求弱合并标 `⚠`，现在只能写在表头的散文里）；`范围外` 的理由是自由文本，机器只核它以「范围外」开头 |


| G18 | **泛化：换一份材料集还能不能只读仓库跑完**（判据是"下一个会话，只读仓库 + 一个材料根，能不能产出同样的东西"） | **已收口到"能跑完"这一档**（2026-09-18）：第二份材料集（`D:\组件技术协议`：技术协议 docx + 质保书 pdf + 规格书 pdf，与第一份**不同领域**）只走正式接口跑完 L1/L2——`probe → parse → ledger → recon(--todo/cells) → intake(--todo/cells) → plan(--scope/--split)` 全链 **rc 0**、两条 `check` 过，**没有任何一次性脚本**（第一轮那三个 `_fill_*.py` 已删）。**并顺手验了"0 流程"这条边界**：这三份是技术条款 / 质保条款 / datasheet，`--grep 流程`、`--grep 验收`、`--grep 申请` 全 0 命中 ⇒ 如实出 **0 条流程 + 3 条排除**，`plan.py check --root` 照样过（**流水线该说"没有"，不硬凑一张图**） | 剩：① **"文本层薄"已实现**（`PIPELINE-SPEC` §1.6 尺子二 / `parse_pdf.thin_note` / `dictionary.yaml` 的 `pdf_text_layer:` / 夹具 **53**）——**并且顺手纠正了当初的登记**：我原先拿"11 页只抽出 11 条元素"当薄的证据，那其实是"一页一个 element"的设计；真读数是 **548 字/页（正常）**，真正的薄样本是那份 **16 页 / 1563 字 ⇒ 98 字/页** 的扫描册。**这也是为什么判据要"两个量一起看"**（同是 1.0 条/页，一个薄一个正常）② 仍是**单一样本对单一样本**的对照，没有第三个领域；③ `import_table.py` **已扩到五种真实表形态**（2026-09-18：英文表头 / 两行表头 / 一行一分支 / 中文点号编号+裸编号引用 / 分支藏备注+字母编号）——**前三类能直接过契约校验，后两类判定为"不该硬转"**（编号是身份、分支要从备注里搬出来）；夹具 **㊾ / ㊿b / 57** 钉住两边 |

| G20 | **「有效节点」的原则**（唯一出处 `references/material-to-nodes.md`：读懂再切句 · 步骤/注解/异述的分水岭是**产出** · 一个节点成立的**五个条件** · 不该成节点的七类 · 判断与并行在**材料侧的信号** · 粒度拆合 · 依据怎么指 · **落完表数两遍**）与**清洗的边界**（`PIPELINE-SPEC` §1.6：不改字 · 不合并去重 · 不语义分类 · 不"修好"读不动的；**每一步要么可逆、要么留痕**） | **写定即本轮成果，且大部分不该有仪器**——"该不该有这个节点 / 这一步是不是注解 / 该拆还是该合"是**理解**，机器拦不了（H1—H9 只核自洽：把并行写成判断、把注解写成节点、把两步并成一步，**它全绿**）。能机器化的只有两处：(a) 「依据」里的 element id 存不存在（H10.1，**未实现**）；(b) `⚠` 留痕的形式（已有软提示"两个都空 = 你没想过"）。**本轮把原则拿到真材料上逐条对过账**（`dev/NODE-AUDIT.md`，甲 43 节点 / 50 边 + 乙 0 流程 + 丙 1171 条证据）：**节点原则 13 条里 8 条成立**（含乙"敢报 0"这条反向极值）、**3 处反例**（`⚠` 0 处 / 一处"材料说并行、表里串行" / 一处两产出没拆）、**2 处空白**（`⊞` 0 例 · 视觉来源按推断档 0 例）；**清洗那一半另有 1 处空白**（尺子二 / 文本层薄）。**再加一次盲读复现**（`NODE-AUDIT` §六—§七，2026-09-18）：把答案藏起来、只给材料与规范，交给两臂子代理独立落表——**甲臂**（3 份转述件 + 1 份干扰项）**49 节点 / 58 边 · `--check` 退 0 · `⚠` 40 处 / `⚠?` 4 处 · 召回 **严格 30 / 部分 7 / 缺失 6 ⇒ 折半 33.5（78%）**（评分由**第三个子代理对抗性复核**，它的严格口径被采纳；两处它自己漏判的已注明）·
多出 13 个（拆分 7 · 规范驱动的结局 1 · **(c) 类 5 = "材料明写而答案键整段没有"**：框架协议的保密 / 违约 / 不可抗力 / 争议条款——
**单向"覆盖答案"的口径对答案键有利**）· **甲臂抓到的真事实：原件写"每 5MW 或每半年"，用户转述件写"每 10MW"，答案键用的是转述件**（原件被判"读不动"，所以差异永远不可见）· 乙臂 73 / 83、`--check` 退 0、`⚠` 31 处，但**答的是另一条流程**（EMC / 收购 / 运维），因为没人告诉它主体与目的；它的两条"缺料"自报经复核为**不实**（`.doc` 可解码 · "62% 单字行"是丢前读数、丢后 37% 且丢掉的 91 行全是单字行 ⇒ 判据零误伤）。**这次实测把规范改了两处**：§3 判据从"有没有产出"换成**"答不出位置的，一定是规则"**（甲臂那 5 个假节点的直接原因）· §4 新增**"并行要落地得先找到共同岔口"**（乙臂自报"本表表达不了并行"的直接原因）；另两处落在 `PIPELINE-SPEC` §1.6 / §8（**legacy 的自包含降级读法** + **质量门读数要写清丢之前还是丢之后**） | 剩：**反例与空白全部落在"机器核不了"的那半边**——补它们靠的是下一次落表时**真的标 `⚠`**、真的试一次 `⊞`，不是加判据；**"两遍自检有没有跑过"没有过程留痕**（只有结果，如甲表里两处 `★补遗漏`）——要留痕就得给产物加一格，本轮不做；**盲读复现本身也没有仪器**（要造沙箱、起子代理、人工比对），只能当"隔一段时间做一次"的人工复核，不进 `accept.py`；**legacy 降级读法**已实现（`parse_legacy`：`runs_of` + 三道过滤 + `py:oletext`，夹具 **51**；真包实测读不动 5 份 → 1 份）· **markdown 表格按行成块**与**质量门"丢前 / 丢完"双读数**也已实现（夹具 **52**）· **尺子二（文本层薄）**已实现（夹具 **53**）· **H10.1 引用完整**已实现（`flowtable_check.check_evidence` + `intake.check_cards`，夹具 **54**，98 张表误伤回归 0）· **依据分布**（`PIPELINE-SPEC` §5.6，读数不是判据：节点级占比 + "只落在一份材料上的连续段"）已实现（夹具 **55**，㊶ 纳入 `READOUT_COLUMNS`）——§6 原稿的 **H10.2 / H10.3 经查与已发口径打架，按"改规范、不叠判据"处理**（H10.2 与 `flowtable-spec` §2「依据 / ⚠ 二选一」相反；H10.3 要的列在 `plan.md` 的澄清申请表里根本不存在，而"⚠? 有没有账"已由 `clarify.py` 的 frontier 覆盖）· **端到端跑真根**（同日在 15 份原件包上从 `init` 走到三份产物）抓到 `plan.py check --root` 的**三处口径打架**并已修（辅助目录 `shots/` 不算流程目录 · 表头三格按字段名取值并逐格点名 · `待澄清` 的流程不要求目录；夹具 **56** + **㊻** 校准）——**新风险**：能力升级（legacy 降级读法）让旧澄清申请过期，而账本与基于它的判断绑在同一版能力上，**没有仪器提醒"回 01 重走"** |

| G19 | **「流程节点的采集与判断归 AI 自由发挥」这条边界**（`PIPELINE-SPEC` §0.1：脚本只做认料 / 记账 / 落笔 / 校核；三条不许 = 不替 AI 定节点集合 · 不因判据跑不出来就替 AI 下结论 · 不把某条路径变成必经）。**同一条边界在产物侧的落点**：§4.1 ② 的澄清申请**不许装 AI 的专业判断**（"要不要拆子流程 / 该并还是拆 / 算不算流程"都该写进表头注记或节点描述并标 `⚠`，不是问句） | **写进规范了，但没有仪器、也不该有**——它不是"可判定的形态"（与 G4 同类）。能做的是**审代码有没有越界**：本轮把 `scripts/` 过了一遍——`parse_*` 只产 element · `recon`/`intake`/`plan` 只填**机器列** · `import_table`/`plan --split`/`cells` 都是**可选的手** · `table_to_dsl`/`build` 只消费 AI 的表、**没有任何脚本"从材料生成节点集合"**；两个看着像反例的都不是（`selfboot_gen.py` 是仓库自己的开发工具；`sync`/`writeback` 是把**图上改过的节点**忠实回写）。**同一个边界在产物侧真的被我自己违反过一次**：真计划里写过一条「验收节点要不要引《验收标准》细化成子流程？」——那是 AI 该自己判的事（§3 纪律 4），已从澄清申请里删掉、改成表头注记里的判断 + `⚠` | 剩：**「未来新增工位有没有越界」仍靠人审**（每次加脚本时对着 §0.1 的三条不许过一遍）；**"这条问句是不是 AI 该自己判的"没有仪器**——它靠人读（`check` 只能核"待澄清都有账"，核不出"这条根本不该问"）|

| G21 | **材料流水线的"操作者入口"**（方案计划书 R2 / R4 / R5 / R6 全落在这条链上）：L0—L2 的八个命令（`probe` `parse` `ledger` `intake` `plan` `recon` `drift` `query`）在 `dev/` 有契约、在 `scripts/` 有实现、在门⑪ 有夹具，**但 `SKILL.md` 一个字都没提，`references/` 也没有 L0—L2 的读本** ⇒ 被技能加载的 AI 看不到这条链，门面仍是流水线之前那张 12 节点表（`01 收到材料 → 03 落表`）；`<cwd>/flowcharts/` 那条兜底路径全仓 0 处。**这不是执行偏差，是计划的漏项**（D-101）——五节只排到"⑤ L4 最后做"，没有一步是"接进入口 + 用一份新材料从头走一遍" | **无仪器，也不该有**（"门面有没有把这一步说清"只能人读，与 G4 / G19 同类）。**能机器化的只有一步**：`dev/verify/contract.py` 可断言"`SKILL.md` 提到 L0—L2 的入口命令"——**现在那条断言管的是反面**（"调度层不掺维护内容"，即别把仓库维护动作写进 SKILL.md），所以它拦不住"整条链没写进去" | 剩：**入口长什么样还没定**（把 L0—L2 加成节点表的新行 + 一节命令，还是单开一份 `references/` 下的材料层读本）——定之前**不许在 SKILL.md 里散着加命令**。**2026-09-19：自举跑通了**（`NODE-AUDIT` §十）——**24 节点的全链表已由它自己产出并收敛**（H1—H10 一次过 · H10 真核了 43 处引用 / 36 个唯一 id · 漂移 0 · 缺口 7 条全有结论），所以"入口长什么样"不再是设计题、而是**上架题（**2026-09-19 已执行，留作对照**）：① 节点编号要不要为 `01a` 重排（`examples/README` 拿它当"插入编号"的示范，不许弄没）② `examples/workflow/` 那 7 张子表与 9 条示范要跟着改 ③ `SKILL.md` 的八节要变十一节 ④ `templates/flowtable-template.md` 的示例行与之**逐字一致** ⑤ `dev/baseline/workflow` 重钉 ⑥ **依据那一列**：自举表引的是账本 id（账本在 `output/` 里、不进仓），上架时必须改回"文档小节"式或把账本进仓（见 `NODE-AUDIT` §十）。**H3 的考场在这里**。**2026-09-19 上架完成**（`NODE-AUDIT` §十一）：`examples/workflow/` **13 → 25 节点**（整条脊柱重排 `01 → 01a → 02…24`，7 张子表 52 行跟着父节点改）· `SKILL.md` **八节 → 十一节**（新增 清点 / 计划 / 循环）· `templates/` 两份逐字同步 · `dev/baseline/workflow/` 20 文件重钉 · **十一道门全过**。**H3 成立且是六处不是三处**（模板被"逐字一致"锁在主表上 · 子表编号跟父走这条**只是约定、没有仪器** · 产品文档里"拿这张表举例"9 处 · `examples/README` 的示范数字全是数出来的 · `01a` 不许弄没 · 一处我自己编的 `render_pages.py -o` 被面① 抓红）。**仍未做**：① "`SKILL.md` 提到 L0—L2 的入口命令"那条断言**还没加**（`contract.py` 现在只管反面：别把仓库维护动作写进 SKILL.md）；② 自举表那版「依据引账本 id」的写法**没有上架**（账本不进仓 ⇒ 上架版改回文档小节引用，H10 整层跳过），"引用**对不对得上**那半句"仍无人核。**已做**：新节点的 `⊞` 子表（`04 侦查` 8 节点 · `20 对账` 9 节点；子表 9 张 / 69 行，见 `NODE-AUDIT` §十二） |

| G22 | **五路并行审计查出的第二 / 三档 11 条**（`dev/NODE-AUDIT.md` §十三 有清单与出处）：主表缺 `rc=2` 出口 · 入口 B 无节点 · 没有"没流程就停"的终止出口 · AI↔JSON 往返不在表里 · `checklist.md` 无产出者 · 材料侧无陈化检查 · 子表编号前缀无仪器 · `init.py` 发的是旗舰示例 · 答案 JSON 无骨架绑定 · 三产物必需性口径不一 · 秒数是编的 | **清单在 `NODE-AUDIT` §十三**（每条都带出处与我的复核结论）；其中**元层面一条**最值钱：把"文档里的命令 flag 必须存在 / 绝对规模数字必须现取 / H 范围写法统一"三类做成断言 | **批 1 已修五条**（`AUDIT-FIX-PLAN` 批 1，D-104）：`rc=2` 出口 · 入口 B 节点 · "没流程就停" · `checklist.md` 产出者 · AI↔JSON 落笔（做成规则不做节点）。**批 2 两条已修**：子表编号前缀进了 H9（把 `20a` 改成 `99a` 会红）· 材料侧陈化（`probe --verify` 硬拦 + `parse` 默认告警 + `--strict-stale`）。**新增缺口**：材料层陈化的**门⑪ 夹具没补**——夹具把材料树与工作目录当同一个目录用，硬拦会当场红 6 条，要提升为"默认硬拦"得先重构夹具布局。**2026-09-19 另修两条功能缺口**（D-106）：① `query.py` 的 `--batch 0` / `--chars 0` 被 `or` 静默换成默认值（紧跟其后的守卫永远轮不到）⇒ 改成"没给才取默认"、门⑪ 夹具 **58**；② **能力升级后旧账过期无人提醒**（本表 G20 的"剩"与 D-99 都点到它）⇒ 产物盖 `capability` 章、消费端喊、`capability.py check` 拦，夹具 **59—61**，射程见 G23。**剩下**· 批 3 四条要拍板（`init.py` 发什么 · 答案 JSON 绑定 · 三产物必需性 · 秒数）。**第一档 12 条已修**（`ae50b21` / `4261bd2`） |
| G23 | **能力指纹的射程**（`PIPELINE-SPEC` §1.2 / D-106）：`code` = `scripts/*.py` + `dictionary.yaml` · `rules` = `references/*.md` + `templates/*.md`，只盖**文件内容** | `scripts/capability.py`（唯一出处）+ 账本 / 计划的章 + 门⑪ 四条夹具（58—61） | **已实现并进验收**。**没盖住的三样**（认下）：① **环境**——Python 版本、装没装 `soffice` / OCR；② **调用参数**——`--encoding` / `--max-*` 这类当次收窄（它们进不了产物，也就不该进指纹）；③ **`dev/` 变更**（工具与门禁改了不影响产物，刻意不盖）。所以"没漂"只等于"与当下这套**文件**同版"，**不等于**"结果一定对"——反例见 `NODE-AUDIT` §十一（"依据引账本 id"那一版上架时改回文档小节引用，指纹对此一无所知） |

## 四、怎么用这张表

- **审代码**：从上往下逐行问"规则在不在、仪器在不在"。发现"规则在、仪器没了"＝**回归**，比新缺陷更该先修。
- **加规则**：先给它找一个仪器；找不到就登记到第三节——**不许假装它被守住了**。
- **减规则**：先删仪器再删规则，否则留下的是一条没人守的纸面条文。
- **判新原则**：行业原则进本表要么落成"机"（有仪器），要么落进第三节（承认靠人记）；两者都不选，就别写进规范。

## 五、残余风险与"已知不做"（2026-09-17 审查收口）

### 5.1 残余风险（认下，不修）

| # | 风险 | 为什么认下 |
|---|---|---|
| R1 | 用户工程里"布局规则变了要删子表 `*-flow.yaml`"仍靠人记（G3 的用户侧那一半） | 自举树那一半已有仪器；用户工程要靠"布局版本戳"，要动 DSL 格式与全链校验，收益不抵改动面 |
| R2 | `api_audit` 不在验收路径（G8） | `hygiene` 已进门⑧、`equiv` 已进门⑩；只剩 `api_audit` 要 git 底本、仍是属"改完手动跑一次"的仪器（见 G8） |
| R3 | 面③ 现在每次要**重造整棵自举树**（生成器 + 整树 build）才比对基线 | 这是"基线改错了也要红"的唯一办法，慢 1–2 分钟换一百多个文件的守卫，值 |
| R4 | 注释是否冗余只能靠通读（G4） | `contract` 的 `注释归属` 段已把"体积不是冗余"写成反例；机械指标分不出"必须留的 why-not" |
| R5 | "回写自检为假"那条路没有夹具（G10） | 要造一份**必然过不了** H1–H8 的回写结果，夹具成本高于这条断言的价值 |

### 5.2 已知不做（想过，决定不做）

> 来源：2026-09-17 两轮并行审查的"不做"视角（10 个子代理按分层逐单元列，39 条），加上此前几轮已否决的。
> **这一节的作用是"下个会话别重新提议"**：每条都给出证据位置与一句"为什么不值"。级别只分两档：
> 不值得做（收益 < 成本）· 不该做（与既有纪律冲突）。

| # | 不做的事 | 为什么不值 / 不该做（证据） |
|---|---|---|
| N1 | 不做"基线重钉"脚本 | 顺序错的**后果**已被"自举树基线含全部模块表 + 重造逐字节相同"兜住（`examples/README.md`「刷新基线」） |
| N2 | 不给注释设体积指标 | `contract.py` 的 `注释归属` 段明写：体积不是冗余，机械裁判会逼人"删掉有用注释去凑数" |
| N3 | 不把 `esc`/`_esc`/泳道底图/边标签发射抽进公共层 | 模块层零横向 import 是门⑦底线；三份共享的只有约 6 行算术，markup 天生不同（`render_drawio.py` `esc` 上方、`render_svg.py` 文件头各写过一次理由） |
| N4 | 不引入新形状 / 类型白名单 | 四类型 ←→ 三形状是 D-74 铁律；`RECT_SHAPES` 那次是**收窄**，不是扩形状 |
| N5 | 不改 `Report.all_same` 的返回语义（D-85③） | 它同时被 `--json` 与尾行消费；逐条补条目更小 |
| N6 | 不把 `DEFAULT_COL_X` 搬进字典 | 它是**内部展开基准**（用户调的是 `col_pitch`）；家在 `geometry` 已只有一个真值（D-84⑥） |
| N7 | 文本宽度启发式不换 `unicodedata.east_asian_width` / 不引 `wcwidth` | 实测 30 个真实标签下两者**逐字同值（0 处不同）**；`0x2460–24FF` 那段是**有意的政策声明**，换标准库后 A/F/W 的取舍还得另写一条（`semantics.py:208-211`） |
| N8 | 不把五处"占位符集合"收敛成一个常量 | 五处回答的不是同一个问题（字段空值 / 解析终止符 / 图例占位 / 改动归一），合并会造出**假的单一真源**；且"等价占位符不算改动"已有机检（`gates.py` 伪 diff 那节） |
| N9 | 不把 `TYPE_RULES` 阈值搬进 `dictionary.yaml` | 字典自称"可按图覆盖的**视觉映射**"，而 H1/H4 是结构门禁——进了字典就成了 per-diagram 旋钮；松紧的唯一档位是 D-55 的 `--quality` |
| N10 | 不改 `Finding(str)` 为 dataclass / 不强制包装 | D-55 的零改动迁移选择：`gates.py` 有 331 处 `c.check`、69 处直接点名 H 编号文案，改类型=重写整个输出面 |
| N11 | 不抽 `('rhombus','ellipse')` 为常量 | 形状集是**键封闭**契约（N4），"漏改"的输入在纪律上不存在；`RECT_SHAPES` 是正向登记、这是补集，立常量等于把补集写成第二处真值 |
| N12 | 不把布线 `tries`/`sweeps` 提进字典 | 它们是**算法搜界护栏**（防死循环 / 迭代上限），不是要被人按图调的画布参数；一旦可调，视觉退化会从"过不了门禁"扩到"过得了门禁但难看" |
| N13 | 不统一 `fit_base_height` 的取整口径 | 分叉只在用户填**离格值**时发生（本就违规）；改成 `snap` 只是把 100 换成 80——换一种同样任意的答案，还要重钉自举树 |
| N14 | 不把形状反查扩到 drawio/svg | 已有几何自检按产物自称形状兜住大半；剩下能抓的只有"几何恰好等价、形状仍旧错"那一种，为它造两层对称守卫=给已有机检的规则再加一层 |
| N15 | 不把注册表的 `ids`/`geom` 合并成一个 reader | 两者对应**两个消费者**（契约反查 / 几何门禁），合并正好违反"第二个消费者就不许合并"；writeback 只走 `read()`，不要几何 |
| N16 | 不把 `xml_reader` 的兜底列坐标收回唯一出处 | 兜底只在"外部图一个 `mxGeometry` 都没有"时走到（drawio 存盘必写）；要改得给 `assign_grid` 穿 cfg 参数=接口变更，收益为零 |
| N17 | 不合并 `manifest.check` 的 `html=`/`drawio=` 与 `products=` 两条分支 | 前者是文档承诺的公开 CLI，后者是 W6 给编排层的通用入口；合并=把注册表形状泄漏进契约模块 |
| N18 | 不把渲染器常量（`MAX_VIEWS`/`SUB_INSET`/`EDGE_FLOW_*`）提成字典或 CLI | 它们是**规格**不是参数：2px/1px/55% 是 D-78 实测定的，`MAX_VIEWS` 是护栏且撞上限已显式报出 |
| N19 | 不把 `render_html` 的 CSS/JS/整页模板外置成文件 | 产物必须自包含（D-52），源侧外置一个字节都不减，只多一条"资源读不到"的失败路径；而那段字符串里的注释正是 D-76/78/79/80 的取舍记录 |
| N20 | 不让 `render()` 返回 receipt 字典 | 统一契约在 ARCHITECTURE 9.8 已改判成"成功信号 = 显式 `return 0`"；回执字段 build 末尾已算过并打印（D-55④） |
| N21 | 不让 `build.STAGES` 驱动执行 | 它存的是**函数对象**，与"注册表只存名字、调用时现查 globals()"正相反——那正是 `gates` 靠替换模块名证明替身被调到的前提（会让替身用例假绿） |
| N22 | 不把 `_render_ctx` 的分派搬进注册表描述符 | ctx 是**调用期数据**；装进模块级常量就得放函数对象，撞 N21 同一条纪律，而 W2→W3、W5→W6 已两度为"改注册表结构"返工 |
| N23 | 不把 14 处 `stdout.reconfigure` 抽成公共 helper | 这条规则**已有仪器**且判据是字面量（`contract.py`「入口命令都要重设 stdout 编码」）；抽走字面量断言当场红——为已机检的规则再加一层 |
| N24 | 不做"一次 `engine.load` 全链复用" | 阶段之间的接口**就是路径**，这是 9.1 定的契约形状，且 `gates`/`invariants` 正是当 CLI 直接跑渲染器 |
| N25 | 不给 `coverage --allow` 找常驻白名单消费者 | 它按设计是"一次性排查的逃生舱"；接进门③等于让验收自己给自己发豁免，"100% 覆盖"当场失去意义 |
| N26 | 不把 `accept` 的截图探针与 `shot.py` 的命令行抽成同一处 | 探针只有一个消费者，且它刻意独立于 `shot.py`（要比的正是"这台机器按这条命令能不能落图"）；动 `shot.py` 公开面还要走 API 记账 |
| N27 | 不把 `coverage`/`selfboot_gen`/`hygiene` 的正则与字面量集合抽公共层 | 三个消费者的语义不同（摘名字 / 保证生成器造的名字落在外侧 / 动态出口花名册），抽出来只是一个正则常量；且两份不同步会被各自的告警撞出来 |
| N28 | 不给"见某文档某节"加锚点机检 | 锚点形态无法机械解析（章节/表/函数名混用），必然要白名单；空锚点读者一搜即知，不像静默丢数据那样瞒得住 |
| N29 | 不加"生成器新鲜度"门 / 不给门⑦ 补"表比图旧"的 mtime 判据 | 后果已由面③"按图重造自举树并逐字节比"守住；不重跑生成器的廉价检查只能重述顺序，属**假仪器**（`accept.py` 自述"不重跑生成器"） |
| N30 | 不给 `accept` 加 `--only`/`--skip` | 与它"一条命令跑完、只给一个结论"的存在理由冲突：过滤器会开出"跳过已知红的那道门、照样退 0"的口子 |
| N31 | 不把 `hygiene` 的判据扩到 `dev/` | `dev/` 的 4 处未用 import／死函数不改变任何产物字节与退出码；而把 `dev/` 塞进依赖图会让 `coverage` 的分母多出上百个 dev 函数 |
| N6 | 不把 `DEFAULT_COL_X` 搬进字典 | 它是**内部展开基准**（用户调的是 `col_pitch`）；家在 `geometry` 已经只有一个真值 |

### 5.3 待业务方拍板（2026-09-17 已拍板，见 D-86）

| # | 事项 | 结论 |
|---|---|---|
| U1 | `render_svg`（交付物③）要不要补齐 `⚠` 留痕、边标签、"行动所需时间"行 | **补**：朴素指没有交互与动画，不是内容可以少；三份产物同口径已由面② 五条断言守住 |
| U2 | 字典 `shapes.task.arc: 6` 的量纲 | **以 drawio 的 `arcSize`（占短边百分比）为准**：html/svg 走 `geometry.arc_px()` 换算，drawio 原样写 |

> 本节只留"拍板结论"一行；理由与影响面全在 `dev/DECISIONS.md` D-86（同一条纪律：同一规则只陈述一次）。
