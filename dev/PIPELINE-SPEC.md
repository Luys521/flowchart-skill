# PIPELINE-SPEC.md — 材料到出图的流水线规范（L0—L4）

> 状态：**L0 / L1 / L2 已落地并进验收路径**（`probe → ledger → intake → plan` 全链有仪器，每轮验收都跑：
> 门⑪ 的材料链夹具）；**L3 / L4 就是本仓既有的流程表与出图机制**（H1—H9 + 渲染）；
> **§6 的 H10 仍未实现**（判据已定，上线清单见 §8.1）。
> 本文件是 L0—L4 契约的**唯一出处**；实现与本文不一致时，先改本文再改代码。
> 读者：**改这个仓库的人**。出图时的 AI 不需要读它（维护区，见 `dev/REPO-MAP.md`）。
> 先读：`dev/ARCHITECTURE.md`（模块怎么切）、`dev/REPO-MAP.md`（数据怎么流）。
> 组织：§0 术语 → §1—§4 逐层规范（探测 / 账本 / 清点 / 计划）→
> **§5 循环的发动机（漂移 → 缺口）** → §6—§8 接缝、边界与登记。**写一节、审一节**。
>
> **§5 的位置不是排版偏好**：§1—§4 各管一层，§6 管 L0 与既有结构校验的接缝，
> 但它们串起来**不是一条单向流水线**——真实过程是"先落表（带假设）→ 表暴露漂移 → 漂移定位缺口 →
> 按缺口定向取证 → 修表 → 再审"，**§1—§4 全是这条循环调用的工位**。先读 §5 再读各层，顺序才不反。

## 0 术语（只在这里定义一次）

| 术语 | 定义 | 不许混用 |
|---|---|---|
| 材料 | 用户给的原始文件。**只读，永不修改** | 不是"证据"，也不是"流程表" |
| 证据 | 材料解析后的**最小可引用单元**（element） | 不是"材料"本身，也不是流程图上的"节点" |
| 证据账本 | 一次任务的全部证据，落盘为 `evidence.json`（唯一事实源） | 不另存手改的 md 视图—双份必漂 |
| 清点 | 对材料做编目：每份一张卡片 | 不是"校验" |
| 材料卡片 | 一份材料的记录：档位 / 主题 / 是否含流程 / 读不动原因 | |
| 计划 | 流程清单 + 关系 + 映射 + 待澄清，落盘为 `plan.md` | **不是"流程表"**：计划说"做几张、谁依赖谁"，流程表说"这一张怎么做" |
| 流程表 | 唯一事实源，12 列（列规范见 `references/flowtable-spec.md`） | 计划 ⇒ 表 ⇒ 图，三段不许互相代替 |
| 子流程 | 被父流程的**一个节点展开**出来的流程，走 `⊞` + `parts/` | 只有"甲"算嵌套（§4，未写） |
| 出处 | 证据的位置：材料 + 页/表/单元格/坐标 + 摘录 | |
| 推断留痕 | `⚠`：AI **有依据**的推断，不问用户 | 与"待裁决"是两回事 |
| 待裁决 | `⚠?`：AI 推不出、**必须**用户拍板 | |
| 澄清申请 | 面向用户的一组问题（每问附推荐答案） | |
| 澄清答复 | 用户对澄清申请的答复。**它是新材料**：回 01 重走探测→解析→落账，不许就地改表 | 与现有 07→回 01 同一条纪律 |
| 漂移 | **表里的一条断言与"已知道的"不一致**。"已知"只有三源：证据账本 / 假设账 / 表自身；判据 D1—D6 见 §5.2 | 不是"表写得不好看"，也不是**结构错误**（那是 H1—H9 的地盘，§6） |
| 缺口 | 为消掉漂移、补上逻辑环而**必须补上、手头却没有**的东西（落 `drift.md`，§5.3） | **不是"澄清申请"**：澄清申请只是它里面"必须业务方拍板"的那一类 |
| 取证 | 按缺口**定向**撬开材料 → 产物落盘 → 子代理读成摘要（§5.5）这一段动作 | 不是"解析"（解析是 §1.4 适配器的职责）；取证是**有具名消费者、被缺口限定范围**的那一次解析 |

材料标识：`M01`、`M02`… 编号顺序 = **材料路径字典序**（同一次任务内稳定、可复现）。

**责任边界**：本流水线交付的是**可追溯的重建**，不是**正确性担保**（用户只对最终渲染图负责，见 `SKILL.md`）。
因此：填不出的字段、判不出的归属、读不动的材料，**一律留痕**（`⚠` / `⚠?` / 读不动），不许用"看起来合理"的话术抹平。

### 0.1 流程节点的**采集与判断归 AI 自由发挥**（脚本只做四件事）

**这条排在所有工位之前**，因为它是本流水线的分工原点（用户 2026-09-18 明确指出；此前各节都在讲工位，
**没把这条边界写成一条**，于是我要么把机械活写成"必经路径"，要么让人以为"判断也该由脚本给"）。

**归 AI（自由发挥，脚本不许代劳）**：

| 判断 | 说明 |
|---|---|
| **该有哪些节点** | 从哪份材料的哪一句里看出一个步骤、哪些句子只是**同一步的另一种说法**、哪些是**注解不是步骤** |
| **从哪来** | 每个节点的「依据」指到哪个 element id（§2.5）——这是"可追溯"的落点 |
| **什么类型** | 是不是判断（出边 ≥2 且每条带条件）、是不是并行出口、是不是子流程（`⊞`） |
| **怎么连** | 并还是拆 · 甲 / 乙 / 丙（§4.3）· 回边画在哪 · 例外分支要不要单列一个结局 |
| **谁在做** | 执行主体 / 执行者（材料没说就填推断并标 `⚠`，**不是留给脚本猜**） |
| **哪份材料算流程** | 清点的「含流程 = 是 / 否 / 不确定」（§3）· 拆成几条流程（§4.2） |

**归脚本（只执行，不承载判断）**——`scripts/` 只干四件事：

| 干什么 | 谁 |
|---|---|
| **认料**：这是什么格式、读不读得动、抽出哪些 element | `probe` / `parse*` / `render_pages` / `ledger` |
| **记账**：把机器可判的事实写进三本账（材料层 / 清点骨架 / 计划骨架 / 漂移账） | `recon` / `intake` / `plan` / `drift` |
| **落笔**：按**列名**把 AI 的判断写进产物，保证不填坏表 | `cells` / `import_table` / `table_to_dsl` |
| **校核**：把"自洽"与"一致"变成读数 | H1—H9（`table_to_dsl --check`）· D1—D5（`drift`）· 各 `check` |

**三条不许（脚本侧）**：
1. **不许替 AI 决定节点集合**——没有"由材料自动生成节点"的功能，也不打算有（"从材料里看出步骤"是理解，不是转换）；
2. **不许因为判据跑不出来就替 AI 下结论**——判据只报"哪条断言与已知不一致"，**它不下"这步该不该存在"的判断**；
3. **不许把某条路径变成必经**——`import_table.py`（外部节点表 → 契约表）与 `plan.py --split` 都是
   **AI 的一只可选的 hand**：有现成的准流程表就用它省力，没有就**直接落表**，两者都不影响 §5 的循环。

**两条必须（AI 侧）**——"自由发挥"不等于"不可复现"：
1. **判断要留痕**：依据指到 element id、推断标 `⚠`、待拍板标 `⚠?`、被推翻的写进假设账；
2. **判断要能被校核**：H1—H9 与 D1—D5 是**"自洽吗 / 与账本一致吗"**，**不是"你想得对不对"**——
   后者只有业务方能判（就是 §0 责任边界那句话的落地）。

**先落表还是先清点，由 AI 定**：§5 的发动机允许**先落一张带假设的表**，缺什么由漂移 / 缺口派活——
清点与计划是那台发动机的**工位**，不是开工前的关卡（§1—§4 各节的"输入"写的是"它需要什么"，
不是"你必须先跑什么"）。契约只要求一件事：**每一格都有个值**（填不出来就 `—` / `⚠` 推断）——
**它不逼你猜对，只逼你别留白**。

### 0.2 产物里的「AI 格」只写 JSON，不自排版（2026-09-18 立，四个产物都适用）

```text
<模块>.py build ... --todo <产物>.todo.json      # 出「待填清单」：键 + 现值 + 取值域 + 要求
python scripts/cells.py fill <产物>.md <答案>.json   # 按**列名**把答案写回产物
```

`recon.md` / `intake.md` / `plan.md` / `drift.md` 都是"机器列 + AI 列"混排的 markdown 表格。
**AI 只写答案 JSON，落笔由脚本做**（公共层 `scripts/cells.py`）——列序、竖线转义、格式全部出不了错。
为什么把它写成硬要求（真材料集实测，同一条路上踩了三次）：`.md` 材料的缩样把正文里的 `|` 带回侦查表，
那一行的列数从 12 变 18；AI 的填格脚本只能**按列序号**硬编码（列序一改就静默填错列）；值里带竖线或
换行会把表格撕开。**根因是同一个**：把「AI 的判断」和「表格语法」混在一处，让 AI 去当排版工。
接口只保证"填得进去、填不坏表"；值填得对不对仍是各层 `check` 的事（取值封闭 / 逐字等于账本 / 推断留痕）。
**机器列一律不许经这条路改**：要改机器列，回 §1 重走。

**任务级产物住哪**：`evidence.json`、`intake.md`、`plan.md` 都住**成果根**（用户给的路径，或 C 档默认根），与各 `<流程名>/` **平级**；**流程目录里只有原七件套**（`flowtable.md`、`checklist.md`、`<流程名>-index.md`、`<流程名>-flow.{yaml,manifest.json,html,drawio,svg}`）。

**自包含优先，环境能力只当加速器**（硬约束）：本 SKILL 必须能**独立跑**—**不许依赖宿主环境里的任何其它技能或工具**。
- 每条能力都要有一条**自包含实现**（只用本仓 `scripts/` + **显式声明的依赖**）。
- 宿主恰好提供的能力**只在探测到时可选用**，用来提速/提质，**不许成为唯一路径**（降级要留痕：见 §1.4 的 `extractor`）。
- **环境要求要显式声明**：哪些格式需要外部工具，缺了给**可执行**的提示，不许静默降级、也不许假装能读。

## 1 格式三档 + 内容探测

### 1.1 三档定义

| 档 | 含义 | 谁读 | 典型格式 |
|---|---|---|---|
| T1 直读 | 抽取器直接给出文本/结构 | 解析器插件（脚本） | `.docx` `.xlsx` `.md` `.txt` `.csv` `.json` 等 |
| T2 转换后可用 | 需转换器抽一次，抽完等价 T1 | 转换器插件（脚本） | legacy `.doc` `.xls`、**文本型** `.pdf`、`.html` |
| T3 需视觉 | 文字与结构都在像素里 | **AI 视觉** | **扫描型** `.pdf`、`.png` `.jpg`、截图、白板照 |
| T4 不参与 | 与流程无关或读不动 | — | 音视频、可执行文件、探测失败者 |

### 1.2 探测规则：**按内容，不按扩展名**

硬理由（实测）：真实材料里两个 `.pdf` 会分属 T2 与 T3；legacy `.doc`/`.xls` 与 `.docx`/`.xlsx` 扩展名像、容器完全不同。

```text
1) 扩展名 → 候选档（只是先验，不作结论）
2) 魔数/容器探测：
   PK..          → OOXML/zip 容器 → **按"确切部件在不在"判**（word/document.xml / xl/workbook.xml /
                   ppt/presentation.xml）；判据与读它的那个适配器**同源**，见下面的不变式
   D0 CF 11 E0   → OLE 复合文档 → T2（legacy doc/xls）
   %PDF          → 查文本层：抽前 N 页，字符密度低于阈值 → T3，否则 T2
   图片魔数      → T3（不用扩展名判，防改名）
   压缩包        → 先列目录，再对每个成员递归判档（不许直接标 T4）
3) 探测必须记账：每份材料一行 {档位, 类型 kind, 依据}；探测失败 → T4 + 原因，不许猜
```

**一条不变式（可机器核）**：**`tier=T1`（可直读）⇒ 必须至少有一个适配器认领。**
探测的**声明不许比读者的射程宽**——"T1"的含义就是"抽取器直接给出文本"（§1.1），
**没有读取器的格式只能记 T4 + 可执行提示**，不许说它可直读。

**为什么把这条写成不变式**（2026-09-18 审计实测，三类硬失败都由它兜住）：
- `probe` 曾按**目录前缀**判 OOXML（`word/` 在就行），而 `parse_ooxml` 要的是**确切部件**
  （`word/document.xml`）⇒ "有 `word/` 却没有 `document.xml`"的包被判 T1 却没人认领；
- `.pptx` 曾被判 T1 可直读，而当时本仓**没有 pptx 读取器**（python-pptx 是可选依赖、未落地）——
  **现已补上零依赖读者**（公共层 `pptx_text` + `parse_ooxml.parse_pptx`），这条留作"声明不许比射程宽"的实例；
- 合法 xlsx 改名 `.et`（WPS 后缀）：探测认了，而 `openpyxl` **按扩展名拒收**。
三类都让分派器判"漏认"并**整条链退 1、不落盘**。修法不是在分派器加特例，而是：
**判据与 reader 同源**（同一句部件名）+ **没有 reader 就不许判 T1** + **读者按内容开门**
（`parse_ooxml` 从内存 `BytesIO` 打开，不让扩展名投票）。

**`kind` 是机器可读的材料类型**（封闭枚举，见 §2.1）：本仓"这是什么"的**唯一判据源**是 `probe`。
**消费者只许读 `kind` 与 `tier`，不许按扩展名或魔数重判**（消费者 = 侦查 / 渲染 / 清点 / 计划）——
审计实测过：同字节材料改个文件名（真 docx 改名 `.doc`），两处判据就给出互相矛盾的结论
（一边建议"直读"、一边建议"调 soffice 转换"），而"按内容判"正是本节的第一条理由。

**生产者不受上面那句约束，但受一条更硬的约束：判据必须同源。** 适配器也要"按内容开门"（§1.4：
`parse_ooxml` 从内存 `BytesIO` 打开，不让扩展名投票；`parse_legacy` 只认 OLE 流标记），
而**它认领一份材料时用的，必须是与 `probe` 判 `kind` 时同一句判据**——`probe._ooxml_kind` 认
`word/document.xml` / `xl/workbook.xml` / `ppt/presentation.xml`，`parse_ooxml.container_kind` 认的
就是这三句（两处注释互指，改一处必须改另一处）。
**为什么这不违反"唯一判据源"**：两处问的是**同一个问题**（部件在不在），答案自然是同一个；
真正的第四份真值长成另一个样子——一边按目录前缀 `word/` 判、一边按确切部件判（§1.2 开头的审计实例）。
所以 §1.4 那张分派表里仍然**只有顺序、没有判据**：认领的判据归各适配器，但它必须与 `probe` 同源。

**可解析格式清单是开放式的**：它是"`probe` 的 `kind` 判据 + 各适配器的认领判据"两处**同源**判据的并集，
不在本文里手写一份封闭清单——手写清单必漂（见 `dev/coding-spec.md` 第三节 G7）。
**本版没有插件注册表**：§1.4 那四行（`name` / `supports` / `extract` / `notes`）是**接口形态**，
落地的形态是"一个模块 + 一个命令行 + 一句认领判据"，"清单由注册表派生"这句话因此改成
**"由同源判据派生"**；注册表等真有第三方适配器时再谈（§7 YAGNI）。
上表的「典型格式」只是例子，不是边界。

N 与密度阈值属**数值**：落地时必须写进 `scripts/dictionary.yaml`（数值只有一个家），本文只写"要探测什么"。

### 1.3 三档的产出义务

- T1/T2：产出进证据账本，`extractor` 写具体插件名；**且必须过 §1.5 的抽取质量门**——
  抽不出来算读不动（诚实），抽出来是碎片 / 坏字符则**不许当 `direct` 证据**（污染唯一事实源，且没有仪器看得见）。
- **T3 也必须产出证据**（element 带 bbox 与摘录）—否则流程表「依据」列会指向空，H10 断链。T3 的 `extractor` 记 `vlm`，`certainty` 一律 `inferred`（视觉推断不许伪装成直取）。
  **产出的路**：`render_pages.py` 把材料渲成图（PDF 走 pdfplumber + pypdfium2，**自包含**；图片材料本来就是图，直接读原文件）→ AI 读图 → 填工具给的骨架（`text` / `bbox`）→ `render_pages.py check` 校验 → 交账本。
  **引用它的节点按推断档走**（`⚠` / H10.2）：视觉读数是"我看到的"，不是"文件里逐字写着的"——**不许当逐字引用**。
- T4：作为 materials[] 的一条记录进账本（status=unreadable + reason，见 §2.1），**不静默丢弃**；清点只读账本，不另立一份。
- **"读没读出来"由解析阶段记账**（材料层补注，见 §1.4）：`extractor` 写走的是哪条路，读不动的写 `status=unreadable` + **可执行**原因。
  这一条不是补充说明，是**必要的一环**：`probe` 只能给档位——4 份 legacy 与"能读的 legacy"在探测阶段长得一模一样，
  没有补注，账本上它们就是"tier=T2 / status=ok / 零证据"，看着像能读却没内容。

**这一版 T2 与 T3 都要做**（不是"先用能读的那些起步"）：T2 走转换器（legacy 二进制、文本型 PDF），T3 走视觉解析。两条路产出的文字同样要进账本、带出处—否则 H10 断链。

### 1.4 解析器插件的接口契约（"可插拔"的最小面）

```text
name                                  插件名（写进 element.extractor）
supports(探测结果)        → bool   它认不认这一档/这一种容器
extract(材料路径, options) → {elements[], warnings[], error?}
notes                     → [{material_id, status?, reason?, extractor?}]   材料层补注（见下）
```

**这四行是接口形态，不是本版的代码形状**（2026-09-18 审计：原先读起来像"已经有一个插件注册表"）。
逐行对照落地的样子——**功能都在，缺的只是"注册表"那一层**：

| 接口行 | 本版落地成什么 |
|---|---|
| `name` | 适配器写进 `element.extractor` 的那个字符串（`py:docx` / `py:openpyxl` / `py:pptx` / `py:pdfplumber` / `py:text` / `soffice+py:docx` / `vlm`） |
| `supports` | **认领判据**——`parse_ooxml.container_kind` / `parse_legacy.ole_kind` / `probe` 判出的 `kind`；按 §1.2，它必须与 `probe` 同源 |
| `extract` | 各 `parse_*.py` 的主函数 + 命令行（`--materials … -o elements.json`） |
| `notes` | 各适配器返回的补注，由 `parse.py` 合并、`ledger.py --notes` 校验落盘 |

也就是说：**"可插拔"现在是"加一个模块 + 在 `parse.py` 的顺序表里加一行"**，没有插件对象、没有注册表。
方向没变（接口留得出来），但"注册表派生清单"是**设计意图**而不是现状——按 §0"声明不许比射程宽"，
本文一律按现状写；真有第三方适配器时再兑现注册表（§7）。

**材料层补注（notes）**：材料层由 `probe` 起头（档位 / 魔数依据），"这份到底读出来没有、走的是哪条路"
只有解析阶段知道。补注是唯一把这条现场事实送回账本的路，**只许改 `status` / `reason` / `extractor` 三个字段**
（键封闭），由 `ledger.py --notes` 校验并落盘，**落完再校一次**（`unreadable` 必须有 reason）。
**不归自己管的材料不许补注**：`parse_ooxml` 看到 legacy 只说"不是 OOXML"，**不下"读不动"的结论**——
那是 `parse_legacy` 的地盘，抢着下就是双份真值。

**分派是查表，不是判断**：档位到读者的映射是固定的（T1/T2→脚本、T3→视觉、T4→只记账），没有裁量余地；
**`T3` 不归文本链**：`parse.py` 的"漏认"判据**排除 T3**——视觉路的 owner 是 `render_pages.py`，
它在文本链上"零证据"是**正确行为**（夹具实测的教训：一张 PNG 曾被算成"探测说谎"、让整链退 1）；
流程图上这一步的执行主体是**脚本**，不是 AI。

**分派器 `scripts/parse.py`**（编排层）：按**固定顺序**跑各适配器（`parse_ooxml → parse_pdf → parse_legacy → parse_text`），
合并产物与补注，可一并把账本写掉（一条命令 = `probe → parse → ledger`）。它这张表里**只有顺序，没有判据**——
"什么材料归谁"由各适配器自己认领（认不出的**不记账**，留给别人），**而认领用的那句判据必须与 `probe` 判
`kind` 的那句同源**（§1.2：两处问的是同一个问题，"同源"才让分派表可以没有判据）。再抄一份格式清单就是
**第四份真值**（`probe` 的档位 + `kind` + 各适配器自己的认领判据已经在说这件事），必漂。

两条硬要求：**元素按 `material_id` 稳定排序**（材料内部保留该适配器的阅读序）——同输入同字节；
**冲突与漏认都退 1 且不落盘**：
- **冲突**：同一 element id 被两个适配器产出（判档重叠），或两份补注对同一材料写出不同的值——静默取一个会
  让账本里出现"没人知道哪来的"记录；
- **漏认**：`probe` 说 `status=ok`，却**既无元素、也无补注**（例：纯文本材料至今没有适配器）——
  这正是账本里最隐蔽的那个洞（"能读却没内容"），不许再让它静默流进账本。
  **它上线当轮就抓到一个真 bug**：`probe` 用 8 字节头判文本，中文一个字 3 字节正好被切断 ⇒
  合法中文 `.txt` 被判 T4；已改成 4 KB + 允许尾部截断（另补 UTF-16 BOM）。

**自包含优先，环境能力只当加速器**（按材料类型分工；**降级必须留痕**）：

| 材料 | **自包含路径（必须实现）** | 可选加速器（**探测到才用**） | 都没有时 |
|---|---|---|---|
| OOXML（docx / xlsx / pptx） | docx/xlsx 用 Python 库（python-docx / openpyxl）· **pptx 零依赖**（标准库 zip + `ppt/slides/*.xml`，公共层 `pptx_text`）—**依赖要显式声明**（见下）。**读者按内容开门**：从内存 `BytesIO` 打开，不让扩展名投票 | 宿主本地 Office SDK（若存在） | 记 T4 + **给可执行提示**（装什么）；**没有读取器的格式不许判 T1**（§1.2 的不变式） |
| **文本型 `.pdf`（T2）** | `pdfplumber`（**纯 Python**，无外部二进制）抽文本层—一页一个 element | — | 记 T4 + **可执行提示**（装 pdfplumber） |
| **legacy**（.doc / .xls / .ppt，以及 WPS 的 .wps/.et/.dps） | **没有纯 Python 的可靠读法** ⇒ 只用**外部转换器**（LibreOffice / soffice，若装了）：转成 OOXML 再交给 T1 适配器抽（§1.1 的 T2 定义就是"转换后可用"），`extractor` 写 `soffice+py:docx`（三种目标都算，`.ppt→.pptx` 也有人读）。**缺转换器时按族给话**：`ole_kind` 认出 `WordDocument` / `Workbook` / `PowerPoint Document` 流 ⇒ 提示**指名族与另存目标**——真样本实测（`关于韶关…告知函.wps`，WPS 产出）它其实是 **Word 97-2003 族**，最省事的动作是"**用 WPS 另存为 .docx**"，泛泛说"另存为 OOXML"等于没说 | 宿主本地 Office SDK（若存在） | 记 T4 + 可执行提示（指名族与另存目标，或"装 LibreOffice"） |
| 图片 / 扫描件 / 截图 / 白板照 | `render_pages.py` **转图片**（图片材料本来就是图，直接读原文件） | **宿主多模态模型**（判据：AI 自报本会话能否直接看图）；OCR（tesseract / paddleocr，可选依赖） | 记 T4 + 进澄清 |
| 纯文本（md / txt / csv / json …） | `parse_text.py`（**只用标准库**）：**解码 + 按行打块**（一块一个 element，`extractor=py:text`；标记 / 数据文件写 `py:code`）、`.csv` / `.tsv` → `table` + `rows`。**不认结构**（见下） | **宿主模型直读文本**：理解归它 | 直读不可用时仍走得通（标准库永远在） |

**能直读的格式：理解归模型，记账归脚本**（这一条把"要不要给文本写解析器"定死）：
`.md` / `.txt` / `.csv` / `.json` / `.yaml` / `.xml` / `.html` 宿主模型**能直接读**，而且比任何结构识别读得好
——所以 `parse_text.py` **不认标题、不认列表、不抽表格语义**，它只干记账那件事（可引用 id / 可复现 /
编码与上限）。给它加结构识别 = 与直读重复，而且**认错了会让 `kind` 撒谎**（账本是唯一事实源，撒谎代价最高）。
反过来说：直读**只用于"理解"，不许当证据来源**——要引用就必须引账本里的 element id（§2.5 / H10），
"我记得文件里写着…"是一条无法机器复核的引用，正是 H10 要断的链。

**纯文本的编码不猜**：只认 UTF-8 与**带 BOM** 的 UTF-16；GBK 这类本地编码要读就显式 `--encoding gbk`——
它同时**授权**去试 `probe` 判成 T4 的纯文本材料（仍限纯文本扩展名），读出来就把 `status` 改回 `ok`。
为什么不能自动猜：GBK 几乎能解**任何**字节对，拿它当判据等于把二进制当文本（与 `probe` 的 NUL / UTF-8 判据同一取向）。

**环境清单要显式**：新增依赖（Python 包）与**可选外部工具**（转换器）都要写成清单（如 `requirements.txt` + 一节"可选外部工具"）；
缺依赖报的错**要可执行**（说清装什么、或改走哪条路）—不许静默降级。

`extractor` 必须写清**具体走了哪条路**（`sdk:edsdk` / `vlm` / `ocr:tesseract` / `py:openpyxl`）—否则 §0 的"可追溯"就是空话。

**依赖清单（自包含侧的显式声明）**：

| 类别 | 清单 | 缺了怎么办 |
|---|---|---|
| **必须**（跑通主链） | `PyYAML`（已有）+ `python-docx` + `openpyxl` + `pdfplumber`（文本型 PDF） | 报**可执行**的错：装什么、装完重跑 |
| **可选**（增强） | `pytesseract` / `paddleocr`（图片 OCR） | 该格式降级为 T4 + 提示 |
| **外部工具**（通用工具，不是宿主技能） | LibreOffice / `soffice`（legacy 转换） | legacy 记 T4 + 提示「另存为 .docx」或「装 LibreOffice」 |

> **`.pptx` 已不在"可选"里**：它由标准库读（zip + `ppt/slides/*.xml`），不再需要 `python-pptx`——
> "能不能读一份演示稿"这件事，不该取决于环境里装没装一个第三方包（§0 自包含优先）。
| **加速器**（宿主提供，**不许依赖**） | 本地 Office SDK · 多模态模型 | 探测不到就当没有，走自包含路径 |

**加速器探测判据**（「探测到才用」必须可执行）：

| 加速器 | 判据 | 判不出的默认 |
|---|---|---|
| 本地 Office SDK | `edsdk.py` **存在** 且 `list` 子命令**能通**（退出码 0 且有工具输出） | 视为不存在 |
| 多模态模型 | 由 AI **自报**「本会话能否直接看图」 | 视为不能 → 走 OCR / T4（**不许猜能看**） |
| **宿主模型直读文本** | 由 AI **自报**「本会话能否直接读文本」（`.md`/`.txt`/`.csv`/`.json`/`.yaml`/`.xml`/`.html`） | **视为能**（读文本是通则）；不能时走 `parse_text.py`（标准库，永远可用） |
| 外部转换器 | `soffice --version` 能通（PATH 或已知安装路径） | 视为没有 |

**探测结论要记账**（写进账本的 `extractor` / 材料卡片的解析路径栏）——不许**默默用**、也不许**默默不用**。


**批量后台读怎么调**（无人值守读 N 份材料）：用 `open_file file_path=<绝对路径>` 在**后台**打开（纯后台、无用户预览时 `file_id` 通常就是路径字符串），再用 `doc_*` / `sheet_*` / `slide_*` 的读工具取内容；**不要**用 `present_files`（那是给用户看预览面板的界面动作）。三条约束：
1. **路径字符串必须完全一致**：注册时用什么字符串，后续就得用什么（`./x.docx` — `/abs/x.docx`）⇒ 本流水线**统一用绝对路径**，且只允许一种分隔符写法。
2. **pool 会累积实例**：每打开一个文件就在 pool 里留一个实例；批量读要**分批**并显式释放（`close_file` 的 schema 单独确认）—SDK 文档里"不要主动 close"是针对"用户正在看"的场景。
3. **格式缺口**：`et` / `ett` / `dps` / `dpt` 不支持；含宏格式可打开但**宏不执行**（安全）。

四条硬要求：

1. **只读**：不许修改、移动、重命名材料。
2. **不抛裸异常**：失败要返回 `error` 文本（上游按"读不动"记账，见 §1.3）。
3. **不做业务判断**：不猜流程、不合并节点—判断在 AI 侧（与 `SKILL.md` "`scripts/` 只执行，不承载判断"同一条）。
4. **输出必须符合 §2 的 element 模型**，否则账本拒绝收（键封闭，见 §2.1）。

### 1.5 侦查（recon）：先排序、再假设、最后攻坚

**触发**：两条路——① 探测发现"异常"（体积 / element 预估 / 表数超阈值，或结构不像文档）；
② **流程表暴露的缺口**（§5.3 的定向取证）。第 ② 条才是常态：**侦查是 §5 那条循环的子程序**，
由缺口触发、被缺口限定范围，用完即走（"先落表、再回头读材料"的因果见 §5）。

**为什么需要**：真实材料里有**工具型文件**（如一张 sheet 就是一台计算器的测算表），能抽出几十万字符，却**没有过程步骤**。**先侦查，再决定解析深度。**

**侦查策略（八步，顺序不可换）**：

| 步 | 做什么 | 判据 / 产出 |
|---|---|---|
| 1 | 读**元数据** | `probe.py` 已给：文件名 / 类型（`kind`）/ 大小 / `sha256` / `mtime` / tier / probe |
| 2 | **难度排序** | 键 = `(tier 难度, bytes)`，`T1 < T2 < T3`；**同输入同顺序**（§2.2 可复现）。**分档只看 `bytes`**（见下"为什么不用 element 预估"） |
| 3 | **先读易的** | T1 且体积小 → 全量解析 |
| 4 | **分堆**（**不可跳过**） | 材料 → 流程归属假设（§3 版本关系 + §4 聚类）；纪律：**宁可多拆一条，不可错并两条** |
| 5 | **堆内拼流程** | 初步流程骨架（标 `⚠`） |
| 6 | **反推上下文缺口** | 由流程结构反推"**必然存在、但材料里没有**"的东西 → 缺口清单（这才是澄清申请的来源） |
| 7 | **攻坚难的** | T2（legacy）：需转换器；T3：视觉 / OCR。**每次只攻一层，产出必须落账本并带出处** |
| 8 | **循环** | **收敛判据与预算的口径只写在 §5.4**（硬漂移 0 且缺口清单空；每份材料攻坚上限 2 轮）；超预算落 `⚠?` 交用户—**不许无限重试** |

**为什么分档用 `bytes` 而不是"element 预估"**（2026-09-18 审计实测）：原先的"内容量"是 docx 数 body 块、
xlsx 数声明行数，**两个量纲被直接比大小**（174 行的表 vs 120 块的文档谁大？），而且一旦某份表读不动，
它的量就成 0、难度直接掉到"易"（崩溃与分档耦合）。`bytes` 跨格式可比、零依赖、不会因为读不动而失真；
结构缩样（段数 / 表数 / 大纲 / 子表尺寸）仍然照做，但只作**参考数字**进表里给人看，**不参与分档**。

**手段（按此优先级；「转图片」是最后的最后）**：

| # | 手段 | 怎么做 | 状态 |
|---|---|---|---|
| 0 | **抽取质量门**（**先过这道门**） | `textquality.py`：单字行占比 · 平均行长 · 坏字符率（替换字符/控制字符/私用区）· 单一字符占比 → `ok` 照收 · `noisy` 丢**纯碎片**元素 + 留下每条挂 `degraded` · `garbled` 该材料**不入账** + 建议处置；阈值在 `dictionary.yaml` 的 `material_quality` | 可用 |
| 1 | **结构缩样**（首选） | `recon.py`：**每一种 `kind` 都有交代**——docx 读非空段/表/大纲 · xlsx 读子表尺寸 · **pptx 读张数与每张标题**（用户故事里那份"大体量 pptx 的解析摘要"）· pdf 零依赖粗数页数 · 图片读像素尺寸 · 文本读行数与头几行 · **legacy 明说"摘要不可得"**（OLE 正文要外部转换器，这是诚实的极限，不是漏做）。宿主 Office SDK 若存在可读更全（公式 / 透视表计数）。**尽力而为**：读不了逐份记"结构读不了 + 原因"，**绝不让一份坏材料把整批带崩**；大纲**先数完再切**（表里写"共 K（列前 N）"，不许把截断数谎报成"共 N"） | 可用 |
| 1a | **护栏按"要读的部件"算**（zip 系） | `recon.py` 的 `max_open_bytes` 挡的是"打开它会不会把内存吃光"，而 **docx/pptx 是按需解压**——图多不等于贵。真样本实测：一份 **42 MB** 的演示稿（媒体 42 MB、`ppt/slides/*.xml` 只有几百 KB）按**整份字节**判⇒"超护栏"⇒**摘要直接没有**，而摘要恰恰是这种大材料最需要的。现在缩样收**路径**、只解要读的部件，护栏按那些部件的字节和算（`pptx_text.scan_cost`）。**例外**：xlsx 仍按整份字节——`openpyxl` 收路径会按扩展名投票（审计 F6），只能从内存开 | 可用 |
| 2 | 转图片 → 视觉 | `render_pages.py`：PDF → 每页 PNG（**自包含**：pdfplumber + pypdfium2，实测 0.3~1.9 秒/页）→ AI 读图 → 填骨架 → `check` 校验；图片材料直接用原文件 | **PDF / 图片可用**；Office 系要转换器（本版不做，见 §8.2） |

**为什么"手段 0"必须排在"手段 1"前面**（实测，不是推演）：`probe` 说某份 PDF"有文本层"（`/Font` x161）、
`parse_pdf` 说"抽到 1563 字"——**两边都真**，可那条文本层是竖排 / 字距碎裂的水印碎片：整份 225 行里
140 行是单字行（62%），前 3 页 100%，首页抽出来是 `验 / 核 / 息 / 信 / 司 / 公 / 于 / 用 / 仅`。
**"读不动"是诚实的，"读出垃圾却当 `direct` 证据"是最坏的一种**——它污染唯一事实源，而没有任何仪器看得见。
所以：**T1/T2 抽出来的东西要先过质量门才算证据**；不过门的按上面的三级处置，**丢元素一律记账**
（不吭声地少几条，下游只会以为"这份材料本来就这么点内容"）。

**样本太小不判**（行数 < `min_lines`）：几行的材料没有统计意义，判了就是拿噪声当结论——
**宁可漏判，不可误判**（误判会把能读的材料判成读不动，那是把仪器的错算在材料头上）。

**假设必须落盘**（否则"循环"是黑箱，事后无法复现）：侦查结论写成表，**被推翻也要留痕**。
**表列逐字按 `recon.py` 的 `CARD_COLUMNS`（12 列）**——列规范在代码里只有一份，本文这份是它的抄本，
夹具 ㊶ 会核"本文里逐字找得到 `CARD_COLUMNS`"：

| 材料 | 档位 | 修改时间 | 难度 | 规模（依据数字） | 解析深度 | 走哪条路 | 读不动 | 假设角色（AI 填 `⚠`） | 依据（AI 填） | 验证方式（AI 填） | 状态（AI 填） |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `M05` 戊-补充.docx | T1 | 2026-09-18T10:00:00 | 易 | 非空段 8｜表 2｜大纲 3 | 全量解析 | 直读（py:docx） | — | ⚠ 技术要求（非流程） | 无「流程 / 审批」词，通篇参数表 | 读大纲看有无步骤章节 | 待验 |

前 8 列是**脚本填**的、后 4 列是 **AI 填**的；上表是**样子**（产物由 `recon.py build` 出，**别手抄**——
手抄会把 `修改时间`、`读不动` 这些抄来的事实变成第二份真值）。

**这张表落哪、谁填哪几格**（`scripts/recon.py`，与 §1.3 的"材料层补注"同一条分工线）：
- **脚本填**（`recon.py build`）：`材料 | 档位 | 修改时间 | 难度 | 规模（依据数字） | 解析深度 | 走哪条路 | 读不动`
  ——全部**查表**得来（`tier` / `kind` / `bytes` / `mtime` 都是 `probe` 判的），**不猜语义**；
  `修改时间` 是 **§3「替代」判据的末位兜底**（日期优先级：正文签署日期 → 文件名日期 → `mtime`），
  以前收了却不进表，等于那条判据没有输入；
- **`解析深度` 只看体积，不看档位**（2026-09-18 修）：超过 `easy_max_bytes` 就「只取摘要」。
  原先只有"T1 但很大"才降，于是 T2/T3 走另一条路——一份 50MB 的 legacy 也会被建议"全量解析"，
  那等于**一上来就全量灌**（§2.4 明令不许）。**档位决定"走哪条路"，体积决定"读多少"**，两个轴各管各的；
- **AI 填**：`假设角色（标 ⚠）| 依据 | 验证方式 | 状态`；
- **两列分开，别混一个轴**（审计：原先一格塞两个问题，下游无法机械消费）：
  - `解析深度` = 本节的**处置三选一**：`全量解析` / `只取摘要` / `不参与`；
  - `走哪条路` = §1.1 的档位映射（`直读` / `文本直读` / `PDF 文本抽取` / `转换器(soffice)` / `转图片→视觉` / `无 reader：T4 + 提示`）。
- **产物落成果根，默认名 `recon.md`**（登记在 `artifact.NON_TABLE_MD`，否则会被判"孤儿表"，§8.2）。
- **`材料卡片`（§3）不加「建议处置」列**：卡片是**标注层**（只装事实与推断），而"建议处置"是**过程性建议**
  ——原先本节写"写进材料卡片的「主题 / 含流程 / 建议处置」"，而 §3 的卡片契约里根本没有那一列（自相矛盾）。
  正确的关系是：**侦查结论落 `recon.md`，AI 据此把「主题 / 含流程」填进卡片**，卡片不照抄建议。
- **校验器**：`python scripts/recon.py check recon.md --materials materials.json`
  ——查材料一一对应、档位逐字等于材料层、`状态 ∈ {待验, 已验证, 已推翻}`、假设必带 `⚠`、
  `依据` / `验证方式` 非空，以及**`已推翻` 必须在假设角色里写成"曾记 X → 现记 Y"**（这就是"留痕"的落点）。
- **难度阈值住 `dictionary.yaml` 的 `recon:` 段**，且产物表头**打印本次用的阈值与输入指纹**——
  阈值是"人给的默认"，不是从某批样本反推的；打印出来才能事后复核（审计教训：原先的 `500` 正好落在
  某个 15 份样本的空档 (324, 606) 里，换一批材料边界就反复穿越）。

**产出与处置**：侦查结论落 **`recon.md`**（落成果根；AI 据此把「主题 / 含流程」填进材料卡片，见上面的分工表）；
处置三选一—`全量解析` / `只取摘要`（侦查结论 + 少量抽样，**不灌满账本**，这是 §2.4 降级该有的姿势）/
`不参与`（记 T4 + 理由，进 §4 的排除清单）。
**处置要能被执行**（2026-09-18 补）：`只取摘要` 不是一句标签，它对应 `parse.py` 的收窄开关——

    python scripts/parse.py --materials materials.json --elements elements.json \
        --notes notes.json --ledger evidence.json \
        --only M06,M15 --pages 40-60 --slides 1-5 --sheet 短名单 --rows 1-200 --grep 审批 --lines 1-300

**产物名要显式给**（`-o/--elements`、`--notes`、`--ledger`）：它们的默认值是**当前目录**下的
`elements.json` / `notes.json`——不给就落在**仓库根**，而那里任何一份 `.json` 都可能被下一次
`git add -A` 顺手带走（真踩过：1.3 MB 的用户材料正文差点进库，见 `dev/coding-spec.md` G13）。

`--only` 省的是**别的材料的解析代价**；`--pages` / `--slides` / `--sheet` / `--rows` / `--lines` 收窄**读到哪**；
`--grep` 只留含这个词的片段（可搜面与 `query.py --grep` **同一句**，见 `textquality.element_haystack`——
两边各写一份，会出现"取子集时说有、收窄时却把它滤掉"）。
**收窄一律留痕**：元素级的写进 `degraded`；**整份被滤空**的材料改记 `status=skipped` + 理由
（"本轮未取"）——不记的话它在账本上就是"`status=ok` 却零证据"，读的人分不清"本来就没有"与"被我滤掉了"。
**记账要求**：侦查结论算语义推断 → 标 `⚠`；**它凭以判断的结构数字（表数 / 行列数 / 大纲）要写进表里**，人能复核（对齐 §0 责任边界）。
**崩溃不外溢**：一份材料的结构读不了，只影响它自己那一行（记"结构读不了 + 原因"），
**不许让整批零产出**——审计实测过：原先一份坏 xlsx 就让 15 份材料一份都出不来、裸栈退 1。

## 2 L0 证据账本

### 2.1 element 模型

`evidence.json` 顶层：`{schema, task, materials[], elements[]}`。

**materials[]（材料层：一份材料一条）**

| 字段 | 必填 | 说明 |
|---|---|---|
| `id` | 是 | `M01`…（§0：路径字典序） |
| `path` | 是 | 材料路径（原样，不改写） |
| `sha256` | 是 | 内容摘要—幂等与"材料换没换"的判据 |
| `bytes` | 是 | 字节数（探测先验之一） |
| `mtime` | 是 | 修改时间（ISO 8601 本地时区）—§3「替代」判据的**末位兜底**（前两位是正文日期、文件名日期） |
| `tier` | 是 | `T1`/`T2`/`T3`/`T4`（§1）；**不变式：`T1` ⇒ 必须有人认领**（§1.2） |
| `kind` | 是 | **机器可读的材料类型**，封闭枚举：`docx` / `xlsx` / `pptx` / `ole` / `pdf-text` / `pdf-scan` / `image` / `text` / `unknown`。**本仓"这是什么"的唯一判据源**——别处只许读它，不许按扩展名/魔数重判（§1.2） |
| `probe` | 是 | 档位依据（魔数 / 文本层密度 / …），人可核 |
| `status` | 是 | `ok` / `unreadable` / `skipped`。口径：**ok** = 这条材料的证据已在账本里（文本侧抽到了，或已由视觉侧产出）；**unreadable** = **读不动**（缺转换器 / 缺 OCR / 文件坏）——必须给 reason；**skipped** = **不参与**（T4：与流程无关、音视频…）——必须给 reason |
| `reason` | 视 status | 读不动 / 不参与的原因（**unreadable 与 skipped 必填**），要给**可执行**的话（装什么、或改走哪条路） |
| `extractor` | 视 status | 谁产的（插件名 / `soffice+py:docx` / `vlm`）。**由解析阶段的材料层补注写**（§1.4）：`probe` 只给档位，给不出这个 |

**谁写这三格**（唯一口径）：`probe.py` 给**初始值**（按档位：T1/T2/T3 → ok；T4 → unreadable + 依据）；
解析阶段用**补注**（§1.4）改它——补注是唯一入口，`ledger.py --notes` 校验（键封闭 / id 存在 / unreadable 必有 reason）
并**落完再校一次**。清点（§3）只读账本，**不重判**这三格。

**elements[]（证据层：只装"抽出来的东西"）**

| 字段 | 必填 | 说明 |
|---|---|---|
| `id` | 是 | 稳定引用 id，规则见 §2.2 |
| `material_id` | 是 | `M01`… |
| `kind` | 是 | **封闭枚举**（见下） |
| `text` | 视 kind | 文字内容 |
| `rows` | 视 kind | 表格：二维数组（`table` / `sheet` 用） |
| `location` | 是 | 出处，见 §2.3 |
| `extractor` | 是 | 产出它的插件名（含 `vlm`） |
| `certainty` | 是 | `direct` / `inferred` 两值；T3 与任何机器猜测一律 `inferred` |
| `degraded` | 否 | **降级说明**（如 `quote 截断到 200 字`、`抽取质量有保留：单字行 62%…`）；没降级就**不写这个键** |

`kind` 初始枚举（**键封闭**：扩枚举必须先改本文）：

```text
heading  paragraph  list_item  table  figure  caption  code  sheet  cell
```

### 2.2 稳定 id 规则

```text
<material_id>#<kind 前缀><序号>        例：M03#p012（第 12 段）、M03#t7（第 7 张表）
```

三条硬要求：

1. **可复现**：同材料 + 同插件 + 同选项 ⇒ 同 id。**禁止**随机数、时间戳、不稳的集合迭代序。
2. **只增不改**：材料解析一次落一次；重解析要生成**新账本**并记录原因，不许就地改旧 id（否则 H10 的引用会集体失效）。
3. **顺序 = 文档阅读序**；表格内按 `行,列` 展开。

### 2.3 出处（location）格式

```json
{ "path": "材料路径", "page": 3, "sheet": null, "cell": null,
  "bbox": [x, y, w, h], "quote": "原文摘录（默认截断 200 字）" }
```

`quote` 的截断上限是为了控制账本体积；截断处必须留省略标记，不许无声变短。

### 2.4 写盘契约

- UTF-8、LF、缩进 2、`ensure_ascii=false`（中文不转义）。
- **幂等**：同输入两次 ⇒ 同字节（对齐现有"build 两次产物不变"的纪律）。
  **具名例外**：`extractor=vlm` 的元素**由 AI 识图产出**，同材料两次不会自动同字节（§1.3）——
  账本对**给定输入**仍然是确定的（同两份 JSON 两次同字节），不可复现的是"看图这一跳"这**上游**。
  所以 vlm 元素的 `certainty` 一律 `inferred`；要复现就得把那次识别的产物（那份 JSON）当输入留着。
- **键序固定**：按 §2.1 的字段序，不随解析顺序漂。
- **超上限先侦查、再决定**（§1.5）：处置为「只取摘要」时才缩 `quote` / 丢 `rows` 明细，并写明"哪份材料被降级"—**不许静默截断**，**也不许一上来就全量灌**。

### 2.5 L0 的对外承诺

**下游只许引用 `id`，不许回头读原文件。** 这是 L0 存在的全部意义：解析只做一次，之后的每一次引用都可机器复核。

### 2.6 与现有契约的接缝（一句话，细节留 §6）

现有"逐行回看出处"将从**人肉声明**升级为**可校验引用**：流程表「依据」列 = element id 列表；据此新增 **H10 证据完整性**（每个节点 ≥ 1 条依据；被引用的 id 必须存在；`⚠?` 必须挂在澄清申请上）。

## 3 L1 清点（材料卡片）

**输入**：`evidence.json`（材料层 + 证据层）。**输出**：`intake.md`—一份材料一行，**只标注、不新增真值**（落**成果根**，见 §0）。

| 材料 | 档位 | 主题 | 含流程 | 版本关系 | 读不动 | 依据 |
|---|---|---|---|---|---|---|
| `M07` 资质证明.pdf | T3 | 主体资质证明 | 否（无过程步骤） | 独立 | — | `vlm` 卡片证据 |

字段契约：

| 列 | 取值 | 规矩 |
|---|---|---|
| 材料 | `M##` + 文件名 | `M##` 一律**从账本抄**，不重编号 |
| 档位 | `T1`/`T2`/`T3`/`T4` | **从账本抄，不重判**（重判 = 双份真值，必漂） |
| 主题 | 一句话 | 语义推断 → 标 `⚠` |
| 含流程 | `是` / `否` / `不确定` | 三值封闭。**`是`/`否` 由 AI 判、必须给理由并标 `⚠`**（哪几份材料属于同一条流程是 AI 的职责，不是问用户的）；`不确定` 只留给"两种判法后果不同、且都说得通"，此时才进澄清申请 |
| 版本关系 | `独立` / `重复(M##)` / `互补(M##)` / `替代(M##)` / **`被替代(M##)`** / `无关(M##)` / **`不确定`** | **必须双向一致**，见下。后两个取值是本节的判据表要求、而取值列原先漏了的：`A 替代(B) ⇔ B 被替代(A)`；判不出写 `不确定`（不许猜）。**`—` 不是合法取值**——"没填"与"判不出"必须能分辨 |
| 读不动 | 原因 | 从账本的 `materials[].reason` 抄；空写 `—` |
| 依据 | element id 列表 | H10 核它 |

**版本关系的判据（可执行）**：

| 关系 | 成立条件 | 后果 |
|---|---|---|
| **重复** | 两份材料的 `sha256` 相同（内容逐字节相同） | 只保留一条（取 `M##` 小者），另一条标「副本」；**不许因此多出一条流程** |
| **互补** | 两材料主体/标的相同、内容不重叠冲突，且有「补充 / 附件 / 变更」类词或时间前后相接 | 归入**同一条流程**的材料集 |
| **替代** | 同名同标的、且版本号/日期更新的一份覆盖旧的一份。**日期取值优先级：正文里的签署/生效日期 → 文件名里的日期 → 文件系统 mtime**（mtime 最不可靠：复制一次就变） | 只采用新的；旧的记「未采用」 |
| 判不出 | — | **不确定 → 进澄清申请**（不许猜） |
| **无关** | 同目录但不共享主体/标的 | 各自成卡片，不进同一条流程 |

**判重只看 `sha256`，不看文件名与路径**：同名未必同内容（同名两版很常见），同内容未必同名（`补充协议.doc` 顶层一份、子目录一份）。

**双向一致**：`A 互补(M02)` 与 `M02 互补(M01)` 必须成对；`A 替代(B)` 与 `B 被替代(A)` 必须成对。单向声明按错处理—学 `§`/`parent` 双向一致的教训（H9 那条）。

三条纪律：

1. **清点不改账本**：发现材料缺失、读不动、可疑，只写卡片与澄清申请，**不许回头改 `evidence.json`**。
2. **不产生新真值**：卡片是账本的**标注层**；任何结论都要能指回材料级（`M##`）或片段级（element id）。
3. **顺序 = 材料路径字典序**（与 §0 的 `M01...` 同一口径）。
4. **拆解是 AI 的职责，不是问用户的问题**：哪几份材料属于同一条流程、要不要拆子流程，由 AI 判断并出草稿（标 `⚠`），用户只负责纠正。**澄清申请只装"推不出、且必须业务方拍板"的事**（口径冲突、外部约束），不装 AI 的专业判断。

**谁填哪几格**（`scripts/intake.py`，与 §1.5 的侦查器同一条分工线：**脚本只执行，不承载判断**）：

| 谁 | 格子 | 依据 |
|---|---|---|
| **脚本**（`intake.py build`） | 材料（`M##` + 文件名）· 档位 · 读不动（抄 `materials[].reason`）· **`重复` 关系** | 判重只看 `sha256`，是纯事实；档位/读不动只许抄，不许重判 |
| **AI** | 主题（标 `⚠`）· 含流程（`是`/`否` 标 `⚠` 并给理由）· 互补 / 替代 / 无关 · 依据 | 语义判断，脚本判不出 |

骨架里 AI 那几格写 `—`，`intake.py check` 会把"没填"当错——**"没填"与"填了"必须能分辨**，
否则一张空卡片也能过校验。校验器只查 §8 登记的那两条（**卡片与账本一致**：材料一一对应、档位逐字相等、
读不动逐字等于 `reason`；**版本关系双向一致**）+ 取值封闭 + "推断必须留痕"；
**不查 element id 存不存在**——那是 **H10.1** 的活（§6），在这里再写一份 id 提取就是两份真值。


## 4 L2 计划

**输入**：`intake.md`（材料卡片）+ `evidence.json`。**输出**：`plan.md`—回答**"做几张图、谁是谁的子、谁依赖谁、谁能并行"**（落**成果根**，见 §0）。

**仪器**：`python scripts/plan.py build intake.md -o plan.md [--scope scope.json] [--split split.json]`
出草稿（机器可算的格子已填）·
`python scripts/plan.py check plan.md --intake intake.md [--root 成果根]` 校验收口后的计划。
**草稿一出来，第一件事是填表头那三格（主体 / 目的 / 材料根）——先问用户，再往下拆**（§4.0）；
答复用 `--scope` 传进来。**AI 的拆解决定用 `--split` 传进来**（见 §4.2 末）。
**三张表的表头逐字按 `plan.py` 的 `FLOW_COLUMNS` / `ASK_COLUMNS` / `EXCL_COLUMNS`**（下面三张是它们的抄本，
门⑪ 的 ㊶ 逐字核）。**分工**（与 §3 / §1.5 同一条线：**脚本只执行，不承载判断**）：

| 谁 | 格子 | 为什么是它 |
|---|---|---|
| **脚本**（`plan.py build`） | 候选流程的**种子**（清点里「含流程 = 是」的材料各出一个）· **强合并**（§4.2 步骤 2 的 `重复` / `互补` / `替代` / `被替代` 是 §3 判据表里的纯事实）· `F##` 编号（按材料集里最小的 `M##`）· 排除清单（抄「含流程 = 否」+ 理由）· 澄清申请的**种子**（哪几份材料的版本关系是 `不确定`）· "含 `不确定` 材料 ⇒ 状态 `待澄清`"这条蕴含 | 都是查表得来的事实 |
| **AI** | 流程名 · 角色（`主` / `子`）· `挂在` · 与其它流程（甲 / 乙 / 丙）· 并行组 · 澄清申请的「问题 / 推荐答案」 | 语义判断，脚本判不出（§4.5） |

### 4.0 前置澄清：**主体**、**目的**与**材料根**（先问，再拆）

**这一节排在最前面，因为它是拆解的前提，不是产物的附录。**

```text
主体   = 这几张图覆盖谁（哪一个 / 哪几个法人或合作关系）
目的   = 给谁看、用来干什么（它决定交付粒度：汇报全貌 / SOP 落地 / 查风险缺口）
材料根 = 材料从哪几个目录 / 文件来——**含用户手边的相关工作目录**，不只是"这次递过来的那个文件夹"
```

**为什么它必须是第一步**（2026-09-18 真实材料集实测，不是推演）：那份材料包里，
合作框架协议是「**怡云智 × 珈伟**」之间的（甲方 = 深圳市怡云智科技有限公司），
而模板（EPC 合同 / EMC 合同 / 转租协议 / 预收购框架协议）与标准（技术要求 / 踏勘报告 / 验收标准 /
供应商准入 / 短名单）全都是「**怡亚通新能源**」体系的（验收标准的制定单位写着「怡亚通新能源 GC003」）。
我先顺着材料拆出四条流程，**才发现主体是混的**——其中两条（EMC 合同的对账流程、供应商准入）
根本不在"怡云智 × 珈伟 的合作链"上，得推倒重来。
**§4.2 的六步没有一步不以"主体"为前提**：种子（谁的流程）· 合并（同一主体的材料集才谈得上互补替代）·
命名（按谁的体系命名）· 定关系（甲 / 乙 / 丙 以同一主体为前提）。**目的**同样前置：它决定
"要不要出子流程、节点粗到什么程度"——汇报全貌与 SOP 落地是两种不同的产物。

**落点与机器可核的形式**：答复写进 `plan.md` 表头的**那一行**（§4.1 前面那段），
`plan.py check` 要求**三个字段都非空**——**没填就是没澄清，直接退 1**。

**"材料根"为什么也是前置项**（2026-09-18 实测，同一天第二次现形）：用户递来的是 15 份「合作资料包」，
我就把"材料"等同于那个文件夹；**没问"同一个项目还有没有别的工作目录"**。结果：那份"读不动"的
《补充协议》（`.doc`、OLE、无转换器）**用户早就转述成了 `.md`**，连同《合作框架协议》的文本、
《工商业光伏踏勘施工流程》主干、以及一张已经画好的 `drawio` / `html`——全躺在同一个项目的另一个目录里
（`D:\工作空间\怡亚通流程SOP分析\参考资料\`）。于是我的澄清申请**拿用户已经给过的东西去问他**——
这是最贵的一种问题：**让业务方为重述自己已有的材料付费**。
`recon` / `intake` / `plan` 的输入都是"一份 `materials.json`"，**而"这份 JSON 覆盖了哪些目录"从来没人问过**——
这一格就是那个问题的落点。

**答复是新材料**（§0：澄清答复 = 新材料）：主体、目的或材料根变了的正确处置是**回 §1 重走**
探测 → 解析 → 落账（不改旧账本），不是就地改图。

**"含流程、但不在本次范围"的材料怎么办**：走 §4.1 ③ 的**第二支**（理由写成「`范围外…`」），
不许静默塞进排除清单、也不许为了安放它凭空多出一条流程。

**落表入口（可选的一只手，不是必经路径）**：真实业务的入口常常**已经有一张"准流程表"**
（阶段 / 编号 / 名称 / 类型 / 主体 / 岗位 / 时间 / 下个节点 / 描述），但列数、列名、分支写法都不合契约。
若 AI 手上有这么一张（别人给的 / 自己上一轮写的），可以用
`python scripts/import_table.py 外部表.md -o <流程目录>/flowtable.md [--subject 默认主体]` 把**机械部分**
交给脚本——它只做三件事：**按同义词认列**（长者优先：`节点类型` 胜过 `节点`）· **规格化写法**
（`<br>`/换行 → `｜` · 类型同义词 → 四种 · 缺列留 `—`）· **补 frontmatter 与「主体配色」**。
**没有这张表就照常落表**（§0.1：节点的采集与判断归 AI 自由发挥）。
**语义列（输入 / 依据 / 输出）一律留 `—`**：那是 AI 的活（依据要指到 element id）。
转完照常跑 `table_to_dsl.py --check`——**契约校验才是门**，导入器不是（它只保证"填得进去"）。

### 4.1 plan.md 的字段

**① 流程清单（一个流程一行）**

| 流程 | 角色 | 挂在 | 材料集 | 与其它流程 | 并行组 | 状态 |
|---|---|---|---|---|---|---|
| `F01` 采购申请 | 主 | — | `M02`、`M07` | — | `G1` | 可落表 |
| `F02` 供应商准入 | 主 | — | `M08`、`M09` | 共享(M08) | `G1` | 可落表 |
| `F03` 结算付款 | 子 | `F01#07` | `M04`、`M08` | — | `G2` | 待澄清 |

| 列 | 取值 | 规矩 |
|---|---|---|
| 流程 | `F##` + 流程名 | `F##` 按"材料集里最小的 `M##`"排序（可复现）；**流程名 = 目录名**（允许中文） |
| 角色 | `主` / `子` | 子 = 被父流程的**一个节点展开**（甲） |
| 挂在 | `F0x#节点编号` / — | 只有子流程填；编号必须是**父流程表里的真实节点**（落表后由 H9 双向核；计划层只核"父流程在本表里"） |
| 材料集 | `M##` 列表 | 每项必须在 `intake.md` 里存在（**可机器核**，§4.4 ①） |
| 与其它流程 | `接力(F0y)` / `共享(M##)` / — | 见 4.3。**嵌套（甲）由「角色 / 挂在」两列表达，本列只写乙 / 丙**。`共享(M##)` 的**机器判据**：那个 `M##` 必须出现在**另一条流程的材料集**里（否则"共享"是句空话）；`接力(F##)` 指向的流程必须存在 |
| 并行组 | `G##` / — | 同组 = 互不依赖、可并行；**同组内不许出现 `接力`**（依赖与"可并行"互斥，可机器核）；**本版只记录、不调度**（§7） |
| 状态 | `可落表` / `待澄清` | `待澄清` = 还有 `⚠?` 没答。**两条机器蕴含**：材料集含 `不确定` 的材料 ⇒ 必须 `待澄清`（§3 判据表"判不出 → 进澄清申请，不许猜"）；`待澄清` ⇒ **澄清申请里必须有一条指向它**（否则这个状态是凭空来的） |

**② 澄清申请**（只装"推不出、且必须业务方拍板"的事，见 §3 纪律 4）

| 编号 | 问题 | 推荐答案 | 指向 |
|---|---|---|---|
| `Q1` | 两份同名文件哪份生效？ | 取正文日期较新者 | `M03`/`M04` |

**它不许装 AI 的专业判断**（2026-09-18 修，我自己踩过一次）："要不要拆子流程 / 这条流程该并还是拆 /
这份材料算不算流程" 都是 **AI 的判断**（§0.1 的自由发挥区 + §3 纪律 4），该写进**表头注记或节点描述**
并标 `⚠`，**不许变成问句**去问用户。踩到的样子：真计划里出现过一条「验收节点要不要引《验收标准》
细化成子流程？」——那不是"推不出、必须业务方拍板"，而是"我该自己判、并且能自己判"的事；
它的正确落点是**判断 + `⚠`**，用户看到草稿后纠正（而不是替我做判断）。
**判据只留给真正推不出的**：口径冲突（两份文件哪份生效）· 外部约束（资质 / 审批 / 主体）· 材料缺席
（读不动、缺附件）这三类。

「指向」写**这个问句属于哪份材料（`M##`）/ 哪条流程（`F##`）/ 哪个节点（`F0x#N`）**——
`plan.py check` 会核它存不存在；§6 的 H10.3（"`⚠?` 的节点必须在澄清申请里有对应条目"）
将来就是靠**这一列**做键比对，所以节点级的问题也要能写进来。

**③ 排除清单**（两支：**没流程**的材料，和**有流程但不在本次范围**的材料）

| 材料 | 理由 |
|---|---|
| `M15` | 主体资质证明，无过程步骤 |
| `M02` | 范围外：EMC 合同属「怡亚通」的 EMC 业务模型，不在「怡云智 × 珈伟」的 EPC 合作链上 |

- **第一支**：从 `intake.md` 的"含流程 = 否"**抄，且必须抄全**（漏一个就是漏项，`check` 会报）。
- **第二支**：材料**有流程**、但按 §4.0 的主体 / 目的**不在本次范围内** ⇒ 理由必须**以「`范围外`」开头**
  （机器可核的标记）。这一支是 2026-09-18 补的：原先排除清单只收"含流程 = 否"，
  于是一份"有流程但不该出图"的材料只有两条烂路——塞进流程清单（凭空多一张图）或删掉（静默消失）。

### 4.2 拆解算法：材料 → 几条流程

按顺序跑，**前面判定的结果不许被后面推翻**：

| 步 | 做什么 | 判据 | 判不出怎么办 |
|---|---|---|---|
| 1 | 出种子 | 每份"含流程 = 是"的材料各出一个候选流程 | — |
| 2 | 合并（强） | `重复` / `互补` / `替代` / `被替代`（§3）→ **必并**（同一流程的材料集）。**这一步是脚本做的**（`plan.py build`：关系是清点卡片上的事实） | — |
| 3 | 合并（弱） | 两份材料共享 **起点事件 / 终点事件 / 交付物** 中的 ≥2 个 → 并 | 标 `⚠`，**不并**（保守） |
| 4 | 命名 | 主题短名（≤12 字） | 标 `⚠`，取材料名主干 |
| 5 | 定关系 | 见 4.3（甲 / 乙 / 丙） | 标 `⚠?`，进澄清申请 |
| 6 | 并行分组 | 互不依赖的流程同组 | — |

**保守原则**：**宁可多出一条流程，不可错并两条**—并错会让两张图的材料互相砸染；拆多了只是多一次合并机会。

**AI 的拆解落成一份结构化产物**（`--split` 的 JSON，四段）：`流程`（材料集 / 名 / 角色 / `挂在` /
与其它流程 / 并行组 / 状态）· `范围外`（材料 / 理由）· `澄清`（问题 / 推荐答案 / 指向）·
**`注记`**（AI 判断的落点：`要不要拆子流程 / 该并还是拆 / 算不算流程` 这类**判断**写在这里并标 `⚠`，
**不许写成澄清申请的问句**，见 §4.1 ②）。
**行集合由 AI 定，落笔由脚本做**：`plan.py` 按「材料集里最小的 `M##`」编号 `F##`、按序编号 `Q##`，
并把「含流程 = 否」的材料自动抄进排除清单——**这样"合并 / 移出范围 / 加一条澄清"不再靠手改表格**
（改行集合时列数、编号、双向引用全靠人保证，真材料集上就是这么踩的）。
**`build` 会当场核"决定完整性"**：每一份「含流程 = 是」的材料**必须有下落**（进某条流程的材料集，
或写进「范围外」，后者理由要以「`范围外`」开头）——漏掉的会**静默消失**，一律退 2 且不写盘。

### 4.3 甲 / 乙 / 丙：三对关系怎么判

| 关系 | 判据（可执行） | 后果 |
|---|---|---|
| **甲 嵌套** | B 的**全部外部输入**来自 A 的**同一个节点 N**，且 B 的**全部输出**回到 N 的后继 | B 挂 `F0x#N` → 走 `⊞` 子表；`level` L1、`parent` 指回 A |
| **乙 接力** | A 的**结束事件** = B 的**开始事件**（A 的输出是 B 的输入），且**无回边** | 并列；描述里写"下游 → `F0y`"，**不画跨表边** |
| **丙 共享** | 两者共享同一**输入 / 依据 / 产出物**（同一 element），且互不包含 | 并列；记 `共享(M##)` |
| 都判不出 | — | 标 `⚠?`，进澄清申请 |

**为什么跨表不画边**：现有纪律是"**表自包含**"（跨表引用只写进描述），这样"每个节点恰好属于一张表"才成立，覆盖率与产物审核才判得准。甲 / 乙 / 丙 都不破坏这条。

### 4.4 计划与门禁的关系

- `plan.md` **不进机器门禁**（它没有唯一形态——`check` 只核下面那几条可机器核的，不核"拆得对不对"）。
  但**每一项都要能指回** `intake.md` / `evidence.json`：`材料集` / `指向` 两列的每个 `M##` / `F##` 都可核。
- **可机器核的三条**（`scripts/plan.py check`，门⑪ ㊹ / ㊺ / ㊻ / ㊼）+ **一条前置**（§4.0）：
  **⓪ 表头写了主体与目的**（没填就没澄清，直接退 1）；
  ① 流程清单里的 `M##` 必须都在 `intake.md` 里；
  ② 计划的流程数 = 实际生成的 `<流程名>/` 目录数（**要 `--root`**；不给就**跳过并打印**，不静默）；
  ③ 排除清单**两支**（「含流程 = 否」必须抄全 · 含流程的材料只能靠「`范围外…`」的理由进来）、
  `待澄清` 必须有澄清申请指回、
  **清点里「含流程 = 不确定」的材料必须在澄清申请里有账**（它既不是流程、也不在排除清单里，
  没有这一条就会**静默消失**——真材料集上现形的：5 份读不动的材料一份都没出现在计划里）、
  `共享(M##)` 必须真的被另一条流程共享、同组内不许有 `接力`。
- **改计划的顺序**：用户改了计划 → **先改 `plan.md`，再落流程表**。计划是"做几张"的事实源，流程表是"一张怎么做"的事实源（§0），两者不许互相代替。

### 4.5 拆解是 AI 的职责

`plan.md` 由 AI 产出（流程名 · 角色 · `挂在` · 甲 / 乙 / 丙 · 并行组 · 澄清问题**都算推断**）。
**留痕不靠逐行加 `⚠` 标记，靠两列指针**：`材料集` / `指向` 里的每个 `M##` / `F##` 都必须在
`intake.md` / 本表里核得到（§4.4）——**加一个没人核的 `⚠` 只会让"留痕"变成一种说法**
（§0 责任边界要的是"能复核"，不是"打了标记"）。按 §3 纪律 4：**只有"两种判法后果不同、且都说得通"
才进澄清申请**；"拿不准但两种判法结果一样"不算不确定—直接判。

## 5 循环的发动机：漂移 → 缺口

**这一节管"过程"，其余各节管"工位"。** 真实过程是这样走完的：

```text
读目录树 → 直接读能直接读的 → 对读不了的做「假设」 → 形成对这批材料的整体理解
   → 先落一张流程表（带着假设与 ⚠?）→ 表自己暴露逻辑漂移
   → 漂移定位出上下文缺口 → 按缺口定向「撬开」文件（产物落盘）
   → 子代理读产物、只回摘要 → 据此修表 → 再审逻辑 → 循环
```

三条推论（它们解释了几处看起来"顺序奇怪"的规定）：

| 推论 | 它约束了什么 |
|---|---|
| **流程表是驱动者，侦查是它的子程序**（§1.5） | 先有表（哪怕整张都带假设），表发现问题，才去撬材料——**不是"等材料读干净了再归纳"** |
| **抽取量由缺口宽度决定，不由材料体积决定** | 没有具名消费者的材料**一个字都不必抽**。这条正面回答了 §1.5 开篇那类"能抽几十万字符却没有过程步骤"的工具型文件 |
| **直接读永远排在撬开前面** | 模型自己能读的格式（`.md` / `.csv` / `.txt` / `.json` …）先读；**脚本是给读不动的东西准备的手**，不是必经工位 |

### 5.1 漂移：定义与三个已知源

**漂移 = 表里的一条断言，与"我们已知的东西"不一致。**

"已知"只有三个来源，**没有第四个**：

| 源 | 它知道什么 | 产物 |
|---|---|---|
| **证据账本** | 这份材料读动没有 · 这条路是直取还是视觉推断 · 哪条被降级截断过 | `evidence.json`（§2） |
| **假设账** | 哪些结论是**猜的** · 猜的是什么 · 怎样才算验过 · 有没有被推翻 | `recon.md`（§1.5） |
| **表自身** | 结构（H1—H9 管得着的）+ 语义衔接（管不着的那些） | `flowtable.md` |

一句可执行的判据：**凡"已知它是猜的 / 已知它读不动 / 已知它被推翻 / 已知它是降级或视觉读数"，
而表里当事实用的，就是漂移。** 反过来说：**漂移的主因通常不是"材料不够"，而是"猜的忘了自己是猜的"**——
所以假设账不落盘，漂移就查不出来。

### 5.2 判据表：D1—D5

**五条全是硬档**：集合 / 枚举比对，机器确凿，可当结论。**这一版刻意没有软档判据**——
试过一条，实测精度太低，按"宁可漏判，不可误判"整条砍掉（理由与数字见本节末）。

| 号 | 判据 | 怎么机器判 | 它在防什么 |
|---|---|---|---|
| **D1** | **等级拔高**：依据指向的证据 `certainty=inferred`（`extractor=vlm`）或带 `degraded`，而节点没有 `⚠` 留痕 | 账本按 id 查 `certainty` / `degraded`，表里查 `⚠`。**`degraded` 要分类**：只认"证据被削弱"的那几类（质量门 / 收窄 / 转换），**`quote 截断` 不算**——那是 §2.3 的浏览摘录上限，正文与逐字引用都不受影响 | 把"我看到的 / 被截断的"当成"文件里逐字写着的"（§1.3 / §2.3 的纪律，此前**没有仪器**） |
| **D2** | **假设被推翻仍在用**：`recon.md` 里该假设 `状态 = 已推翻`，而表里仍有节点依据它 | 按 `M##` / 假设角色比对 | 表没跟着新认知走 |
| **D3** | **建立在读不动的材料上**：节点依据里的 `M##` 在账本里 `status = unreadable`（含质量门判 `garbled`） | 集合比对 | 这条断言的来源根本不存在 |
| **D4** | **含流程的材料零引用**：`intake.md` 里「含流程 = 是」的材料，表里没有任何节点引用它的 element | 集合比对 | 这份材料的信息**没进表**（漏读 / 读了没落表） |
| **D5** | **读了没用上**：某材料的 element 数 ≥ `coverage_min_elements`，而被表引用的比例 < `coverage_min_ratio` | 账本按 `material_id` 计数 + 表里的引用集合 | 这份材料**只被读了个开头**（或读了没落表） |

**出漂移还是出缺口**：D1—D3 是"**表写错了**"（→ 漂移清单）；D4 / D5 是"**还有东西没看**"
（→ 缺口清单）。后者不指责表，它**派活**——这正是 §5.3 那张表存在的理由。

**启用条件按输入可用性分档**（与 §6 的 H10 同一条纪律：**没有输入就不启用，绝不误伤合法的旧表**）：

| 判据 | 要什么输入 | 缺了怎么办 |
|---|---|---|
| D1 / D3 | 证据账本 | 跳过（不报错） |
| D2 | 证据账本 + 假设账 | 跳过 |
| D4 | 证据账本 + `intake.md` | 跳过 |
| D5 | 证据账本 + 流程表 + `drift:` 阈值 | 跳过 |

**D5 为什么要阈值**：覆盖比例是"读了多少用上多少"的读数，**不能用"比例低就是漂移"一刀切**——
一份 300 条元素的合订本，表里引 5 条可能正好够（该材料只有 5 处与流程有关）。所以它出的是**缺口**
（"这份材料还有 295 条没看，要不要看由 AI 判"），不是"表写错了"。阈值住 `dictionary.yaml` 的 `drift:` 段。

**为什么砍掉「输入输出接不上」（2026-09-18 实测，别再提它）**：原判据是"相邻两节点之间，
前驱「输出」与后继「输入」没有任何公共词"。在 `examples/workflow/flowtable.md` 上实测：
**14 条边报了 12 条**，逐条看过 **11 条是误报**，只有两类成因：

1. **回边**（`09→08`、`04→03`）：判断或自检发现问题就回上一环——前驱的产物本来就不是后继的输入；
2. **只是"顺序上的下一步"**（`01a→02`）：表里的 `下个节点` 表达的是**时序**，不是数据流，
   两列的措辞本来就不该有交集。

根因是**前提错了**：`输入` / `输出` 描述的是**单个节点自己的进出**，不是**一条边上的交接**；
拿它们比一条边，等于先假设"表里的边都是数据流"，而本仓的表不是这么建的。
**8% 精度的提示会让人去改 11 条本来对的表**，所以整条砍掉（登记在 `coding-spec` G14）。
真要判"中间少了一个环节"，得先让表能表达"**这条边传的是什么**"——那是字段契约的改动，不在本轮。

### 5.3 缺口清单：循环的燃料

**漂移说"哪里有毛病"，缺口说"缺什么、去哪找、怎么找"。** 后者才是 LLM 行动的依据。

脚本给**机器可判的那半**（哪份材料 · 它现在的状态 · 已抽几条 · 走哪条路）；
**"去要哪一片、值不值得要"是 AI 的判断**。

| 缺口 | 触发 | 需要哪份材料 | 要哪一片（AI 填） | 走哪条路 | 状态（AI 填） | 说明（AI 填） |
|---|---|---|---|---|---|---|
| `Q01` | D4 含流程的材料零引用：表里没有任何节点引用它的证据 | `M04` | `lines=1-40`（第 1–40 条元素） | 直读（py:docx） | 已取证 | 取证到 3 个审批节点，已补进表 |

**表头逐字按 `drift.py` 的 `GAP_COLUMNS`（7 列）**——列规范在代码里只有一份，本文这份是它的抄本，
夹具 ㊶ 会核"本文里逐字找得到 `GAP_COLUMNS` / `DRIFT_COLUMNS`"。
（原先这里只有 6 列、列名也各自带注解，按它写出来的 `drift.md` 会被 `check` 判"表头不对"退 2。）

- **缺口的来源只有两类机器判据 + 一类 AI 反推**（2026-09-18 修：原先写"三类来源：漏读 / 猜的没验 /
  读了没用上"，与 §5.2 的"**D1—D3 出漂移、D4 / D5 出缺口**"直接矛盾——"猜的没验"就是 D2 / D3，
  它们是**漂移**，不是缺口）：
  ① **漏读**（D4：含流程却零引用）· ② **读了没用上**（D5：元素够多、被引用比例过低）·
  ③ **AI 反推的上下文缺口**（§1.5 第 6 步："必然存在、但材料里没有"的东西）——
  ③ 里"**必须业务方拍板**"的那一支（§3 纪律 4 / §4.1②）就是**澄清申请**，不另立第四类账。
- **`要哪一片` 由 AI 填**（页 / 表 / 行区间 / 关键词）：这就是"渐进式批量"的抓手——
  **点名一小片，而不是把整份材料灌进上下文**。**取那一片的手是 `scripts/query.py`**：

      python scripts/query.py evidence.json --material M06 --grep 审批 --batch 5 --skip 10
      python scripts/query.py evidence.json --range pages=40-60 --range rows=100-200

  **它只查不抽**：在**已经落盘**的账本上按「哪份材料 / 哪一片 / 含哪个词」取一小批，回一小批 + **下一批的游标**。
  为什么不顺手把"抽取时收窄"也做了：那是两件事——收窄抽取省的是**解析代价**，本工具省的是**上下文窗口**；
  账本再大也躺在盘上，真正稀缺的是"这次给模型看几条"。所以**先能点名，再谈抽取时收窄**。
  坐标语法（`--range`，可重复）：`pages=A-B`（`location.page`，1 起）· `lines=A-B`（该材料内第 N 条元素，通用兜底）·
  `sheet=名`（xlsx 子表）· `rows=A-B`（**只裁显示**，表格类证据只打这几行——一张 10 万行的测算表是一条证据，
  不裁行等于把整张表倒进上下文）。`--grep` 是**朴素子串匹配**（正文 / 表格单元格 / 出处摘录），
  **不做分词、不做模糊**：判语义是 AI 的事（§0 责任边界）。
- **`走哪条路` 从 `recon.md` 抄，不另立一份**（§1.5"两列分开"已经算过这件事，重算是双份真值）。
- `状态 ∈ {待取证, 已取证, 已放弃}`；**`已放弃` 必须写理由**（不许静默放弃）。
- **澄清申请（§3 纪律 4 / §4.1②）是缺口清单里"必须业务方拍板"的那一类**，不另立第四类账。

### 5.4 三本账与收敛

"假设必须落盘"（§1.5）只是三本里的第一本。循环要能复现、要能不重复付费，三本都得留痕：

| 账 | 记什么 | 落点 |
|---|---|---|
| **假设账** | 哪些是猜的 · 猜什么 · 怎样才算验过 · 被推翻写成"曾记 X → 现记 Y" | `recon.md`（已有） |
| **证据账** | 撬开的产物与出处 · 等级 · 降级留痕 | `evidence.json`（已有） |
| **漂移账** | 每条漂移：发现 → 处置 → 依据 | `drift.md`（本节产物） |

**漂移账的表列**（同一份 `drift.md` 里的两张表之一，另一张是 §5.3 的缺口清单）——
表头逐字按 `drift.py` 的 `DRIFT_COLUMNS`（6 列），`check` 认的就是这六个名字：

| 漂移 | 判据 | 位置 | 事实 | 处置（AI 填） | 依据（AI 填） |
|---|---|---|---|---|---|
| `X01` | D1 等级拔高 | 节点 02 | `M01#p001` 带降级留痕（quote 截断到 200 字） | 已解释 | 截断不影响该节点引用的那半句 |

前四列（`漂移` 编号 / `判据` / `位置` / `事实`）由 `build` 填，后两列留给 AI。
**对账键是 `(判据, 位置, 事实)` 三元**——事实变了（同一节点换了成因）旧账就不再覆盖，
这种"改写成因再复用旧结论"的路走过一次（审计实测），所以键里必须有事实。

**收敛判据**（口径只写在这里；§1.5 第 8 步引用它）：

```text
收敛 = 硬漂移 0 条  且  缺口清单里没有「待取证」（每条都已取证，或已放弃且写了理由）
                    且  计划里没有 `待澄清` 的流程（澄清申请里每一条都有结论）
```

**三条各自谁在读数**：`drift.py check` 管前两条（它自己就是那本账）；**第三条由 `plan.py check` 打印**
（`✓ … `待澄清` 都有账（当前 N 条）`）——**读数有仪器，阈值应用没有硬拦**：计划里还有 `待澄清` 是
**过程状态**，不是错误（要不要接着问、问到什么时候，是 AI 的判断，§4.5）。

**这条"澄清申请"的来历**（2026-09-18，写清楚免得下次又摘一遍）：原先的口径是
"缺口清单空（澄清申请空 + 无取证缺口）"，两处都不对——① 缺口清单**合法的空法有两种**：
`已取证` 与 `已放弃`（§5.3 准许放弃，只要求写理由），放弃掉的缺口仍然列在表上，所以"清单空"过严；
② 澄清申请住在 `plan.md`（§4.1②），而当时 §4 **没实现**，把它并进收敛口径等于写一条谁都不能核的判据
（§0 声明不许比射程宽）——所以**先按"有仪器的那半"写**，另一半登记在 `coding-spec` G14。
**§4 落地之后它回来了**，而且换了写法：不是"澄清申请这张表是空的"，而是
"**计划里 `待澄清` 的流程 = 0**"——因为问句答完之后，终态是**流程状态翻成 `可落表`**（表本身会留着
那份问答记录，不该被清空）。

**预算**（防"越修越乱"）：

- **每份材料攻坚上限 2 轮**（§1.5 第 8 步）；
- **每一轮硬漂移数必须下降**：连续两轮不降就**停手**，把当前表 + 未决项交用户并标 `⚠?`——**不许无限重试**。

**漂移账的状态列（封闭）**：`已修`（改了表，重跑后该条消失）· `已解释`（判定不是漂移，**必须写理由**）·
`待验`（还没处理）——**收敛时不许有 `待验`**。

### 5.5 谁执行

| 谁 | 干什么 |
|---|---|
| 脚本 `scripts/drift.py` | 跑 D1—D5 · 生成缺口清单骨架 · 表头打印输入指纹、跳过的判据与阈值 |
| **AI** | 决定缺口补不补、**要哪一片** · 改成什么 · 修完复查（再跑 `check`） |
| **子代理** | 读撬开后落在盘上的产物，**只回摘要 + element id**——原文不进主上下文 |

**子代理摘要的三条纪律**（它是新的失真入口，别当成免费）：

1. **每个论断要能指回 element id**——否则摘要成为新的不可复现源（与 §2.4 的 vlm 具名例外同类）；
2. **只回摘要，不回原文**——原文在盘上，要复核按 id 回查；
3. **摘要搬运的是证据**：它引用的 id 照 D1 判等级，视觉来源照旧标 `⚠`。

**产物落点**：`drift.md` 落**成果根**，名字登记在 `artifact.NON_TABLE_MD`（否则被判"孤儿表"，§8.2）。

## 6 与现有契约的接缝：H10 证据完整性

**H10 是 L0 与现有结构校验（H1—H9）之间唯一的桥。**

**启用条件（最要紧的一条）**：**这次任务有账本**（`evidence.json`）时才启用。纯口头需求、示例表、自举树都没有账本 ⇒ **H10 跳过、不报错**。这条保证"新判据不误伤合法的旧表"。

| 判据 | 内容 | 为什么它可机器判 |
|---|---|---|
| H10.1 引用完整 | **任何产物**（流程表 / `intake.md` / `plan.md`）里出现的每个 element id 都必须在账本里存在 | id 是封闭语法（§2.2），可正则提取 + 集合比对 |
| H10.2 推断有据 | 描述以 `⚠`（推断留痕）开头的节点，「依据」列必须非空 | `⚠?`（待裁决）**不在此列**：它本来就"推不出" |
| H10.3 待裁有账 | 标 `⚠?` 的节点必须在**澄清申请**里有对应条目（键 = 节点编号） | 申请是一维表，键比对 |

**H10 不要求「依据」必须是 element id**：用户完全可能凭口头需求落表，那时依据是散文。它只保证"**引用了就必须存在**"。

落地位置：`scripts/flowtable_check.py` 的三层校验内（与 H1—H9 同一套报错格式）。
启用前必须先在现有全部表上跑一遍确认 0 误伤——这是 H 系列历来的规矩。

## 7 不做什么（YAGNI 清单）

**这一节的作用是"下个会话别重新提议"。**

| 不做 | 为什么 |
|---|---|
| **这一版不实现并行调度** | 需求要的是"接口留得出来"（脚本=接口、可插拔、可并发），不是"现在就并发"。真实用例 15 份材料 / 1—3 条流程；按 YAGNI：先把**任务单元**切干净（每流程一棵产物树、零共享状态），调度器等真遇到"一目录几十条流程"再上 |
| 不从事件日志"挖"流程 | 输入是非结构化文档，没有事件日志；流程挖掘只能借它的**校验判据**（可达性 / 死锁 / 活锁），不能借挖掘算法 |
| 不再讨论"要不要封闭的格式清单" | 已定：开放式，由插件注册表派生（§1） |

## 8 本期新规则由谁拦

| 规则 | 仪器 | 现状 |
|---|---|---|
| H10 证据完整性（§6） | `scripts/flowtable_check.py` 三层校验内 | 判据已定，**未实现**（上线清单见 §8.1） |
| 漂移判据 D1—D5（§5.2）：等级拔高 · 假设被推翻仍在用 · 建立在读不动的材料上 · 含流程材料零引用 · 读了没用上 | `scripts/drift.py`（判据）+ `dictionary.yaml`（阈值）+ **门⑪**（`dev/tools/pipeline-fixtures/suite.py`） | **已实现并进验收**（`build` 出草稿 + `check` 查收敛：文件里标 `已修` 的必须真不命中、现在命中的必须都在账内）；夹具 = 每条判据正反各一例 + **13 条对账路径**（①—⑨b / ㉟ / ㊱ / ㊲）+ ⓪b（**D1 的两类降级要分开**：只有 `quote 截断` 的不算等级拔高——2026-09-18 真材料集上实测：不分时一份规矩引条款的表被报成 23 条"拔高"） |
| 缺口清单与漂移账（§5.3 / §5.4）：状态封闭 · `已解释` / `已放弃` 必须写理由 · 收敛时不许有 `待验` / `待取证` | `scripts/drift.py check` + `artifact.NON_TABLE_MD`（产物名登记）+ **门⑪** | **已实现并进验收**（③ 收敛 · ④ 标「已修」却仍命中 · ⑤ ⑥ ⑦ 缺理由 · ⑧ 漏了现在的命中 · ⑨b 账与表对不上；`已修` 由**重跑判据**验证，不是靠声明） |
| 子代理摘要三条纪律（§5.5）：每个论断能指回 element id · 只回摘要不回原文 · 引用按 D1 判等级 | 无（AI / 子代理侧，与 G4 同类） | **无仪器**——它是新的失真入口，登记在 G14 |
| 账本 schema：键封闭 / 枚举合法 / id 唯一 | `scripts/ledger.py` 写入前自检 | **已实现**（不过就不落盘，退 1）；**正例进了门⑪**（夹具的账本由 `ledger.py` 写出，退 0 才算过），**拒绝分支仍无人跑** |
| 探测记账完整：每份材料一行、无"未判" | `scripts/probe.py` 一律给档位 | **已实现并进验收**（⑳ 合成树 20 份都有档位 + `kind` · ㉑ `T1 ⇒ 有 reader` 的不变式 · ㉞ 头文本尾二进制的 `.txt` 判 T4 而大中文 txt 仍 T1 · ㊳ 判不出时的 `reason` **可执行**——本地编码的纯文本给 `--encoding gbk` 这条出路） |
| pptx 零依赖读取（§1.4）：probe 判 T1 ⇔ `parse_ooxml` 认领 · 一张幻灯片一个 element + `location.page` 就是页码 · **纯图片页丢掉并记账**（空元素不是证据）· 摘要与读者**同一句判据**（公共层 `pptx_text`） | `scripts/pptx_text.py`（公共层）+ `parse_ooxml.parse_pptx`（读者）+ `recon._pptx_scale`（摘要）+ **门⑪** | **已实现并进验收**（⑰/⑱/⑲/⑲b 四条路径 + 真样本复验：19 张的真稿里 3 张纯图片 ⇒ 16 条元素、**0 条空元素**，每条挂"那几页要视觉"的留痕；摘要里也点明"3 张无文字"） |
| 解析器四条硬要求（只读 · 不抛裸异常 · 输出符合 §2 · 缺依赖报可执行错） | **门⑪**（部分） | **部分进验收**：整链不崩 + 坏材料逐份记账（㉒）· 阈值 / 材料层形状坏不裸栈（㉛ / ㉜）· 产物出 `ledger.py` 过键封闭（退 0 才算过）；**「只读」（不改材料）与「缺依赖报可执行错」仍无仪器** |
| 材料层记账完整：`status` / `reason` / `extractor` 由解析阶段补注写入（§1.4） | `scripts/ledger.py --notes` | **已实现并进验收**（㉕：「读不动」不许被收窄改写成「未取」· ㉔b：缺转换器的 `reason` 逐字进账本）；**补注冲突 / 键封闭的拒绝分支仍无人跑** |
| 分派是查表（§1.4）：固定顺序跑适配器 · 同输入同字节 · 冲突与漏认不许静默 | `scripts/parse.py` | **已实现并进验收**（㉒ 整链退 0 + 坏材料逐份记账 · ㉕ 收窄留痕 · ㉖ 范围下沉到适配器 · ㉙ 范围写歪 / 点名不存在 ⇒ 退 2 · ㉚ 收窄读空记 `skipped` 而不是假报「探测说谎」）；**「id 重复 / 补注冲突 / `status=ok` 却零证据 ⇒ 退 1 不落盘」三条仍无人跑** |
| 抽取质量门（§1.5 手段 0）：碎片 / 坏字符主导的抽取**不许当证据入账** | `scripts/textquality.py`（判据）+ `scripts/parse.py`（执行）+ `dictionary.yaml`（阈值） | **已实现**（三级判决；真数据实测 M15 单字行 62% → `noisy`：丢 10 条纯碎片、留 6 条逐条挂 `degraded`；干净材料零误判；样本 < `min_lines` 不判）；**未进验收路径**——合成树里没有"碎片主导"的样本 |
| 纯文本不猜编码（§1.4）：只认 UTF-8 / 带 BOM 的 UTF-16；GBK 等要显式 `--encoding` | `scripts/probe.py`（记读不动 + 给出路）+ `scripts/parse_text.py`（读）+ **门⑪** | **已实现并进验收**（㊳：GBK 材料 `status=unreadable` 且 `reason` 里就有 `--encoding gbk` 这条出路（2026-09-18 修：原先只写"魔数不认识，且不是文本"，是判据不是出路，§1.3 要求 `unreadable` 的原因可执行）；显式 `--encoding gbk` 读出来并把 `status` 改回 `ok`；同一批里的 UTF-8 材料不受影响——实测修掉"一份 GBK 把整批拖成读不动"） |
| 文本档只记账、不认结构（§1.4）：**理解归直读的模型**，脚本只保证"可引用 / 可复现 / 编码与上限" | `scripts/parse_text.py` | **已实现**（按行打块：行数守恒；块超上限**先收后放**，单行比块长才截断并记 `degraded`——实测第一版"先攒后收"会把整行吃掉还标假降级）；**未进验收路径**（夹具只跑到"整链不崩"） |
| legacy 只走外部转换器：判据 `--version` 能通；缺了就记读不动 + 可执行提示，**不许假装能读** | `scripts/parse_legacy.py` | **已实现并进验收**（㉔b：Word / Excel / PowerPoint **三族各一份**合成 OLE ⇒ 提示逐族指名"另存为什么"）；转换分支用**替身转换器**验过——LibreOffice 自身的转换保真度不在射程内 |
| 转图片 → 视觉（§1.5 手段 2）：T3 / 质量门不过的材料，渲成图交给会看图的模型 | `scripts/render_pages.py`（渲染）+ `scripts/render_pages.py check`（校验 AI 填的骨架） | **已实现**（PDF 自包含渲染实测 0.3~1.9 秒/页；图片材料直接读原文件；Office 系不做、按不可行记账）；**未进验收路径**（渲染要 `pdfplumber`+`pypdfium2` 同时在场，夹具只测到"不参与"那一侧） |
| vlm 证据通道（§1.3）：视觉读数要带 `bbox` + 摘录、`extractor=vlm`、`certainty=inferred` 进账本 | `scripts/render_pages.py check`（写补注）+ `scripts/ledger.py`（`--elements` 可重复、**`vlm` ⇒ `inferred` 机器拦**） | **已实现**（实测：扫描件 M13 4 页 → 7 条 vlm 证据（第 4 页空白、按纪律删掉那条），材料层 `unreadable → ok` 且**覆盖被打印出来**；账本 1165 条 = 机器 1158 + 视觉 7）；**未进验收路径** |
| AI 格只写 JSON，不自排版（§0）：`build --todo` 出待填清单 → `cells.py fill` 按**列名**回写 | `scripts/cells.py`（公共层）+ 四个模块的 `--todo` + **门⑪** | **已实现并进验收**（`recon` / `intake` / `drift` 三个产物的填充在夹具里**全部走这条路**；键错 / 列名错 / 空值三类反例都要**不写盘**）。**`plan.md` 另有两条**：表头三格走 `--scope`、行集合走 `--split`（AI 的拆解落成 JSON，`build` 当场核"每份材料都有下落"，漏一份退 2 不写盘；门⑪ ㊽） |
| 幂等：同输入两次同字节 | 待定 | **未实现**（本轮人工实测：`probe → 三个适配器 → ledger` 两次同字节；`vlm` 元素的**具名例外**见 §2.4） |
| 计划引用完整：`plan.md` 的 `M##` 都在 `intake.md` 里 | `scripts/plan.py check` + **门⑪** | **已实现并进验收**（㊹ 正例 · ㊺ 反例九连：造材料 / 不确定不标 / 排除漏项 / 共享空话 / 接力撞并行组 / 组名歪 / 接力悬空 / 挂在悬空 / 表头坏 · ㊼ **「含流程 = 不确定」的材料必须有账**——真材料集上现形的那条：5 份读不动的材料既不是流程、也不在排除清单里，计划里一份都没出现） |
| 计划与实际一致：流程数 = `<流程名>/` 目录数；`F0x#N` 的 N 是父表真实节点 | `scripts/plan.py check --root` + **门⑪** | **已实现并进验收**（㊻：4 条流程 / 3 个目录 ⇒ 退 1 并点出缺哪条；补齐 ⇒ 退 0）。`F0x#N` 的**节点**那一半仍要落表后才存在 ⇒ 留给 H9 / H10（§6），计划层只核"父流程在本表里" |
| 计划骨架：机器可算的格子先填（种子 / 强合并 / `F##` 排序 / 排除清单 / 澄清种子 / "含 `不确定` ⇒ `待澄清`"） | `scripts/plan.py build` + **门⑪** | **已实现并进验收**（㊸；§4.2 步骤 1—2 是脚本做的，步骤 3—6 留给 AI） |
| 落表入口（**可选的一只手**）：外部节点表 → 契约流程表（同义词认列 · `<br>`→`｜` · 类型归一 · 缺列留 `—` · 补 frontmatter 与主体配色） | `scripts/import_table.py` + **门⑪**（㊾ / ㊿ / ㊿b） | **已实现并进验收**（夹具：一张 9 列外部表 → 12 列契约、**产物过 H1—H9**；反例：编号重复 / 表头认不出 ⇒ 退 1 不产出坏表）。**真样本对账**：用它转用户那张 43 行节点表，与手写版**逐节点一致**，差异只在"AI 该做的那三件"（精简过长分支标签 · 补 `行动所需时间` · 补 `依据`）。**它不是必经路径**（§0.1）：没有现成的准流程表就直接落表 |
| 前置澄清（§4.0）：**主体**、**目的**与**材料根**必须在拆解之前问清，答复落 `plan.md` 表头 | `scripts/plan.py check`（表头三格非空）+ **门⑪** | **已实现并进验收**（㊺ 里"表头没澄清"是反例之一）。**这两条是 2026-09-18 补的**：① 真实材料集上，我先拆出四条流程才发现材料跨两个主体（合作框架是「怡云智 × 珈伟」的，模板与标准是「怡亚通」的），两条流程要推倒——**§4.2 的六步没有一步不以"主体"为前提**；② 同一天又发现我把"材料"等同于用户递来的那一个文件夹，**没问同一个项目还有没有别的工作目录**，于是把用户早已转述成 `.md` 的《补充协议》当成"读不动"记进了澄清申请 |
| 版本关系双向一致：`A 互补(B)` → `B 互补(A)`；`A 替代(B)` → `B 被替代(A)` | `scripts/intake.py check` + **门⑪** | **已实现并进验收**（㊵：断掉双向一致 ⇒ 退 1；另查 `重复` 与 `sha256` 是否对得上、副本必须是编号更大那份） |
| 卡片与账本一致：档位 / 读不动必须逐字等于 `materials[]` | `scripts/intake.py check` + **门⑪** | **已实现并进验收**（㊴ 正例退 0 · ㊵ 改「档位」/ 少一行 ⇒ 退 1；材料一一对应 / 读不动逐字，`｜` 转义按同口径比对） |
| 清点骨架：机器可算的格子先填（材料 / 档位 / 读不动 / `重复`），语义格子留 `—` 给 AI | `scripts/intake.py build` + **门⑪** | **已实现并进验收**（夹具自己那份 `intake.md` 就是 `build` 出来的骨架 + AI 四列，㊴ 让它同时过 `check` **且**让 D4 仍命中；另附**材料摘要**供 AI 填「主题 / 依据」） |
| 侦查（§1.5）：异常材料必须先出侦查结论，再定解析深度 | `scripts/recon.py`（**自包含优先**）+ **门⑪** | **机器可算部分已实现并进验收**（㉓ 合成树 20 行每行都有交代 · ㉔ / ㉘ 护栏按"要读的部件"算且记在行里 · ㉛ 阈值形状坏 / ≤0 退回默认 · ㉜ 材料层形状坏退 2 说人话 · ㉝ `check` 用同一批函数**重算脚本列再比**、重复行退 2）；"假设角色 / 依据 / 验证方式 / 状态"是 AI 侧 |
| 抽取时收窄（§1.5 / §5.3 的执行面）：`--only` / `--pages` / `--slides` / `--sheet` / `--rows` / `--grep` / `--lines` · 收窄一律留痕（元素 `degraded` / 整份滤空记 `skipped`） | `scripts/parse.py`（分派 + 收窄）+ 三个适配器的范围开关 + **门⑪** | **已实现并进验收**（㉕ `--only+--grep` 只留该留的、其余记「本轮未取」· ㉖ `--sheet+--rows` 只回第 2 行且带降级 · ㉚ 收窄读空记 `skipped`） |
| 材料链判据的仪器（G13）：探测不说谎（T1 ⇒ 有人认领）· 改名件/半容器按内容判 · 坏材料逐份记账、整批不崩 · 缩样每种 kind 都有交代 · 护栏记在行里 | `dev/tools/pipeline-fixtures/suite.py` 的合成材料树（**20 份**）+ **门⑪** | **已实现并进验收**（真造 20 份材料：真 docx/xlsx/pptx/PNG · 改名 `.et`/`.png` · 半容器 · 垃圾部件 · 流式无 dimension · GBK · 空文件 · **三族各一份**假 OLE（`.doc` / `.wps` / `.dps` / `.xls`）· 有/无 `/Font` 的 PDF） |
| 列规范同源：§1.5 / §3 / §5.3 / §5.4 的表头逐字等于仪器列规范（`recon.CARD_COLUMNS` / `intake.COLUMNS` / `drift.GAP_COLUMNS` / `drift.DRIFT_COLUMNS`） | `dev/tools/pipeline-fixtures/suite.py`（㊶）+ **门⑪** | **已实现并进验收**（本文漂过两处：§5.3 的缺口表只有 6 列、§1.5 的示例表只有 6 列，按它们写出来的产物会被 `check` 退 2；现在逐字对齐并由 ㊶ 钉住） |
| 依赖清单与自包含自检：新依赖 / 可选外部工具要显式声明，缺依赖报可执行的错 | `requirements.txt` + 适配器缺依赖分支 | **清单已显式声明**；**缺依赖分支与启动自检无仪器**（夹具不拆环境：`pdfplumber` / `pypdfium2` 在场就跑得到，缺了那几条路径没人跑） |
| 收敛口径里的"澄清申请"（§5.4） | `scripts/plan.py check` + **门⑪** | **已实现**（2026-09-18：L2 落地后它回来了，但换了写法——不是"澄清申请那张表是空的"，而是"**计划里 `待澄清` 的流程 = 0**"；`check` 打印当前条数，并且**每个 `待澄清` 都必须有账指回**（㊹ / ㊺）） |
| **文本层"薄"的探测**（每页元素数 ≈ 1 且每页字数远低 ⇒ 正文多半在图/矢量层里）。**与质量门是两种病**：质量门管"抽出来是碎片"，这条管"抽出来太少"。实测（2026-09-18）：808 KB / 11 页的质保书只抽出 11 条元素、2.3 MB / 4 页的规格书只抽出 2 条，而两者的单字行占比与平均行长都正常 | 待定（`probe` / `parse_pdf` + `dictionary.yaml` 阈值） | **未实现**（登记在 `coding-spec` G18；触发动作是"要不要走 §1.5 手段 2 转图片"） |
| 点名取子集（§5.3）：只查不抽 · 一批一小口 + 游标 · 坐标语法一种写法 · 语法错不猜 | `scripts/query.py`（+ `dictionary.yaml` 的 `query:` 段）+ **门⑪** | **已实现并进验收**（真账本实测：1165 条里 `--grep 审批` 3 条带出处、扫描件按 `pages=2-3` 取到 vlm 证据；夹具 8 条路径：⑩—⑯ + ㊷ 的 `pages=` / `sheet=` 正例）；**边界仍未收**：`--batch 0` / `--chars 0` 静默取默认、多 `rows=` 只用第一个 |
纪律：**现在没有仪器 ⇒ 先按 `dev/coding-spec.md` 第三节登记为缺口**，实现时一起接上，不许假装被守住。

### 8.1 H10 上线清单（实现 H10 时照单执行）

1. **改全仓写死的 H 范围**—`dev/verify/contract.py` 有一条**硬断言**（"flowtable-spec 里 H1—H9 齐全"，比的是 `list('123456789')`），**补了 H10 它会当场判红**。要同步的地方：`references/flowtable-spec.md` 的 H 列表 · `dev/verify/contract.py` 那条断言 · `dev/tools/accept.py` 的门② 标题 · `dev/tools/README.md` 的门表 · `dev/tools/layering.py` 的注释 · `dev/tools/equiv-fixtures/broken.md` 的用例描述。
2. **误伤回归**：在现有**全部表**（自举树 + `examples/` 的样例；张数现取，别手抄）上跑 `scripts/table_to_dsl.py --check`，要求 **0 条 hard 增量**。

### 8.2 已知的代码接缝（实现 L0 / L1 时必改）

1. **任务级产物会被判"孤儿表"**：`scripts/layer_index.py` 的 `_find_orphans` 扫 `root.rglob('*.md')`，
   只放行四类——可达集里的表 · `artifact.NON_TABLE_MD` 里的名字 · `<stem>-index.md` ·
   `README*` 与 `*.sync.md`；其余一律报 `孤儿表: xxx.md`。**白名单只有一处**
   （`artifact.NON_TABLE_MD`，现含 `checklist.md` / `intake.md` / `plan.md` / `recon.md` / `drift.md`）：
   新增一个任务级产物名就改那一处。
   （2026-09-18 修：本条原先写成"加 `drift.md` 时改那一处即可"，读起来像**还没做**，而
   `drift.md` 早已登记；这个名单纯靠"改的时候记得"是不牢的，所以本文只保留"唯一出处"这一句。）
2. **父表链扫描会读它们**：`scripts/flowtable.py` 的 `_parent_by_scan` 沿目录向上 `glob('*.md')` 找 `⊞` 父表。
   **同一条白名单已经在用**（`if parent_ft.name in NON_TABLE_MD: continue`）——所以 `plan.md` 里
   出现 `⊞` 字符也不会被误判成父表；剩下的是纪律：任务级产物**不要去写 `⊞`**。
3. **修法（不许各写一份）**：这类"不是表的 `.md`"集中登记一处（按既有纪律：命名只走
   `scripts/artifact.py`），`layer_index` / `flowtable` 从那里取白名单——**这一条已落地**
   （两处都改成 `from artifact import NON_TABLE_MD`），本节只作为"以后新增产物名该改哪"的索引。
4. **宿主 Office SDK 不许当唯一路径**：它只是**加速器**（本 SKILL 必须自包含）。
   ~~实测它没有 export / render / 截图类工具 ⇒ §1.5 的"视觉侦查"目前没有实现手段~~
   ——**这条已修正**（实现 `render_pages.py` 时实测）：**PDF 有自包含渲染能力**（`pdfplumber` 的
   `page.to_image`，底层 `pypdfium2` 是它声明的依赖，无外部二进制）；**图片材料本来就是图**。
   真正没有的只有 **Office 系**（docx/xlsx/pptx → 图）——那要 `soffice` 之类的转换器，按"不可行 +
   进澄清"记账，或先人工转 PDF。
