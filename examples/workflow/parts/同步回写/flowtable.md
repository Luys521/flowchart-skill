---
id: 同步回写
level: L1
parent: ../../flowtable.md
description: 同步闭环：读回 → 回写 → 重渲染
---

# 同步闭环：读回 → 回写 → 重渲染

## 流程表

| 项目运作阶段 | 节点编号 | 节点名称 | 节点类型 | 输入 | 依据 | 输出 | 执行主体 | 执行者 | 行动所需时间 | 下个节点 | 节点描述 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 读回 | 19a | 读回 drawio | 开始 | drawio 文件 | 外部图的读回规则（`references/import-existing.md` §1） | 节点 / 边 / 几何 | 脚本 | xml_reader.py | — | →19b | 用户在图上可以自由编辑，读回时不做限制：**能读回的只有名称、形状、连线与坐标**——语义属性图里一个都不写（见 `references/visual-spec.md` §1），所以读不回也不该读回 |
| 对比 | 19b | 与流程表比差异 | 任务 | 图 + 流程表 | 差异四分类（增删节点 / 关系变化 / 文字变化 / 坐标位移） | 差异清单 | 脚本 | sync.py | — | →19c | 差异分四类：**增删节点**、**关系变化**、**文字变化**（名称 / 类型 / 标签）、**坐标位移**。前三类是语义，只有第四类是几何；**四类都空就是图上什么都没动**（主表据此直接交付）；判据是 `(标签, 目标)` 配对——没碰过的分支，回写后一字不差 |
| 冲突 | 19c | 「下个节点」冲突？ | 判断 | 差异清单 | `(标签, 目标)` 配对——表说 A、图说 B | 有 / 没有 | 脚本 | sync.py | — | 有→19d ｜ 没有→19e | ★ 表说某条分支去 A、图里却去 B：这是方向性冲突，不是正常增删，不能静默覆盖 |
| 拦截 | 19d | 拦下并回报 | 结束 | 冲突清单 | — | 拦截报告（退出码 1） | 脚本 | sync.py | — | — | 退出码 1，流程表不动；哪边对由主表 11 裁决——表对就重跑 16 让图跟上，图对就回 11 把表改对 |
| 回写 | 19e | 回写流程表 | 结束 | 差异清单 + 原表 | 最小差异原则（原文一致的分支逐字回填） | 更新后的流程表 | 脚本 | sync.py | — | — | 语义（名称 / 类型 / 标签 / 走向 / 输入 / 依据 / 输出）的落点是流程表，写回完毕即本表终点；"接下来往哪走"是主表的判断（都没变→交付 ｜ 改了语义→回 11 重落表 ｜ 只动几何→回 16 重布局）。几何只落进 DSL，交回算法定终值 |