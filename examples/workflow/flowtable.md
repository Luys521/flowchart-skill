---
id: workflow
level: L0
description: flowchart-skill 自举：把散落材料整成流程表，再渲染成图
---

# flowchart-skill 自举：一条流程从材料到交付

## 主体配色

| 执行主体 | 颜色 |
| -------- | -------- |
| 用户 | #dae8fc |
| AI | #d5e8d4 |
| 脚本 | #ffe6cc |

## 流程表

| 项目运作阶段 | 节点编号 | 节点名称 | 节点类型 | 输入 | 依据 | 输出 | 执行主体 | 执行者 | 行动所需时间 | 下个节点 | 节点描述 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 读材料 | 01 | 收到材料 | 开始 | 原始材料、起止范围 | — | 原始材料（转交 01a） | 用户 | 用户 | — | →01a | 一次任务的入口：目录 / 单份文件 / 一段口头需求都算，**材料只读、永不修改**。触发形态不限：口头需求 / 合同文档 / 表格 / 图片白板 / **已被直接改过的流程表**；**15 的答复也从这里进来**——每轮补充的信息都是新材料，这张表的入口不止开一次 |
| 读材料 | 01a | 建目录 | 任务 | 流程名称 | 两份模板与目录约定（`SKILL.md` 读材料段） | `<成果根>/<流程名>/` + 两份模板 | 脚本 | init.py | 1-2 秒 | →02 | ⚠ 材料没给流程名时按材料主题起一个（原文未写、按惯例补）。`init.py <流程名> -d <成果根>` 建目录并把 `templates/` 两份模板拷进去——**目录名就是流程身份**，产物名跟着它走 |
| 读材料 | 02 | 读回现状对齐口径 | 任务 | 已有流程表（可无） | `⚠` / `⚠?` 的分拣口径（`references/flowtable-spec.md` §2） | 现状复述 | AI | AI | — | →03 | ⊞ parts/读回现状/flowtable.md；**每轮开工第一件事：手上有什么就读什么**——已有流程表就用 `clarify.py` 读回来（它只读不写），报出哪些是 AI 推断、哪些还没定；没有表就把材料要点复述一遍，给用户当场纠正的机会。**已定的不重复问**——「还剩哪些要问」由 14 独家算 |
| 读材料 | 03 | 探测分档 | 任务 | 材料目录 | `SKILL.md` 读材料段（探测） | `materials[]`：档位 + 类型 + sha256 + 修改时间 | 脚本 | probe.py | 1-2 秒 | →04 | **按内容判，不按扩展名**：同一个 `.pdf` 可以跨两档、改名件按魔数认。分档是**记账**不是门槛——读不动的逐份写明原因并给出路，探测结论一律进账，不许**默默用**、也不许**默默不用** |
| 读材料 | 04 | 侦查：排序与假设 | 任务 | `materials[]` | `SKILL.md` 读材料段（侦查） | `recon.md`（难度排序 + 结构缩样 + 假设表草稿） | 脚本 | recon.py | 2-10 秒 | →05 | ⊞ parts/侦查/flowtable.md；先廉价后昂贵：元数据 → 难度排序 → 先读易的。脚本只给机器可算的列（档位 / 规模 / 处置建议）；**「假设角色 / 依据 / 验证方式 / 状态」由 AI 填**，被推翻也要留痕 |
| 读材料 | 05 | 解析入账 | 任务 | `materials[]` | `references/flowtable-spec.md` §3（证据层）· `references/material-to-nodes.md` §6 | `evidence.json`（唯一事实源） | 脚本 | parse.py → ledger.py | 5-60 秒 | →06 | 适配器按固定顺序跑，收窄（`--only` / `--pages` / `--slides` / `--grep`）一律留痕。**这一步之后下游只许引用 element id，不许回头读原件**——「依据」列指的就是它们。⚠ 清洗的四条不许（不改字 / 不合并去重 / 不语义分类 / 不"修好"读不动的）在这里生效 |
| 读材料 | 06 | 定范围：主体 / 目的 / 材料根 | 任务 | 已读到的材料 + 侦查结论 | `references/material-to-nodes.md` §1（先定主体与目的） | 三格答复（落 `plan.md` 表头） | AI ↔ 用户 | AI 问、用户答 | — | →07 | ⚠? **唯一一件值得在切句前打断的事**：答不出就先问，**不许拿"材料里最强的那条流程"当默认**（盲读实测里那么干过：表全绿、却答了另一条流程）。⚠ 放在认料之后是刻意的——没看见材料全貌之前，"主体是谁"问不到点子上；纪律只要求**在拆解之前**问清 |
| 清点 | 07 | 要分堆吗？ | 判断 | 含流程的材料 ≥ 3 份 | `SKILL.md` 清点段 | 分堆结论（材料 ↔ 流程归属） | AI | AI | — | 是→08 ｜ 否→11 | ⚠ "分堆不可跳过"与"清点、计划是**工位**不是开工前的关卡"这两句同时成立 ⇒ 它只能落成一个**判断**：单份材料 / 单条流程时平凡成立、直接落表；多份材料才先分堆。纪律：**宁可多拆一条，不可错并两条** |
| 清点 | 08 | 清点成卡片 | 任务 | `evidence.json` | `SKILL.md` 清点段 | `intake.md`（一份材料一行） | AI | AI（脚本 build/check） | — | →09 | 脚本先填机器列（档位 / 读不动 / `重复`）；**「主题 / 含流程 / 版本关系 / 依据」是 AI 填的**（`是` / `否` 必须给理由并标 `⚠`）。卡片是账本的**标注层**：只标注、不新增真值 |
| 计划 | 09 | 拆解成计划 | 任务 | `intake.md` | `SKILL.md` 计划段 | `plan.md`（做几张 / 谁依赖谁 / 谁能并行） | AI | AI（脚本 build/check） | — | →10 | 出种子 → 强合并（脚本做）→ 弱合并（AI 判：共享 ≥2 个起止点 / 交付物）→ 命名 → 定甲 / 乙 / 丙 → 并行分组。**要不要拆子流程、该并还是拆，是 AI 的判断**（写进「注记」并标 `⚠`），**不许写成澄清申请的问句** |
| 计划 | 10 | 计划要改吗？ | 判断 | `plan.md` | `SKILL.md` 计划段 | 用户的纠正 | 用户 | 用户 | — | 改→回 09 ｜ 不改→11 | **人审这一层最便宜**：在计划阶段说一句"这两条其实是一条"，比画完两张图才发现便宜一个数量级。⚠ `plan.md` 不进机器门禁——两条 `check` 只核可机器核的那几条，不核"拆得对不对" |
| 落表 | 11 | 落流程表 | 任务 | 材料 + 答复 + 计划 | 字段登记表（`references/flowtable-spec.md` §2） | 十二列流程表 | AI | AI | — | →12 | ⊞ parts/落表/flowtable.md；★ 全流程**唯一**的语义落笔点：校验不通过、用户答复、图上的语义改动、表被直改、缺口派活，都回到这里改表。**列填不满的地方，就是流程没说清的地方**——主动提问、给出候选、补上缺失项 |
| 校验 | 12 | 结构校验？ | 判断 | 流程表 | H1–H8 + 表头 H9 + 证据层 H10（`references/flowtable-spec.md` §3） | 通过，或逐条硬错误 | 脚本 | table_to_dsl.py | — | 通过→13 ｜ 不通过→回 11 修正 | ⊞ parts/结构校验/flowtable.md；三层校验（① 节点：编号 / 语义 · ② 类型：起止、出边、分支 · ③ 关系：引用、连通、回路），硬错误**阻断渲染**。它拦得住**写错**的表，拦不住**理解错**的表 |
| 标注 | 13 | 逐行回看内容出处 | 任务 | 流程表 | 依据不来自材料就标 `⚠`（`references/material-to-nodes.md` §6） | `⚠` / `⚠?` 留痕 | AI | AI | — | →14 | ⊞ parts/回看出处/flowtable.md；材料说的照写并指到 element id；AI 按常理补的打 `⚠` 并写明依据；推不出的打 `⚠?`。**打标不阻断**——标完照常往下，仍未决的由 14 报出 |
| 标注 | 14 | 有待裁决项？ | 判断 | 流程表 | frontier = 前驱已定的 `⚠?`，只问当前可问的 | frontier 清单 | 脚本 | clarify.py | — | 有→15 ｜ 没有→16 | `clarify.py` 算出 frontier（当前能问的）——这是**算出来的状态**，不是看法；**只问 frontier 里的**，上游还没定的先不问。frontier 为空即收敛 |
| 标注 | 15 | 问并收答复 | 任务 | frontier 清单 | — | 答复（新材料） | 用户 | 用户 | — | →回 01 当新材料 | 每问附 AI 的推荐答案，用户只需否决。**与 11 的分工**：11 问的是「列填不满」，这里问的是「两条路都说得通、必须业务方拍板」。**答复是新材料**——收完回 01 重走一圈再落表，不在问答处就地改表。仍有未决的照常交付，但交付时如实报「仍未决 N 处」 |
| 出图 | 16 | 渲染三份视图 | 任务 | 通过校验的流程表 | 六环 + 产物侧九项判据（`references/visual-spec.md` §4.1） | 三份产物 + 层级索引 + sha256 回执 | 脚本 | build.py | 5-30 秒 | →17 | ⊞ parts/渲染/flowtable.md；一条命令跑六环，任一环不过即 exit 1 并把产物还原——盘上不留没审过的交付物 |
| 看 | 17 | 视觉自检通过？ | 判断 | 三份产物 | 看三样：交叉多不多 / 标签压不压线 / 走向反不反直觉（`references/visual-spec.md` §5） | 看过的结论 | AI | AI | — | 通过→18 ｜ 有问题→回 16 调几何 | ⊞ parts/视觉自检/flowtable.md；用 `shot.py` 出图后**由 AI 看着核对**：交叉是否多到影响追踪、标签是否居中压线、分支走向是否反直觉。质量门禁只管几何，好不好看必须看图 |
| 回流 | 18 | 表被直改过吗？ | 判断 | 流程表 + 上次渲染记进契约的指纹 | 现算指纹 vs `source_sha256`（`SKILL.md` 回流段） | 同版 / 已过期 | 脚本 | table_to_dsl.py | — | 有→回 11 重走 ｜ 没有→19 | ★ 拿 `flowtable.md` 现算指纹，对照上次渲染写进 `<流程名>-flow.manifest.json` 的那一份（`table_to_dsl.py --fresh`）：**变过**说明有人绕过流水线改了事实源，回 11 重走校验与出图——**不许走 19**（回灌是按图里的节点集合重建表格行的，手工加的节点会被删掉、改过的名字会被覆盖）；**没变**→19 看图上动了什么。没有基线时 `--fresh` 报「无从判断」，照样按「改过」走保守路线 |
| 回流 | 19 | 读回并分拣改动 | 任务 | drawio + 流程表 | 差异四分类（增删节点 / 关系变化 / 文字变化 / 坐标位移） | 回写后的流程表 | 脚本 | sync.py | 2-10 秒 | 都没变→20 ｜ 改了语义→回 11 重落表 ｜ 只动几何→回 16 重布局 | ⊞ parts/同步回写/flowtable.md；**图这一面不另存指纹——本来就要解析**：元素与关系都没变、只有坐标位移 → 几何，回 16 交算法判吸附阈值（**像素不作终值**）；**增删节点 / 改关系 / 改文字（含名称、标签、形状）→ 语义**，先回写进流程表，再回 11 重走校验与出图。两者都有时按语义走；**diff 为空即静默通过**，不许因格式噪声把人拉回 11 |
| 循环 | 20 | 对账：漂移 | 任务 | 流程表 + 账本 + 假设账 + 清点 | `SKILL.md` 循环段（D1—D5 与缺口清单） | `drift.md`（漂移账 + 缺口清单 + 依据分布） | 脚本 | drift.py | 2-10 秒 | →21 | ⊞ parts/对账/flowtable.md；★ **循环的发动机**：拿表去比"已经知道的"（账本 / 假设账 / 清点）。分得清两类：**"表写错了"** → 漂移账；**"还有东西没看"** → 缺口清单，它**派活**。对不上的逐条给「走哪条路」，AI 只需填处置与依据 |
| 循环 | 21 | 收敛了吗？ | 判断 | `drift.md` | 收敛判据：硬漂移 0 且没有「待取证」（`SKILL.md` 循环段） | 收敛结论 | 脚本 | drift.py check | — | 收敛→24 ｜ 没收敛→22 | ★ 收敛才许交付：每条漂移都要有处置（`已修` 会被重跑判据验真，`已解释` 必须写依据），每条缺口都要有结论（`已取证` 写清要哪一片、`已放弃` 写理由）。⚠ 超预算（每份材料攻坚上限 2 轮）的落 `⚠?` 交用户——**不许无限重试** |
| 循环 | 22 | 缺口在哪一头？ | 判断 | 缺口清单 | 两类缺口的二分（`SKILL.md` 循环段） | 材料侧 / 表侧 | AI | AI | — | 材料侧→23 ｜ 表侧→回 11 | 分得清才派得对活：**材料侧** = 还有东西没看（去 23 撬开）；**表侧** = 看过了没写进去（回 11 改表）。这一问不许跳过——把两类混成一句"再补补"，等于没派活 |
| 循环 | 23 | 攻坚：点名取子集再读 | 任务 | 缺口清单 | 只查不抽 · 一批一小口 + 游标（`SKILL.md` 循环段） | 只取该看的那几处（不灌满上下文） | 脚本 | query.py · render_pages.py | 1-5 秒 | →回 05 重新入账 | `query.py` 按材料 / 词 / 行段点名取子集；`render_pages.py` 把扫描件与质量门不过的材料渲成图交给会看图的模型（**视觉来源一律按推断档**：`certainty` 不是逐字引用）。取回的东西**必须回 05 重新入账**，不许直接落表——绕过账本等于给「依据」列造一条查不到的引用 |
| 交付 | 24 | 交付 | 结束 | 19 的回写结果 + 全部产物 | — | 三视图 + `flowtable.md` + `checklist.md` + 层级索引 | 用户 | 用户 | — | — | 三份视图（html 评审 / drawio 协作 / svg 中间态）+ `flowtable.md`（唯一事实源）+ `checklist.md`（自检报告）+ `<流程名>-index.md`（层级索引，多张表时的构建顺序）。**有子表就一并发 `parts/`**。产物末尾打印 sha256 回执。**仍未决的 `⚠?` 照常交付**，但交付说明里如实报「仍未决 N 处」 |
