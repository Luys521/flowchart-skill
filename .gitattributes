# 本仓的产物靠「逐字节不变」当安全网：dev/baseline/ 是比对基准，面③ 的幂等检查比字节，
# 而工作树里同时存在 CRLF 与 LF 文件。Git 一旦做 EOL 归一化（Git for Windows 的系统级
# 配置就是 core.autocrlf=true），checkout 出来的字节会随平台/客户端漂移，那些比对会无声失效。
# ⇒ 全部按二进制处理：Git 不碰内容，提交的是什么字节、取出来就是什么字节。
# 代价：git diff 对文本文件不给行级差异——本仓靠四面九门裁决，不靠人读 diff。
* -text