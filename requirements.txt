# flowchart-skill 的 Python 依赖（**自包含侧**，见 PIPELINE-SPEC 1.4）
#
# 装：python -m pip install -r requirements.txt
# 缺依赖时脚本必须报**可执行**的错（说清装什么），不许静默降级。
#
# 必须（跑通主链）
PyYAML>=6.0          # 流程表 frontmatter / 字典（已有）
python-docx>=1.1     # OOXML：.docx 读取（解析适配器用）
openpyxl>=3.1        # OOXML：.xlsx 读取（解析适配器用）
pdfplumber>=0.11     # PDF **文本层**抽取（纯 Python，无外部二进制；scan 件不归它管）
                     # 同一份依赖也管**渲染**：`page.to_image()` 走它声明的 pypdfium2（无外部二进制）
#
# 可选（增强，缺了该格式降级为 T4）
# python-pptx 不需要：.pptx 由 scripts/pptx_text.py 用标准库读（zip + ppt/slides/*.xml）
# pytesseract>=0.3    # 图片 OCR（需另装 tesseract 二进制）
# paddleocr>=2.7      # 图片 OCR（备选）
#
# 可选外部工具（不是 Python 包；缺了 legacy（.doc/.xls/.ppt）记 T4 + 提示用户另存为 .docx）
# LibreOffice / soffice >= 7    判据：`soffice --version` 能通（文件存在 ≠ 能跑，见 parse_legacy.find_converter）
# OCR（图片 / 扫描件走 OCR 时需要，另一个自包含选项是宿主多模态）：
# tesseract 二进制（配 pytesseract）/ paddleocr 运行时
